export interface Trip {
  trip_id: string;
  trip_number: number;
  truck_id: string;
  unit_number: string;
  driver_id: string;
  driver_name: string;
  trip_date: string;
  trip_status: string;
  odometer_start: number;
  odometer_end: number;
}
