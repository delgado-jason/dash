# MEMORY.md — Live State & Behavior Log

> **READ THIS FILE FIRST**, before `CLAUDE.md`, before any action.
> This file holds live project state and a running log of behavior/functionality changes.
> **Whenever Jason asks for new behavior or functionality from me, append it to the BEHAVIOR LOG below and update the relevant state.**

---

## HOW TO USE THIS FILE

1. On session start: read this file top to bottom, then read `CLAUDE.md`.
2. Before acting: check "Active work" and "Open decisions" so you resume correctly (don't re-derive or re-scope settled things).
3. When Jason requests new behavior/functionality: add a dated entry to the **BEHAVIOR LOG** and update state sections as needed.
4. Keep this file current — it is the source of truth for _where things are_.

---

## CURRENT FOCUS

**Active arc:** `dash` — **Expenses page / P&L upload — issue #163 (Phase 1 SHIPPED v1.27.0, PR #164 `f7c7213`, 2026-07-09).** ⚠️ **RUN MIGRATION `024` ON PROD at deploy** (ran on dev only) or `/expenses` 500s. Ship had a transient branch-push failure that misplaced the v1.27.0 tag on the old dashboard commit → recovered (deleted+recreated tag on correct commit, re-pushed, merged). NEXT: Phase 2 (wire CPM app-wide). Roadmap step 2.

**Monthly obligations feature — issue #165 (SHIPPED v1.28.0 2026-07-09).** Balance-sheet insight (Jason uploaded a Balance Sheet, wanted loan/tax payments captured): P&L correctly EXCLUDES loan PRINCIPAL + owner draws (not expenses — they're balance-sheet/equity); only interest is a P&L expense (already shown); payroll taxes ARE already on the P&L. Balance sheet shows balances OWED, not payments made → can't auto-import. So: user-level recurring `obligations` (label/amount/active) → TRUE cash break-even = operating cost + obligations. Built: migration `025_obligations.sql` (RUN ON DEV pending Jason), `obligationServices.js` + `obligationRoutes.js` (`/obligations` CRUD, mounted in server.js), `getCashMetrics` in `expenses.ts` (2 tests), frontend `obligationsService.ts` / `types/obligation.ts` / `components/expenses/ObligationsCard` (list + add/edit/delete + Obligations/mo + True cash out + True break-even RPM) wired into ExpensesPage. 54 tests green, build green. NOT committed. v1 SIMPLIFICATION: obligations applied uniformly to ALL months (not effective-dated). Separately told Jason: cost/mile + break-even inflated because his LOADS/miles for those months aren't fully entered (not a calc bug — ~2,000 mi/mo implies missing loads). **REFINEMENTS 2026-07-09 (dev feedback):** (a) month-selector chips now CHRONOLOGICAL (ascending, oldest→newest — was reversed); (b) cost/mile + break-even + true break-even now **BLENDED over trailing 3 months** (Σcost ÷ Σmiles over the same window — variable cost moves with miles so both must be trailing; not this-month-cost ÷ avg-miles) labeled "· Nmo" (`ExpensesPage` `trailing` memo; obligations card gets avg-monthly cost+miles); (c) obligations card hint "loan **principal only** — interest already on P&L" (Jason caught the interest double-count: full payment double-counts the P&L interest). 54 tests green, build green. **SHIPPED v1.28.0** (PR #166, 2026-07-09) — obligations + all three refinements, backend+frontend+migration `025`. ⚠️ DEPLOY PENDING: migration `025_obligations.sql` must RUN ON PROD + deploy backend/frontend (Jason had only run it on dev). Design signed off via mockups: cost KPIs (monthly/weekly cost, cost-per-mile, break-even RPM, net margin), fixed/variable split, top categories (%rev), + **editable ledger** (reclassify fixed↔variable, edit value, add, delete; reclassify sets the category default going forward). Architecture: **FRONTEND parse (papaparse) → confirm-before-save → POST structured to backend**; backend stores monthly expense periods + classified lines + per-category fixed/variable defaults (NEW migration). **CHUNK 1 DONE + tested 2026-07-09:** `frontend/src/lib/pnl/parsePnl.ts` — `parsePnlRows` (collapses QuickBooks nested "Total for X" groups incl. parent-with-direct-amount; captures section totals; reconciles) + `classifyExpenses` (unchanged-months→fixed heuristic + trucking keyword defaults); **8 tests green** incl. single-month. **IMPORTANT (Jason 2026-07-09): the NORMAL upload is a SINGLE month (prior period only) — the 2-month comparison is a bonus, not the backbone.** So classification leans on REMEMBERED per-category defaults (backend) + keyword defaults; parser handles 1 or 2 amount columns (prior→null). Built against Jason's real P&L (fuel in COGS; Landstar commission netted into income vs app's gross load revenue — reconcile Phase 2). **ADDED to Expenses page scope (2026-07-09): YTD revenue-vs-expenses chart** — Jason has a year of P&Ls to upload; plot each stored month's P&L income vs total cost (gap = profit). Chunk 1 ✅ parser. **Chunk 2 ✅ backend (2026-07-09):** migration `024_expenses.sql` ran on dev (3 tables: expense_periods / expense_lines / expense_category_defaults + `expense_type` enum); `backend/src/services/expenseServices.js` (`saveExpensePeriod` = upsert period + replace lines + remember defaults, in a txn via `db.pool.connect()`; `getExpensePeriods`, `getExpensePeriod` w/ lines, `getCategoryDefaults`, `addExpenseLine`/`patchExpenseLine`/`deleteExpenseLine` each `recomputePeriodTotals`); `expenseRoutes.js` mounted at `/expenses` in server.js; node-check clean. API: POST `/expenses`, GET `/expenses`, GET `/expenses/:id`, GET `/expenses/defaults`, POST `/expenses/:id/lines`, PATCH/DELETE `/expenses/lines/:lineId`. **Chunk 3 ✅ (2026-07-09):** papaparse installed; `services/expensesService.ts`, `lib/pnl/parseFile.ts` (papaparse→parsePnlRows→classify w/ remembered defaults + `labelToMonth`), `lib/metrics/expenses.ts` (`getExpenseMetrics` — 4 tests), `types/expense.ts`; components under `components/expenses/`: `ExpenseUpload` (upload→confirm→save), `ExpenseLedger` (fixed/variable groups, reclassify/edit-value/add/delete), `ExpenseYtdChart` (recharts income-vs-cost); `ExpensesPage` (KPIs, split, ledger, chart, month selector); route `/expenses` + nav. Month miles from loads' odometers. **52 tests green, build green. DEV-TESTED 2026-07-09 — Jason uploaded real May+June P&L: PARSER WORKED (all categories/amounts correct, reconciles). Bugs found + FIXED same day:** (1) used dead Tabler `ti` icon classes (app = lucide-react, no Tabler font) → all Expenses action icons + shipped Lanes chevrons INVISIBLE → swapped ALL to lucide (ExpenseLedger, ExpenseUpload, LanesTable; AlertBanners still uses `ti` but never rendered yet → fix at alert-engine); (2) YTD chart → **2 clean lines (Income/Cost) + rich hover tooltip (income/cost/profit/margin%)**, moved under KPIs; (3) add-expense cancelable; (4) COGS "Cost of goods sold" → **"Fuel"** on import (`parseFile`; NEW uploads only — existing saved rows unchanged until re-uploaded). NOT COMMITTED. Minor (not bugs): high CPM/break-even = thin dev-load miles (real data fixes); $0/negative lines are real P&L data (deletable in ledger).** Ship = backend+frontend+migration `024`, `feat` → **1.27.0**. Then Phase 2 = wire CPM app-wide (real break-even replaces $2.96, net profit on dashboard, profit-per-lane). **Phase 2 (separate arc):** wire CPM app-wide — real break-even replaces hardcoded $2.96, net-profit on dashboard, profit-per-lane on Lanes. **OBLIGATIONS FOLD-IN (SHIPPED v1.29.0, PR #167, 2026-07-09 — dev-verified by Jason "works perfectly"; frontend-only, NO migration):** Jason's feedback post-deploy — obligations only moved the one card, headline KPIs + chart ignored them; he wants ONE set of numbers (true cost = P&L operating + active obligations; revenue − true cost = margin; per-mile off true cost). Built: `getTrueMonthly(operating, oblig, income)` → {trueMonthlyCost, trueWeeklyCost, trueNetMargin} (this-month basis) + 2 tests; ExpensesPage lifts obligations to page level (`reloadObligations`), computes `obligationsTotal` (active only), feeds KPIs via getTrueMonthly (dollars, this-month) + getCashMetrics (per-mile, trailing blend); Monthly/Weekly/Cost-mile/Break-even/Net-margin ALL now true-cost; chart gets `obligationsTotal` prop (adds to each month's cost → "true cost" line); ObligationsCard slimmed to a pure LIST manager (removed the redundant true-cash stat trio — now the headline) + added a real **active toggle** (Eye/EyeOff → patch active; the old card had NO toggle despite the model supporting it); reconciliation line on fixed/var card (operating + obligations = true monthly). obligationsTotal=0 → identical to operating (no regression). 56 tests + build green. Semver: feat → **1.29.0** (frontend-only, NO migration). **OPS SHEET RECON (2026-07-09):** Google Drive `DTS_Operations` sheet (id `1Lz6fEmB-mNZzsqRcm7xfpu4Yh4Prh88QvU8JrXXMjRg`, in My Drive > Operations > Workbooks) — the "Expenses" tab is the SOURCE of the hardcoded $2.96 and a hand-built version of the app's Expenses page. **Revenue targets** = tiers on break-even/LOADED mile: Walk-Away = total CPM ($2.99) ÷ PTR (0.73) = $4.10; Minimum(15%)=×1.15=$4.71; Target(35%)=×1.35=$5.53; Strong(60%)=×1.60=$6.55. Assumptions on sheet: Avg miles 8500/mo, PTR 0.73 (loaded÷total). Also Breakeven Gross $8,096/wk (= weekly cost ÷ PTR) + Daily Rate $1,582 (gross-pace targets). PROPOSAL PENDING: derive these live from the app's true break-even RPM (already computed) as a "Rate targets" strip on Expenses + echo Target on Dashboard; tiers 15/35/60 configurable. **DECISION (Jason 2026-07-09): live-from-loads, BUT the break-even basis = avg cost ÷ avg loaded miles over the 3 most recent COMPLETE months (EXCLUDE the in-progress current calendar month — it inflates per-mile since miles lag cost). Anchored to today, NOT tied to the selected month tag (targets are forward pricing guidance).** Same window for numerator + denominator. Build order: ship fold-in v1.29.0 first, then Rate Targets strip (Expenses) + Target rate on Dashboard as its own issue. Open sub-Q at build time: keep Expenses per-mile KPIs trailing-from-selected (historical context) vs unify to the anchored prior-3-complete basis. This is the natural Phase 2. **DASHBOARD PLACEMENT DECIDED (Jason 2026-07-09, via mockup):** dedicated "Rate and pace targets" card DIRECTLY UNDER the KPI strip (above the charts). Contents: (a) **rate ladder** per loaded mile — walk-away/target/strong with a marker for current Avg RPM, colored red-below-breakeven→green-above-target; (b) **this-week gross pace bar** — MUST include BOOKED freight (sum rate of all loads running the current week Mon–Sun regardless of status: booked/in-transit/delivered, anchored on delivery date — mid-week committed+earned view), with TWO ticks = break-even floor + 35% target ("show both", my rec, Jason deferred to me); (c) **daily rate target**. Same rate-ladder strip also on Expenses page by the break-even. **This work RETIRES `BREAK_EVEN_RPM=2.96` (frontend/src/lib/constants/targets.ts, has a TODO) AND fixes a real bug: dashboard currently compares Avg RPM (revenue/LOADED mile) to $2.96 (cost/TOTAL mile) = apples-to-oranges, flatters profitability; correct comparison is vs walk-away (~$4.10, break-even per LOADED mile).** **MATH CATCH:** Jason's sheet Weekly Gross $8,096 + Daily $1,582 = cost ÷ PTR (÷0.73 an EXTRA time — assumes earning the rate on empty miles too); correct break-even gross = the cost itself (weekly/daily TRUE cost incl. obligations, ~$6,800/wk). Derive live + correctly, do NOT copy the sheet. Daily = monthly true cost ÷ 22 working days (configurable), weekly = monthly ÷ 4.333. Tiers 15/35/60% + working-days = configurable constants to start. PTR live-derived = loaded÷total over prior-3-complete-months. Its own issue/PR → v1.30.0. **SHIPPED v1.30.0 (PR #168, 2026-07-09 — dev-verified "looks great"; frontend-only NO migration):** engine `frontend/src/lib/metrics/rateTargets.ts` (pure, `now` param for testability): `completeMonthsBefore` (3 complete months before current, excludes in-progress), `getCostBasis` (blends true cost=operating+active-obligations ÷ loaded miles over those months, only months WITH a P&L counted so cost/miles stay aligned; returns breakEvenRpm=walkAway, windowRpm=your rate, trueMonthlyCost, months), `getRateLadder` (walkAway/minimum/target/strong = ×1+tier), `getGrossTargets` (weekly=÷4.333, daily=÷22, +target markup), `payWeekRange` (Wed–Tue via `PAY_WEEK_START_DOW=3`), `getWeekBookedGross` (Σ rate of non-cancelled loads delivering in [start,end), all statuses). Constants in `lib/constants/targets.ts`: `RATE_TIERS{minimum:.15,target:.35,strong:.6}`, `WORKING_DAYS_PER_MONTH=22`, `PAY_WEEK_START_DOW=3` (TODO→settings page), `BREAK_EVEN_RPM=2.96` now FALLBACK-only. Hook `hooks/useRateTargets.ts` (fetches periods+obligations, assembles). Components `components/dashboard/RateLadder.tsx` (shared ladder viz, marker) + `RateTargetsCard.tsx` (ladder + this-week booked PaceBar w/ floor+target ticks + daily target). DashboardPage: card under KPI strip; **retired hardcoded $2.96 → Avg RPM card status + RpmChart now use liveBreakEven=walkAway (fallback 2.96); FIXED apples-to-oranges (was Avg RPM vs cost/total-mile).** Dashboard ladder marker = all-time avgRpm (matches Avg RPM KPI, self-consistent); Expenses ladder strip marker = windowRpm (recent). 12 new tests (68 total), build green. Rate ladder also on ExpensesPage (anchored today, not selected month). Caveat: per-mile numbers only as good as ENTERED LOADS; break-even reads high until miles in. **REFINEMENT (SHIPPED v1.30.1, PR #169, 2026-07-09):** dashboard "your rate" split into two: **Avg RPM KPI = rolling 3-COMPLETE-month blended RPM** (`getWindowRpm`, loads-only, relabeled "Avg RPM · 3mo"; replaced all-time `getAverageRPM`) — same window as the break-even it's compared to; **card ladder marker = CURRENT pay-week RPM** (`getWeekRpm` = this-week booked+delivered revenue ÷ loaded miles, captioned "marker = this week"). Both added as pure fns + 4 tests (72 total). Expenses ladder marker stays windowRpm (3mo). Hook exposes weekRpm + rollingRpm.

**NEXT BIG ISSUE — MAINTENANCE PAGE (DESIGN PHASE 2026-07-09, NOT built; awaiting Jason's direction — he dismissed the layout/scope questions, wants to respond in his own words).** Two tabs: **Schedule** (forward "what's due") + **Services** (event log). Track odometer OR date depending on service type. Units: **Tractor 580991 = International LT625 w/ Cummins X15**, **Trailer 780991** (separate HUBODOMETER). Current tractor odo ~568,737 (May 2026). Recon: he already keeps `DTS_Maintenance_Tracker` Google Sheet (id `1IM5WPk5ckS0MaeRsEYp7gjQsHM8-ClGT1o_m5HaAEEY`, Operations folder) — chronological log w/ cols Date/Unit/Vendor/Location/Service/Mileage-Hub/Cost/Invoice#/Receipt/Notes, "used to build maintenance reports for Landstar." His real PM cadence ~22–25k mi / ~3 mo = confirms SEVERE duty. **Researched severe-duty schedule (starter, confirm vs actual O&M manuals):** X15 — oil+filter 25k/6mo, fuel filter+separator 25k, coolant filter 50k/12mo (SCA 30k/6mo), crankcase breather ~150k, DEF filter ~300k, DPF clean 250–400k, **valve lash/overhead ~500k/5yr (⚠ he's at 568k — likely DUE)**. LT625 chassis — chassis lube 25k (every PM), diff/axle fluid (synthetic) 250k/3yr, trans fluid ~500k (confirm trans model), air dryer desiccant ~3yr/100–150k, wheel seals inspect/PM, alignment+HVAC filter 12mo, brake inspection every PM. Compliance (date) — **FHWA annual DOT insp 49 CFR 396.17 tractor+trailer, next 09/08/26**, IFTA/plates/registration 12mo. Trailer — lube/brakes/ABS/lights/tape every PM (hub miles). Data model: each item has mileage interval &/or time interval (due = whichever first) + unit (tractor odo / trailer hub / engine hrs); logging a matching Service RESETS the item; current odo can auto-pull from latest load odometer_end. Showed 3 layout mockups: **A status board (recommended)**, B register table (like his sheet), C odometer runway (batch shop visits). Proposed extras: predicted due-DATES (from miles/mo), feed dashboard alert region (first real alert content), Landstar report export, cost→Expenses tie-in, receipt attachments. My lean: A+B hybrid v1, C phase 2. Sources: Cummins X15 QRG (mart.cummins.com), FleetRabbit LT PM, oxmaint/semitruckoil intervals. **SCOPE LOCKED (Jason 2026-07-09): "go with your lean + all suggestions."** Layout = A+B hybrid; units = tractor(+engine items)+trailer; v1 extras = predicted due-dates, dashboard alert feed, Landstar export, receipt REFERENCE field (not real upload — phase 2), + **VENDOR PRICING** (Jason's reframe: DON'T roll service cost into P&L — QuickBooks already books maintenance → would double-count; cost here is for tracking vendor pricing/negotiation instead). C runway = phase 2. Receipt = filename/link field v1. Each item stores editable last_done_miles+date (denormalized; logging a matching service updates it). Build chunks: (1) DB+backend, (2) Services tab, (3) Schedule tab+due logic, (4) extras. **CHUNK 1 DONE 2026-07-09 (node-check clean, NOT committed):** migration `026_maintenance.sql` (maintenance_items / maintenance_services / maintenance_service_items join); `maintenanceServices.js` (CRUD items+services; `createMaintenanceService` txn resets linked items' last_done w/ back-date guard; `seedMaintenanceItems` = 20-item starter, only if user has none); `maintenanceRoutes.js` mounted `/maintenance` (`/items`,`/items/seed`,`/services` + PATCH/DELETE). Jason ran migration 026 on dev ✓. Jason also DROPPED receipts (stores them in QuickBooks — no receipt UI; DB `receipt_ref` column stays inert/unused). **CHUNKS 2+3 DONE 2026-07-09 (84 tests, build green; NOT committed):** `types/maintenance.ts`, `services/maintenanceService.ts` (coerce; num/date slice(0,10)), `lib/metrics/maintenance.ts` + test (12 tests: `computeDue` — mileage/time/both, worst-lens-wins, overdue/soon(≥85%)/ok/unknown, etaDate projection; `currentTractorMiles` = max load odo; `avgMilesPerMonth` = last-90d delivered miles ÷3), components `maintenance/MaintenanceItemForm`, `ScheduleTab` (A+B: summary chips + DOT chip, Board/Table toggle, seed button in empty state, add/edit/delete items), `ServiceForm` (log w/ item-completion checkboxes that reset schedule), `ServicesTab` (log table + **vendor pricing summary** — count/total/avg by vendor), `pages/MaintenancePage` (tabs, fetches items+services+loads, current tractor odo from loads / trailer hub from services, milesPerMonth). Route `/maintenance` + nav link added. **REFINEMENTS (Jason dev feedback 2026-07-09):** (a) **COMPLIANCE REMOVED from maintenance** — going on its own separate Compliance page; dropped FHWA/DOT/IFTA from seed + removed compliance chip; (b) **Transmission = Eaton Fuller**, now its OWN section: fluid change 500k mi (synthetic PS-386, per Eaton TCMT0021) + level/magnetic-plug check 25k; (c) Schedule board now GROUPS BY SYSTEM (Truck / Transmission / Trailer via `sectionOf`: category 'transmission'→Transmission, unit trailer→Trailer, else Truck), summary chips (overdue/soon/ok/no-baseline) kept; table got System column + section sort; (d) fixed Services table ODO/Service column collision (added `[&_td]:pr-5` padding) + full-year dates; (e) current odo now = max(loads, service log) not just loads (his loads lag at 314k, service log has real 568k). NOTE: seed only inserts when user has ZERO items, so his already-seeded dev data keeps old compliance/transmission rows — he deletes those + adds new, or clears all + re-seeds. **CHUNK 4 DONE 2026-07-09 (85 tests, build green):** Landstar export DROPPED (Landstar wants entry via their app, not export). Instead: **Services MONTH-RANGE filter** (From/To `<input type=month>`, default last 3 mo, "All time" clears, live "N services · $total" summary; vendor pricing + table both respect range) — lets him pull any window to re-enter into Landstar / future authority reporting. **`maxOdometer(...vals)` helper** (fuel-ready: loads+services now, one-line add when fuel page gets odometer — fuel will be freshest). **CHUNK 5 — ALERTS + "DUE SOON" REWORK + DATE PICKERS DONE 2026-07-09 (89 tests, build green):** (a) **"Due soon" redefined** from %-elapsed (broke long intervals — valve lash flagged soon 75k mi out) to LEAD-TIME: soon = projected due date within `SOON_WITHIN_DAYS=30` (uses etaDate = earlier of time-interval date or miles-remaining÷pace); overdue = past; falls back to %≥0.85 only when no pace/ETA. `computeDue` gained `soonWithinDays` param (default 30 — the one knob, settings later). (b) **Alert engine:** `maintenanceAlerts(items,currentMiles,now,mpm)` pure fn (overdue→critical, soon→warning, overdue-first, msg "<name> overdue/due soon · X mi/days", href /maintenance) → `hooks/useMaintenanceAlerts(loads)` (fetches items+services, computes currentMiles via maxOdometer) → DashboardPage `const alerts = useMaintenanceAlerts(loads)` feeds existing `AlertBanners`. **Fixed AlertBanners dead Tabler `ti` icons → lucide (Wrench/ShieldAlert) + `<a>`→router `<Link>`.** REUSABLE: Compliance page will call maintenanceAlerts-style generator too. (c) **Services date pickers upgraded** (Jason: native month input "hard to know where to click"): preset pills `3m/6m/12m/ytd/all/custom` (RangeMode) + Custom reveals labeled month `<select>` dropdowns (fmtMonth) built from months-with-services; replaced `<input type=month>`. **CHUNK 6 — PER-ITEM WARN LEAD + PICKER LAG FIX 2026-07-09 (90 tests, build green):** Jason: global 30-day "due soon" doesn't fit all (truck-wash 30-day interval can't have 30-day notice). → **PER-ITEM `warn_lead_days`** (migration `027_maintenance_warn_lead.sql`, INTEGER NOT NULL DEFAULT 14, backfills existing rows at 14). `computeDue` now uses `item.warn_lead_days ?? soonWithinDays` for the soon threshold. Backend: added to SELECT/INSERT/RETURNING/ITEM_FIELDS/seed (seed leads: frequent 14, coolant/breather/alignment 21, big-ticket DEF/DPF/valve/diff/air-dryer/trans-fluid 30). Type + coerce + MaintenanceItemForm "Warn me (days before)" field (default 14). Overdue=critical, within-lead=warning; **compliance's 2-stage (30 warn→14 pre-due critical) DEFERRED to Compliance page (would add a `critical_lead_days` field).** **PICKER LAG FIXED:** custom-dropdown `monthOptions` built one option per month back to earliest service → a stray 2006 date = ~240 options/menu = click lag; capped at `monthKey(-35)` (~36 mo). **FEATURE COMPLETE — ship → v1.31.0 pending Jason dev-test. PROD needs migrations 026 AND 027.** NOTE his already-seeded dev items get warn_lead_days=14 (DB default backfill), NOT the tuned seed values — he edits long ones or re-seeds. Lint: set-state-in-effect matches existing ExpensesPage pattern; not a CI gate. **DEV DB SEEDED VIA SUPABASE MCP 2026-07-09 (Jason: "do the migration yourself, drop current data, add seed data"):** Supabase projects — **DEV = `dts-dash-dev` id `vbiboblkyavhmnegjbrz`**, **PROD = `Dash` id `zeeglmaqitxjqzxfikwy`** (older, UNTOUCHED). Applied migration `027_maintenance_warn_lead` to DEV; dropped maintenance_items/services for user `a0000000-...-0001` (jason@example.com — the account he tests as); inserted 18 items (tuned warn leads + baselines → mix: ~6 overdue [fuel filter, valve lash, coolant(time), diff(time), HVAC(time), trailer brakes], 1 soon [oil], 1 no-baseline [DEF], rest ok) + 6 services (Jan–May, tractor+trailer, vendors Ray's Truck Express Lube/Cummins/TA Petro/Ray's Tire Service). Current odo anchored via services: tractor 568,737, trailer hub 456,123 (services > dev loads ~314k). **SHIPPED v1.31.0 (PR #171, 2026-07-09). Jason ran migrations 026 + 027 on PROD himself.** **FLEET ENTITIES FEATURE (in progress 2026-07-09, after maintenance). Scope: FLEET-READY, all 3 entities (truck/driver/trailer).** Decisions: avatars = **AI anime via fal.ai** (Jason provides API key, keep server-side; falls back to SVG if unavailable), SVG fallback = the SIMPLER flat style (Jason: "previous svg were better, the less detailed ones" — NOT the cel-shaded detailed pass); **real photo uploads via Supabase Storage**. Recon: `trucks` table already RICH (unit#/vin[unique]/plate/make/model/year/current_odometer/status enum `truck_status`=active|maintenance|out_of_service|inactive/in_service_date/soft-delete) + backend truckRoutes/driverRoutes exist, but TrucksPage/DriversPage were PLACEHOLDER stubs. `drivers` was sparse (name+active). `trips`+`fuel_entries` already had truck_id (trips also driver_id); **loads had NO truck/driver**; maintenance used only `unit` string. **CHUNK 1 (schema+backfill) DONE on DEV via MCP:** migration `028_fleet_entities.sql` (repo + applied to dev `vbiboblkyavhmnegjbrz`): new `trailers` table (mirrors trucks, reuses truck_status enum; unit_number/vin/plate/trailer_type default flatbed/length_ft/make/model/year/current_hub/status/avatar_url/soft-delete), enriched `drivers` (phone/email/cdl_number/cdl_state/cdl_expiration/endorsements/hire_date/avatar_url/notes), `avatar_url` on trucks, nullable FKs truck_id/driver_id/trailer_id on `loads`, truck_id/trailer_id on maintenance_items+services. **PROD needs migration 028 at ship.** Dev data (user `a0000000-...0001`): pre-existing seed truck `b0000000-...0001` was generic "Freightliner Cascadia" → UPDATED to International LT625 2019 odo 568737; driver `c0000000-...0001` (Jason Delgado) enriched; created trailer 780991 (flatbed 48ft hub 456123); backfilled all 8 loads (+truck/driver/trailer), 18 maintenance items, 2 trips, fuel. **CHUNKS: (2) BACKEND DONE 2026-07-09 (node-check + dev column-validated):** `trailerServices.js` + `trailerRoutes.js` (self-contained, soft-delete, mounted `/trailers` with `/me` + `/me/:id` pattern like trucks); `truckServices` getTrucks/getTruck +avatar_url + create/patch allowedFields +avatar_url (note trucks also has a `truck_name` col); `driverServices` getDrivers/getDriver + create/patch +enriched (phone/email/cdl_number/cdl_state/cdl_expiration/endorsements/hire_date/avatar_url/notes); `loadServices` getLoads SELECT +loads.truck_id/driver_id/trailer_id; `maintenanceServices` getMaintenanceItems SELECT +truck_id/trailer_id. Existing truck/driver routes use `/trucks/me` + `/drivers` (check driverRoutes paths when building frontend). DEFERRED: load/maintenance WRITE-assignment via form selectors (reads only now). **REMAINING: (3) detail pages Truck/Driver/Trailer as hubs (specs + rolled-up maintenance/fuel/loads) + lists + UN-HIDE nav (TrucksPage/DriversPage are PLACEHOLDER stubs, `/trucks`+`/drivers` commented in AppSidebar; need TrailersPage + route); (4) avatars — simple SVG defaults + Supabase Storage upload + fal.ai generation. Default single rig in UI; multi-truck selectors later.** **AVATAR STYLE LOCKED (Jason 2026-07-09): comic-book/digital-illustration via fal `fal-ai/flux/schnell` (NOT hard anime — he loved the flux output); framing = truck-0 (clean front 3/4) + driver-0.** Test gen proven working with his FAL_API_KEY (in backend/.env). **CHUNK 4 BACKEND DONE (node-check OK, NOT yet runtime-tested — needs storage key):** created public `avatars` Supabase Storage bucket on dev (5MB, jpeg/png/webp); `avatarService.js` (`generateAvatar(user,kind,id,variant)`: buildPrompt[truck/driver/trailer, STYLE const locked] → fal flux/schnell square_hd → fetch bytes → POST to `{SUPABASE_URL}/storage/v1/object/avatars/{kind}/{id}.jpg` w/ service key → set avatar_url; `uploadAvatar` for raw photo override); `avatarRoutes.js` POST `/avatars/:kind/:id/generate` {variant=male|female for driver} + POST `/:kind/:id/upload` (express.raw image/*, 5mb) mounted `/avatars`. **CHUNK 4 BACKEND RUNTIME-VERIFIED 2026-07-09 — avatar pipeline WORKS end-to-end on dev (fal→Supabase Storage→avatar_url). Generated + persisted his truck (b0000...0001) + driver (c0000...0001) avatars; both look great, on-style.** ENV SETUP (two files): `backend/.env` = PROD (has FAL_API_KEY + prod SUPABASE_URL/key), `backend/.env.development` = DEV (dev SUPABASE_URL + dev service_role key; NO fal key). Runtime: `npm run start:dev` = `dotenv -e .env.development -- node src/server`, and server.js/db-pool also `import "dotenv/config"` (loads .env but dotenv NO-override) → net: dev Supabase/DB from .env.development WIN, FAL_API_KEY filled from .env. So dev backend has dev storage + fal key. **PROD (.env) needs the `avatars` bucket created on prod project (zeeglmaqitxjqzxfikwy) at deploy — only dev bucket exists so far.** Trailer avatar NOT generated yet (do at trailer page). **CHUNK 3 (DETAIL PAGES) DONE 2026-07-09 (90 tests, build green):** frontend types truck/driver/trailer (+ truck_id/driver_id/trailer_id on Load type & MaintenanceItem type + coerce + test factory); services trucksService (`/trucks/me`), driversService (`/drivers`), trailersService (`/trailers/me`), avatarsService (generate/upload). `components/fleet/AvatarFallback.tsx` (flat SVG truck/trailer/driver) + `EntityAvatar.tsx` (img or fallback + Generate/Upload buttons + driver male/female variant toggle, cache-busts URL). Pages: TrucksPage/TruckDetailPage, DriversPage/DriverDetailPage, TrailersPage/TrailerDetailPage (hub = avatar + specs + maintenance overdue/soon via computeDue(current_odometer/current_hub, avgMilesPerMonth(loads)) + loads/revenue filtered by truck_id/driver_id). Routes /trucks/:id /drivers/:id /trailers /trailers/:id in App.tsx; nav un-hidden (Trucks/Trailers/Drivers). **Jason must RESTART dev backend to load new routes (trailers, avatars) + loadServices/maintenanceServices SELECT changes.** DEFERRED (later polish): edit-specs forms, fuel/MPG rollup on truck page, load/maintenance write-assignment selectors, multi-truck UI. **SHIP CHECKLIST for whole fleet feature → v1.32.0: PROD needs migrations 026(already?)+027(already?)+028 run — actually 026/027 already on prod (maintenance shipped v1.31.0), so PROD needs 028 + create `avatars` bucket on prod (zeeglmaqitxjqzxfikwy) + prod .env has SUPABASE_URL/service key already.** Backend files (trailer/avatar services+routes, truck/driver/load/maintenance edits, migrations 028) + frontend all uncommitted. **FIXES 2026-07-09 (Jason dev feedback):** (1) "no trucks" bug — `trucks` table has NO `truck_name` col but truckServices getTrucks/getTruck SELECT + create/patch allowedFields referenced it → 500 → empty list; REMOVED truck_name from truckServices + frontend Truck type + pages (title now "Unit {unit_number}"). **Backend change → Jason must restart dev backend.** (2) CREATE FORMS added: `components/fleet/EntityForm.tsx` (generic field-spec form, sends non-empty fields, numbers parsed) + createTruck/createDriver/createTrailer services + "Add" buttons on TrucksPage/DriversPage/TrailersPage. Truck create validation (truckValidation.js) only REQUIRES unit_number; if vin given must be 17 chars, plate 6-9, state 2 chars, make ≤50 non-blank. Driver requires first_name+last_name. (3) trailer avatar prompt now uses `row.trailer_type` + forces STANDALONE trailer ("single detached … NO truck NO cab NO tractor, on landing gear") — flux was drawing a flatbed body-truck; regenerated his flatbed (32bab578-…) correctly. 90 tests + build green. **SHIPPED v1.32.0 (PR #172, 2026-07-09).** Deferred to follow-ups (Jason ok): inline spec editing, fuel/MPG on truck page, load/maintenance assignment selectors. **PROD DEPLOY NEEDS: migration 028 + create `avatars` Storage bucket on prod (zeeglmaqitxjqzxfikwy); prod already has Supabase + fal keys in `.env`.** **PENDING: PROD BACKFILL of REAL truck/driver/trailer data (Jason asked, MUST preserve existing prod data).** PLAN = purely ADDITIVE (insert 3 real records + UPDATE existing loads/maintenance/trips/fuel to set currently-NULL FKs, scoped to his prod user_id, idempotent `WHERE ... IS NULL`, wrapped in a transaction; NO deletes/overwrites). Process: deploy → migrate 028 → create bucket → recon prod (real user_id? existing seed truck/driver like dev's Freightliner? counts) → Jason provides real VIN/plate/CDL/etc → DRY-RUN counts → confirm + ensure Supabase backup/PITR → run txn via MCP → verify → generate prod avatars. **PROD BACKFILL DONE 2026-07-09:** Jason created truck(3686d2e6…, unit 580991)/driver(7d061a99…, Jason Delgado)/trailer(7c2988b1…, unit 780991) via app on prod; I ran transactional UPDATE linking all 56 loads (truck+driver+trailer) + 1 trip (truck+driver), verified 0 orphaned, nothing overwritten. Prod user_id 2fe59f51-155c-4a18-b9e1-1d8a30cfc59d. **PROD avatar env fix: Railway backend needed FAL_API_KEY + SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY set in Railway Variables (not just local .env — .env gitignored); prod backend on RAILWAY.** **UI FOLLOW-UPS SHIPPED v1.33.0 (PR #173, 2026-07-09; frontend-only):** (a) date fields formatted date-only via `lib/format.ts` formatDate (in_service_date/cdl_expiration/hire_date were showing tz); (b) EDITABLE — EntityForm gained `initial` prop, `lib/fleetFields.ts` (shared TRUCK/DRIVER/TRAILER_FIELDS + toFormValues), patchTruck/patchDriver/patchTrailer services, Edit button on each detail page; (c) DERIVED odometer — truck "Odometer·latest" = maxOdometer(stored, loads odometer_end for truck_id, maintenance services odometer unit=tractor); trailer hub = max(stored, services unit=trailer); FUEL pending (folds into maxOdometer + MPG when fuel page carries odometer). MINOR DEBT: list pages still have inline FIELDS duplicating fleetFields (works; centralize later). **APPROACH CHANGED (Jason 2026-07-09): he'll CREATE the truck/trailer/driver via the app on prod (real data, validated, proper IDs), then I just backfill the FK links — cleaner than hand-INSERTing.** PROD RECON (project zeeglmaqitxjqzxfikwy): migration 028 ✓ applied; `avatars` bucket CREATED ✓; **prod has NO existing trucks/drivers/trailers (clean — no seed to disambiguate)**; his prod user_id = `2fe59f51-155c-4a18-b9e1-1d8a30cfc59d` (jason@delgadotruckingservices.com, single user); **BACKFILL SCOPE = 56 loads + 1 trip to link** (maint_items=0, fuel=0 on prod — nothing to link there). After he creates ONE of each via app: recon their IDs → dry-run → transactional UPDATE loads/trips SET truck_id/driver_id/trailer_id WHERE user_id=his AND FK IS NULL → verify. Driver table has NO gender col → variant passed per-request (UI toggle). After key: generate his truck/driver avatars, then Chunk 3 pages display avatar_url + Generate/Upload buttons + SVG fallback. ⚠ **SUPABASE RLS ADVISORY: RLS DISABLED on 7 newer tables (expense_periods, expense_lines, expense_category_defaults, obligations, maintenance_items, maintenance_services, maintenance_service_items)** — exposed to anon key IF the anon key is reachable/used client-side; app uses Express+JWT so practical risk depends on whether the Supabase anon key is public. Flagged to Jason; address separately (enable RLS + policies, don't blind-enable). 

**COMPLIANCE ALERTS DESIGN (Jason wants compliance alerts INTRUSIVE/PERSISTENT — dismiss for session but return next open until item actually resolved; NOT needed for maintenance):** MY TAKE = alerts stay DERIVED-FROM-STATE (inherently persist until resolved; resolving item = only permanent clear), "dismiss" becomes a time-boxed SNOOZE stored server-side (`alert_snoozes` table: user_id, alert_key, snoozed_until); compliance snoozes kept SHORT (return next session) + hard-overdue legal items (expired DOT/insurance/registration) NON-snoozable + escalated (top-pinned banner, nav badge count, maybe login interstitial). Maintenance alerts skip snooze/escalation (auto-clear on service). Build with the Compliance page (future).

**SIDEBAR REGROUPED — SHIPPED v1.33.1 (PR #174, 2026-07-09, frontend-only):** `AppSidebar.tsx` — 11 flat items → Dashboard (top) · **Freight** collapsible (Loads/Trips/Lanes/Agents) · **Fleet** collapsible (Trucks/Trailers/Drivers/Maintenance/Fuel[/fuel-entries]) · Expenses (top). Collapse via local `open` state (no Radix Collapsible dep; uses shadcn SidebarMenuSub/SubItem/SubButton + lucide ChevronDown/Right); group auto-opens when its route is active, manual toggles of others preserved. FUTURE SLOTS: Compliance → Fleet (or own top-level); Settings → footer.

**SINGLE-ENTITY AUTO-RESOLVE PATTERN (Jason 2026-07-09): app should NOT force truck/driver/trailer selection when the user has only ONE — auto-assign; only show a picker when >1.** Implemented for MAINTENANCE (SHIPPED v1.34.0, PR #175, 2026-07-09, backend-only): `maintenanceServices.js` added `singleEntity(runner,table,idCol,user)` (returns id iff exactly 1 active, else null) + `resolveFleet` + `linkFor(unit,ids,data)`; wired into `seedMaintenanceItems` (each starter item), `createMaintenanceItem`, `createMaintenanceService` → sets truck_id (unit=tractor) / trailer_id (unit=trailer) from the single truck/trailer (or data.truck_id/trailer_id if a future selector sends it). **WHY it mattered: seeded/logged maintenance had NULL truck_id → Truck/Trailer detail pages (which filter maintenance by truck_id/trailer_id) showed nothing. Now auto-links.** `unit` (tractor/trailer) stays = asset-TYPE choice, not entity selection. Jason HASN'T loaded starter schedule on prod yet (waited for fleet backfill) — after this ships + Railway backend redeploys, loading it will auto-link to his prod truck (3686d2e6…)/trailer (7c2988b1…). REUSE this pattern for LOADS truck/driver/trailer assignment later. Frontend maintenance page needs NO change for single-rig. **SERVICE BOTH-UNITS SHIPPED v1.35.0 (PR #176, 2026-07-09; migration 029 applied dev + PROD):** a shop visit/invoice often covers tractor+trailer together (combined Landstar Ultimate PM, one cost) but a service could only be logged against one unit. Migration `029_service_both.sql`: `maintenance_services` +`trailer_hub INTEGER`, `unit` CHECK now allows `both`, and existing `unit='trailer'` rows moved their reading `odometer`→`trailer_hub` (dev had 2 such rows: 445000/456123; prod had 0 services). **Semantics: `odometer`=TRUCK reading, `trailer_hub`=TRAILER reading** (was: single `odometer` held whichever unit's reading). Backend `maintenanceServices.js`: `isServiceUnit`=tractor|trailer|both; `linkFor` sets truck_id when tractor|both, trailer_id when trailer|both; `createMaintenanceService` inserts both readings + on item-completion resets EACH item against the reading for ITS OWN unit (item.unit==='trailer'→trailer_hub else odometer, via `RETURNING item_id, unit`); getMaintenanceServices/patch/RETURNING +trailer_hub. Frontend: `ServiceUnit` type (+`both`), ServiceForm has "Both (truck + trailer)" option → reveals TWO reading fields (Truck odometer + Trailer hub) + completable-items list spans both units; ServicesTab odo column renders truck/trailer/both(stacked) via `reading()`; Truck/Trailer detail latest-reading + `useMaintenanceAlerts`/MaintenancePage per-unit max now include `both` services and read the right column (truck→odometer, trailer→trailer_hub). **Deploy order (done): migration 029 + backend redeploy BEFORE frontend** so old backend never receives `both`/`trailer_hub`. (Login-kick Jason hit = 401 = expired session, not a bug; app hard-logs-out on any 401 — soften later w/ longer token/refresh.) **HOTFIX v1.35.1 (PR #177, 2026-07-09, backend-only):** the item-reset loop used `INSERT INTO maintenance_service_items (...) SELECT ... RETURNING item_id, unit` but RETURNING only sees the TARGET table and the join table is just `(service_id, item_id)` → every service completing an item 500'd with `column "unit" does not exist`. Fix: `SELECT unit FROM maintenance_items` first (also the ownership check), then plain `INSERT ... VALUES`, then per-unit reset. **LESSON: build + 90 unit tests are GREEN but exercise ZERO real SQL — a RETURNING-on-wrong-table bug sailed through my "verified" claim. For DB query changes, actually run the statement shapes against dev (e.g. a `DO $$ ... RAISE EXCEPTION 'ROLLBACK_OK' $$` that rolls back) before claiming verified.**

_(Dashboard redesign — issue #161 — SHIPPED v1.26.0 2026-07-09, PR #162, backend+frontend, no migration. Detail below.)_

**Dashboard redesign (issue #161) — SHIPPED v1.26.0.** Bento layout + top-5 agents (clickable → agent detail) + Loads-MTD KPI + What's-next (booked/in-transit) + **reserved alert-banner region** (empty `Alert[]` source now; renders nothing when empty). Metrics `getLoadsMonthly`/`getTopAgentsByRevenue`/`getUpcomingLoads` (frozen-clock tested, 37 total green); components `AlertBanners`/`TopAgents`/`WhatsNext` under `components/dashboard/`; `DashboardPage` rewritten to bento. Instructor kept Revenue YTD (→ 5 KPIs) + the RPM chart (mockup omitted both — did NOT silently drop existing features; Jason can veto). **REFINEMENTS folded in 2026-07-09 per Jason's dev feedback:** top agents now = revenue over last 90 days + ≥2-load floor + shows load count (window kills stale, floor kills one-offs — same pattern as Lanes); drill-downs everywhere (agents→detail; outstanding/what's-next/recent-loads → `/loads/:id`; added `load_id` to those metric shapes); new **Recent loads** tile (`getRecentDeliveredLoads`); Outstanding shows total + avg aging (`getOutstandingSummary` — NOTE: aging of unpaid, not true DSO; real days-to-pay needs a payment-date we don't track yet). 40 tests green, strict build green. **BUG FIXED 2026-07-09:** getLoads LIST query omitted `agent_id` → `load.agent_id` undefined on dashboard → agent links went to `/agents/undefined` AND top-agents grouped every load under one undefined key (list was WRONG, not just mislinked). Fix: added `loads.agent_id` to getLoads SELECT (`backend/src/services/loadServices.js`). **This makes the #161 ship BACKEND + frontend (still no DB migration) — restart dev backend to re-test.** Latent mismatch flagged: getLoads also omits broker_id/agent_email/market ids the `Load` type declares — cleanup later. Not committed. Ship = `feat` → **1.26.0**.

**FUTURE FEATURE (captured 2026-07-09): "times a top agent" badge** on agent cards/directory (e.g. "3× top agent") so agents who fell off the current top-5 (via the 90d window / ≥2 floor) still show their track record = good partners. Recommended impl: compute from load history — per calendar month, that month's top-5 by revenue, tally per agent — **NO new table/cron**. Its own feature, AFTER the dashboard; pairs with the "richer agents list" backlog item.

**ROADMAP (agreed 2026-07-08, in order):** (1) THIS dashboard redesign → (2) **monthly P&L upload → cost → net profit**: Jason uploads P&L once/month, app parses (format matters: CSV/Excel >> PDF) and shows parsed figures to CONFIRM before saving (escapes his error-prone ops sheet); total cost from P&L, miles from loads → CPM computed; replaces hardcoded `BREAK_EVEN_RPM=2.96`; unlocks net-profit tiles. **Google-Sheets live-sync REJECTED** (error-prone, fragile per-load). → (3) **alert engine** (maintenance from truck mileage/service intervals, compliance from doc-expiry dates) → feeds the reserved banner region; needs Jason to start tracking service intervals + doc expiry dates. **EXPANDED 2026-07-09 (Jason):** also surface **intelligence / recommendation banners** — RULE-BASED (deterministic) anomaly detection + operational recs from the app's own data (expense category vs trailing avg, deadhead spike, below-break-even lane, aging AR). Same `Alert` type + banner region (add `kind: 'insight'`). **Rules-FIRST, NOT LLM** (hallucination risk on financial advice; rules are cheap, explainable, testable — instructor pushed back on leading with AI; LLM narrative synthesis a possible later layer). Needs a HISTORY BASELINE to define "unusually high" → do AFTER P&L gives the expense history. Guard against alert fatigue (few high-signal rules).

_(Lanes page — issue #159 — SHIPPED v1.25.0 on 2026-07-08; detail below.)_

**Lanes page — issue #159 (SHIPPED v1.25.0, PR #160 `950cb4f`, 2026-07-08).** Region/market/lane rollup + top-lane KPI cards + US choropleth (loads by origin state, hover→RPM+markets). Design signed off via mockups (regions = US Census 9 divisions relabeled for trucking: Southeast/Mid-South/Gulf/Midwest/Northeast/New England/Plains/Mountain/Pacific; origin-based; delivered loads). **Foundation DONE + tested 2026-07-08:** `lib/constants/states.ts` (state→region map + `getRegion`/`getStateName`), `lib/metrics/lanes.ts` (`getRegionRollup`, `getStateLoadMap`, `getLanesSummary`; RPM = gross/loaded like dashboard; KPIs need ≥3 loads to avoid single-run noise), `lanes.test.ts` **9/9 green** (incl. recency). **Frontend-only, NO migration.** RECENCY (2026-07-08): page-wide window selector **30/60/90 days, default 90**, filtered on `delivery_date` (`getRecentLoads`); RPM-based KPI winners still need ≥3 loads. MAP dual-window (`getStateMapData`): shading = load-count FOOTPRINT over rolling **365 days** (stable territory), hover RPM = the selected window; a state can be shaded yet show "no recent loads" when the RPM window is empty (tested). Map lib: **d3-geo + topojson-client + us-atlas** (chose over react-simple-maps for React-19 safety; audit vulns are pre-existing, not from these). **UI BUILT 2026-07-08:** route `/lanes` + nav, `LanesPage` (30/60/90 window selector), `LanesKpis`, `LanesMap` (d3-geo choropleth, dark amber ramp, hover tooltip w/ "no recent loads"), `LanesTable` (expandable region→market→lane, thin lanes dimmed), `components/lanes/rpmStyle.ts`, `src/us-atlas.d.ts` (ambient JSON decl). **33 tests green, strict build green.** Lazy-loaded the `/lanes` route (React.lazy) — main bundle back to 263KB gz, Lanes is a 50KB gz on-visit chunk. Dev-tested by Jason (passed). **SHIPPED 2026-07-08:** PR #160 squash-merged (`950cb4f`), tagged **v1.25.0**, frontend-only (no migration) → Vercel auto-deploy. Phase-2 follow-ups: per-lane "last load N days ago" freshness indicator; prune now-dead all-time `getDeadheadPercent`; pre-existing npm audit vulns (not from map libs). (Prior shipped 2026-07-08: deadhead MoM v1.24.0; trips v1.23.0.)

_Prior arc (SHIPPED 2026-07-08): deadhead metric wired to trips + month-over-month, v1.24.0, issue #157/PR #158._
  - **STATUS (2026-07-08): SHIPPED.** Issue **#157** → PR **#158** squash-merged to main (`0f44a8b`), tagged **v1.24.0**, frontend-only (no DB migration) so Vercel auto-deploy is the whole ship. First feature shipped end-to-end under the new build-mode workflow (instructor built + committed + merged + tagged via `gh`). `getMonthlyDeadhead(loads, trips)` in `dashboard.ts` → `{thisMonth, lastMonth}`; per-month: loads (delivered, both odos, delivery_date in month) + ALL trips (both odos, trip_date in month); `total = Σ load+trip windows`, `loaded = Σ load.loaded_miles`, `deadhead% = (total−loaded)/total` or null. Display (per Jason 2026-07-08): raw percentages, NOT a relative delta — KpiCard `label="Deadhead · MTD"`, value = this month %, subtext = `Last month: X%` (value colored green/red by 20% target). `invertDelta` idea was reverted (unused). DashboardPage fetches trips via `useTrips`. 13 tests pass (6 new: math, null, trips-only, cancelled/tonu excluded, delivered-unpaid included, Jan→Dec rollover). Strict build green. NOT committed. Old `getDeadheadPercent` now unused by app (still exported/tested) — prune candidate. Ship will be a `feat` → **1.24.0**.

**Working mode — dash (CHANGED 2026-07-08, durable):** Issue-driven **build mode**. Loop: (1) Jason creates a GitHub issue = the spec → (2) align on scope + design/formula decisions (instructor still surfaces these one at a time, doesn't railroad) → (3) **instructor builds it** → (4) verify together: matches intent AND formulas/calcs are provably correct (instructor brings the rigor — dates, numeric-string coercion, null-for-no-data, empty/edge cases, tests with a frozen clock) → (5) test on dev → (6) ship (PR→merge) + version bump only if warranted (semver additive-vs-corrective; not every change bumps). GitHub access: `gh` CLI authed as `delgado-jason` (repo scope). **NOTE:** this overrides CLAUDE.md §2's teaching default FOR dash only. **dts-tools / Track B stays TEACHING mode** (concept → Jason writes → review) — it's a learning course, not build-for-him.

---

## PROJECT STATE SNAPSHOT

### dash — current version 1.90.0 (profit-lever bottleneck · days-based utilization · oversize achievements 2026-07-15)

Latest arc (see the 2026-07-15 BEHAVIOR LOG entry for the full run): RLS hardening (#240), the driver-page cluster (Overall grade, oversize/heavy-haul strips, hometime), structured load dimensions (#244), the operation setting + blue oversize achievement set (#231), days-based truck utilization with Road Warrior/Relentless, and **epic #268 Phase A** — the three-lever (Rate/Utilization/Margin) bottleneck diagnostic on the driver card. **Active:** #268 Phase B (in-month dollar bridge) next, then Phase C. **Queued:** #266 (load-detail fuel calc uses hardcoded rate/mpg vs real). _(Snapshots v1.47–v1.89 were not individually logged; the behavior log carries the detail.)_

### dash — prior version 1.46.0 (compliance hub 2026-07-10)

**COMPLIANCE HUB — SHIPPED v1.46.0 (PR #191, closes #190, 2026-07-10; backend + frontend, migration 030).** Single Compliance page tracking renewal/expiry-dated docs across 3 scopes: **business / driver / vehicle**. Design = maintenance's twin, date-keyed instead of miles; alerts flow into the dashboard region already wired for `kind:"compliance"`. **Data model:** `compliance_items` table (030, user-scoped, optional driver/truck/trailer link): scope, label, category, issued_on, expires_on, renewal_months, warn_lead_days, doc_number, notes, active. **CDL DECISION (Jason inverted my first call):** CDL stays STORED on the driver record (number/state/expiration/endorsements = identity, won't fit generic table), but the compliance hub is the SOLE EDIT SURFACE (writes back via patchDriver) and the driver page is READ-ONLY for CDL — removed CDL from `DRIVER_FIELDS`, added "managed on Compliance page" link on DriverDetailPage. One source of truth. **Engine** `lib/metrics/compliance.ts` (10 tests): `computeComplianceDue` (valid/expiring/expired/unknown; `daysUntil` = whole-day UTC math, no shift), `complianceSummary` (counts + status **cleared/grounded** — grounded ONLY when a driver/vehicle doc expired; expired BUSINESS doc counts but doesn't ground), `complianceAlerts` (expired=critical before expiring=warning), `itemToCheckable`/`cdlToCheckable` (CDL_WARN_LEAD_DAYS=45). **Page** `CompliancePage.tsx`: green CLEARED TO ROLL / red GROUNDED `<Stamp>`, valid/expiring/expired KPIs, per-entity sections (Business, Driver·name, Vehicle·unit, Trailer·unit) with drill-down links + inline add/edit/delete via `ComplianceItemForm` (common-doc PRESETS per scope so setup = dates not typing) + inline CDL editor. Backend: `complianceServices.js` (CRUD, per-scope entity ownership via reconcileScope), `complianceRoutes.js` (`/compliance`, national-diesel-style handle()), `complianceValidation.js`, mounted in server.js. Sidebar: **Compliance** top-level (Dashboard·Freight·Fleet·Compliance·Expenses). Dashboard merges compliance+maintenance alerts. **⚠️ OPS GOTCHA (learned this build): backend/.env DATABASE_URL points to PROD, not dev** — so NEVER run mutating CRUD through the backend pool (writes prod); verify query changes by running the SQL against the DEV Supabase project via MCP execute_sql. Migration 030 applied to BOTH dev + prod (idempotent CREATE TABLE IF NOT EXISTS). Dev seeded w/ demo items (medical/2290/registration/LLC) + Jason's dev driver given a CDL. Strict build clean, 143 tests. Couldn't preview authed page → Jason verifies add/edit + CDL editor on prod. **NEXT AGREED FEATURE: Per Diem tracker** — its own focused page on the money side (near Expenses), NOT compliance (different engine: counts days-away-from-tax-home → IRS deduction). IRS transportation-industry rate ~$80/full day, ~75% partial travel days, DOT hours-of-service drivers deduct 80% (confirm current-year rate at build — resets Oct 1). Open design Q: count days from load/trip dates vs a manual departure→return log. Also still deferred: fuel vendors by city/state.

**FEATURE BACKLOG — ideas Jason greenlit to revisit (parked 2026-07-10, not yet scheduled):** (1) **Load scorer / "Take it or leave it"** — at the dispatch phone, enter rate + miles + deadhead → instant all-in $/mi vs break-even (~$4.10 walk-away) AND vs that lane's own historical avg, with a comic TAKE IT / PASS / STEAL stamp. All inputs already exist (break-even engine + lane history). LIGHT. Jason's top-liked. (2) **Deadhead radar / next-load positioning** — from a delivery location, rank best-paying markets to reposition to using Jason's own lane history, netted against deadhead cost; rides the existing US load map as a radar sweep. MEDIUM. (5) **Season recap — SHIPPED v1.52.0 (PR #200, 2026-07-11).** `/recap` page, **Month/Quarter/Year toggle + prev/next nav** (resolvePeriod: 0=most recent complete). Screenshot-worthy comic poster per period: gross hauled, miles, states conquered, loads, best week, biggest load, longest haul, best tank, avg RPM, **net profit + best/hardest month + best grind streak** (Jason's added metrics), top lane, top agent, computed rank. **YEAR = grand finale: truck's avatar_url as hero image + gold foil edge**; month/quarter lighter amber. `lib/metrics/recap.ts` (computeRecap over any RecapRange, rangeFor/resolvePeriod, 4 tests) reuses loadRevenue/getGrossTargets/getWeekBookedGross/mpgWindows/grind classify+bestStreakOf. `components/recap/RecapPoster.tsx`, `pages/RecapPage.tsx`. Entry points: sidebar "Recap" + dashboard "Your {year} recap →" link + player-card "See your full recap →" link. Real 2026: $173.8k hauled, 30,328mi, 25 states, 47 loads, top lane Beech Grove→Las Vegas, top agent Mike Sorrentino. NOTE topLane/topAgent use frontend market/agent NAMES (getLoads joins them); gross includes accessorials (loadRevenue).

**TROPHY ROOM (NEW FEATURE, in progress 2026-07-11) — Jason's vision: an IMMERSIVE, AI-GENERATED trophy HALL** (whole page = a hall of fame). AI-generated background + **each trophy is its own AI-generated image** (reuses the existing fal pipeline in `avatarService.js`, house style = comic-cel steel-blue + amber). **Jason signs off on each trophy in-app before it's committed** (build a generate+approve tool; I can't call fal from chat). Distinct from the player-card trophy shelf (that = frequent everyday awards as mini-cards; the Trophy Room = ONLY the major once-in-a-career milestones, big/defined, earned lit + LOCKED-with-progress = a goals board too). **LOCKED CATALOG (10 trophies, signed off):** 1 **Owner Operator** (manual, EARNED — the origin), 2 **Own Authority** (manual — own MC off the Landstar lease), 3 **Free & Clear** (manual — truck note paid), 4 **Trailer Paid Off** (manual), 5 **Second Driver** (auto: 2+ drivers — Brandie is NOT a 2nd driver, that was DEV seed only; prod has 1 real driver=Jason), 6 **Second Truck** (auto: 2+ trucks), 7 **Five-Truck Fleet** (auto: 5+ trucks), 8 **Million Mile Club** (auto: 1M lifetime rig miles; at ~582k=58%), 9 **One Million Hauled** (auto: $1M cumulative gross; ~$174k so far), 10 **Highway Legend** (CAPSTONE — derived: own-authority AND free-and-clear AND 1M miles; the true pinnacle, no longer just the 1M-mile rank). Manual ones = Jason marks earned (no data signal); auto = computed from data. **BACKEND SHIPPED v1.53.0 (PR #TBD):** migration `032_trophies.sql` (trophies table: user_id, trophy_key, **earned bool**, earned_on date, image_url [AI art], notes; unique per user_id+trophy_key) applied dev+PROD; prod owner-operator seeded earned=true (date null, editable). `trophyServices.js` (getTrophies, upsertTrophy = INSERT ... ON CONFLICT DO UPDATE w/ only-provided-fields so partial upserts don't clobber, deleteTrophy), `trophyRoutes.js` (`/trophies`: GET, PUT /:key upsert, DELETE /:key), `trophyValidation.js` (TROPHY_KEYS whitelist). Frontend `types/trophy.ts`, `services/trophyService.ts`, `lib/trophies/catalog.ts` (TROPHY_CATALOG: key/name/form[medallion/plaque/cup/belt/star]/kind[manual/auto/capstone]/blurb/promptIdea). **NEXT PHASES (not built): (1) house STYLE prompt for trophies + hall bg, (2) fal generate+approve tool (Jason signs off each), (3) auto earn-detection engine (miles/fleet/revenue/capstone), (4) the immersive hall PAGE (background + spotlit pedestals, earned lit/locked dimmed+progress).** Trophy-earn milestones could also fire marquee award pops. **STYLE LOCKED (Jason approved 2026-07-11), v1.53.1 `lib/trophies/style.ts`:** anchored to the AVATAR house style (avatarService.js: "comic book digital illustration, cel shaded, bold clean line art, steel-blue + amber, rim lighting") for coherence, PLUS the adult-comic/noir direction (heavy ink, halftone Ben-Day dots, film-noir grit, grown-up not cartoonish). `TROPHY_STYLE` suffix + `trophyPrompt(subject)` (subject = catalog promptIdea), `HALL_PROMPT`. **HALL BG has TWO empty forward-facing ornate gold frames** (left + right walls) — the page COMPOSITES Jason's REAL driver avatar (left) + truck avatar (right) into them (his actual generated avatars hung in his hall, not AI-reimagined); trophies also composited onto pedestals. Fine-tune overlay coords to the approved bg. **GENERATE-AND-APPROVE TOOL SHIPPED v1.54.0 (PR #203, 2026-07-11):** `/trophy-room` "Trophy Studio" page (sidebar "Trophy Room"). Generate hall bg + each of 10 trophies from style.ts → preview → regenerate → approve (only approved art attaches). Manual trophies get Mark-earned + date; auto/capstone note they earn from data. Backend reuses fal pipeline: `avatarService.falGenerate` now exported + takes `image_size` (square_hd default; landscape_16_9 for hall via `wide`), `uploadToStorage` exported; `trophyImageService.generateTrophyImage` stores unique-path preview to avatars bucket under `trophies/{user}/{key}-{ts}.jpg`, returns url; approve = upsertTrophy({image_url}). `POST /trophies/:key/generate {prompt, wide}`. Generation runs on DEPLOYED backend (fal key on Railway) — I can't run fal from chat; JASON drives it in-app (~few cents/image, iterative). Minor: discarded previews leave orphan storage images (cleanup deferred). **NEXT PHASES: (1) auto earn-detection engine (miles/fleet/revenue/capstone → mark earned automatically); (2) the immersive HALL PAGE (composite the approved hall bg + driver avatar left frame + truck avatar right frame + trophies on spotlit pedestals, earned lit / locked dimmed+progress). **HALL BG = ROOM ONLY, NO baked pedestals** (v1.54.1 PR #204 — Jason's feedback: deep 1-point-perspective corridor buried far pedestals + 10 trophies won't fit a fixed baked row). Retuned HALL_PROMPT to a WIDE eye-level FRONT-ON empty gallery w/ open foreground floor; **the PAGE places the pedestals + trophies over the floor** (full control, all 10 full-size, earned/locked). Frames + back-wall emblem still in the bg. First real generation looked great (comic hall, frames, spotlights, amber-on-steel). **HALL PAGE SHIPPED v1.55.0 (PR #205, 2026-07-11):** `/trophy-room` = the immersive HALL (approved bg hero + driver/truck avatars framed on walls + "HALL OF FAME" + 10-trophy grid: earned=lit w/ art+date, locked=dimmed+lock+live progress bar). Studio MOVED to `/trophy-studio` (renamed TrophyRoomPage→TrophyStudioPage, back-link from studio; sidebar "Trophy Room"→hall). Earn-detection `lib/trophies/status.ts` (computeAllStatuses, 3 tests): million-mile-club=lifetimeMiles/1M, second-driver=active drivers≥2, second/five-truck=trucks.length≥2/5, one-million-hauled=cumulative delivered gross≥$1M, highway-legend capstone=own-authority&&free-and-clear&&million-mile earned; manual read record.earned. **Jason already generated+approved art for ALL 10 trophies + hall bg in the studio, and marked owner-operator earned (2025-06-01)** — hall renders fully populated. Data: driverCount=active drivers, truckCount=trucks.length, cumulativeGross=Σ delivered loadRevenue, lifetimeMiles=max(truck odo, maxFuelOdometer, load odo). **HALL v2 SHIPPED v1.56.0 (PR #206, 2026-07-11):** Jason's review of v1.55.0 caught two misses — (a) it was a hero-banner + separate grid-on-a-slab, not ONE immersive page; (b) avatars floated at the edges, not IN frames (the AI bg's baked empty frames competed). FIX: `TrophyHallPage` rewritten — approved hall bg is now a FIXED full-bleed backdrop (`background-attachment:fixed`, clips to content area so NO sidebar bleed, holds as you scroll), driver+truck avatars hang in PAGE-DRAWN ornate gold frames (always aligned; drill-down links to /drivers/:id + /trucks/:id per the link rule), 10 trophies STAND over the floor as spotlit circular medallions (earned=lit gold border+glow+date, locked=grayscale+lock+progress bar) each with a light pool. `HALL_PROMPT` drops its baked frames (page draws them now). **TEXT-FREE TROPHY ART — Jason: AI garbles baked text/numbers ("1,000,000"→"10,0 000", MC gibberish) + defaults to a generic cup every time; he chose "keep AI-generated but text-free":** every catalog `promptIdea` reworked to a WORDLESS SYMBOL (own-authority=eagle+shield, million-mile=highway to horizon, one-million-hauled=freight mountain, free-and-clear=snapping chain across grille, trailer-paid=bursting golden padlock, second-driver=two drivers, fleet=5-rig lineup; owner-operator rig + highway-legend legend-rig KEPT); `TROPHY_STYLE` dropped "a single heroic trophy…museum product shot" → "bold emblematic hero symbol" + a HARD no-text/no-numbers/no-lettering clause. The app draws ALL labels + figures on the card, so art never spells anything. Approved LAYOUT via a clickable mockup (Artifact) before building. **⚠ JASON MUST REGENERATE the hall bg (frames now gone) + all 10 trophies (text-free style) in the Studio after this deploys — the currently-approved art still has the old frames/baked text.** STILL OPTIONAL: trophy-earn → marquee award pop (not wired); auto-stamp earned_on for auto trophies when first detected (earned computed live, earned_on only for manual).

**METRIC FIXES v1.56.1 (PR #207, 2026-07-11) — two corrections Jason caught before Compliance:** (1) **Weekly target/pace was counting BOOKED + in-transit freight as "hit target"** — booking loads ahead flashed the win before anything was hauled (and would evaporate on TONU/cancel). Split the week into EARNED (delivered) vs COMMITTED (booked/in-transit): added `getWeekEarnedGross` (delivered-only) to rateTargets.ts; `useRateTargets` now exposes `weekEarned` + `weekBooked`; RateTargetsCard shows "This week · earned" = weekEarned, TargetBurst + beatTarget fire on EARNED only, PaceBar renders committed as a faded 30%-opacity extension behind the solid earned fill + a "+ $X committed, still to haul" line; **grind meter grades EVERY week on earned** (computeGrind uses getWeekEarnedGross — past weeks identical since delivered==booked historically in his data, in-progress week now honest; GrindMeter "home" copy → "Nothing delivered yet this week."). NOTE mid-week earned is often $0 until loads deliver — intended/honest, not a bug. Root cause was NOT a date leak: `loadsInWeek` correctly windows on delivery_date, so a load dated 3 weeks out never hit this week; the issue was un-hauled freight counting. (2) **Season recap headline Miles was loaded-only** (dropped deadhead) → `RecapStats.totalMiles` = Σ per delivered load of (odometer_end − odometer_start), **fallback to loaded_miles when a load lacks odometer readings** (so total never reads < loaded); kept `loadedMiles` for avgRpm (= gross ÷ loaded, revenue math, unchanged); RecapPoster MILES hero → totalMiles. Verified vs prod: his load odometers tile continuously so Σ deltas = true driven miles incl deadhead (e.g. a 552-loaded run = 815 odo = 263 deadhead). **FUEL MPG VERIFIED for Jason (2026-07-11 fill, on request):** app ~6.1 is CORRECT — last full tank Jul 8 @ odo 582,450 → today Jul 11 @ 583,244 = 794 mi ÷ 130.067 gal = **6.10 MPG**; his truck dash's 7.0 is ~15% factory-optimism (estimates off injector duty, reads high — trust the app). `fuelEconomy.mpgWindows` = full-to-full miles ÷ all gallons since last full (partials roll in); FULL_THRESHOLD=120 gal. 171 tests, build green, frontend-only.

**SETTLEMENT SCHEDULE → NET REVENUE — PHASE 1 SHIPPED v1.57.0 (PR #208, 2026-07-12).** Jason (Landstar BCO) enters each load's FULL customer rate but keeps only **65% linehaul + 8% flatbed trailer = 73%, 100% fuel surcharge** (from his signed ICOA — see auto-memory [[landstar-settlement-schedule]]). App was reporting pre-cut gross as "revenue" — ran 14–27% high vs his actual P&L income. **FIX:** per-user `settlement_schedules` table (migration `033`: linehaul_pct/trailer_pct/fuel_surcharge_pct/accessorial_pct, defaults 1/0/1/1 so net==gross when unconfigured [productizes; own-authority=100%], CHECK 0–2); `settlementScheduleServices.js` (get returns DEFAULTS if no row; upsert partial-merges) + `settlementScheduleRoutes.js` GET/PUT mounted `/settlement-schedule`; **`getLoads`/`getLoad` now return `gross_revenue` + `net_revenue`** computed server-side via `LEFT JOIN settlement_schedules` + `ROUND(linehaul*(lh_pct+tr_pct) + fuel_surcharge*fsc_pct + accessorials*acc_pct, 2)`. Frontend: `Load` type +`gross_revenue?`/`net_revenue?` (OPTIONAL — metrics fall back to linehaul+fsc+accessorials sum when absent, so fixtures/local loads don't break); `rateTargets.loadRevenue` now returns NET (`Number(net_revenue)` else gross via `Number.isFinite`), NEW `loadGross` returns full rate; `dashboard.getLoadRevenue` routes through loadRevenue; recap/pace/grind/RPM all net automatically. **TARGETS SELF-CORRECTED** — pace/grind now compare NET take vs cost, so "hit target" = netting cost+margin (were ~27% too low, counting full pre-cut gross). Relabeled dashboard "Net revenue · MTD/YTD", "Net RPM · 3mo", "Net rate per loaded mile". NEW **Settings page** (`/settings`, sidebar footer link) edits the schedule w/ live "$2,000+$300 FSC → $X net" preview; `services/settlementScheduleService.ts` (coerces to numbers) + `types/settlementSchedule.ts`. **VERIFIED vs his real books: schedule-net within 0.9% of his P&L income over Q2** ($78,651 vs $79,347; monthly ±8% = settlement-vs-delivery-date timing, washes out); in-transit load $6,703.80 gross → **$5,124.30 net** confirmed on prod. **Migration 033 + his prod schedule (65/8/100/100) applied to PROD BEFORE merge** (getLoads LEFT JOINs the table — would 500 if absent). `accessorial_type` is FREE-TEXT (LoadDetailPage capitalizes user input) so per-type %s DEFERRED — single `accessorial_pct` for now (his accessorials ~$0). Cost basis confirmed ALREADY rolling-3-month (`getCostBasis` `completeMonthsBefore(now,3)`); Jason chose to LEAVE it at 3-month (June maintenance spike $4,990 vs ~$1,200 norm lifts blended target ~$380/wk, accepted as real cost). Followed new [[ask-before-shipping]] rule (built → dev-tested by Jason → shipped on his go). 173 tests, build green. **PHASE 2 PENDING (agreed): rate ladder "show BOTH" — net rate/mile (truth) + the full booked rate needed to clear each tier (pricing gross-up ÷ effective take), using `loadGross`.** Also deferred: per-type accessorial rates + a controlled accessorial dropdown; widen/amortize cost window if maintenance lumpiness distorts.

**RATE LADDER = GROSS PER *TOTAL* MILE (Jason's booking basis) — SHIPPED v1.58.1 (PR #210, 2026-07-12). CRITICAL — do NOT relitigate:** Jason books loads in **gross $/mile DRIVEN**, folding his deadhead into the miles himself (200mi to pickup = 200 unpaid miles he adds to the denominator). His break-even floor = **rolling cost per TOTAL mile ($3.17) ÷ his 0.73 linehaul keep = $4.34 gross/mile**, scaled by 15/35/60% tiers. Anything he books above $4.34 (with deadhead in his miles) = profit; it's CONSERVATIVE because he keeps 100% of FSC+accessorials on top of the 73%. **The ladder now shows: walk-away = costPerTotalMile ÷ linehaulTake ($4.34), marker = grossPerTotalMile (his actual gross rate/mile, $4.52 = gross $101,152 ÷ 22,368 total mi).** `getCostBasis` gained `costPerTotalMile` + `grossPerTotalMile`; `useRateTargets` builds `bookingLadder` + `grossRate`; RateLadder reverted to simple (dropped the v1.58.0 per-rung "book $X" annotation — plotting a NET marker next to GROSS rungs caused a multi-turn blowup); Expenses ladder matched. **THE TWO SEPARATE ~30% HAIRCUTS (root of all the confusion, both ~0.7 for him so easy to conflate):** (1) DEADHEAD — his loaded/total = 0.67 (33% empty); turns cost/total ($3.17) → cost/loaded ($4.72). (2) LANDSTAR CUT — he keeps 73% of linehaul; turns net rate → gross booking rate. They STACK and apply at different steps. $4.34/total-mile ≡ $6.47/loaded-mile (same break-even, different denominator); Jason books per TOTAL mile so $4.34 is his number. I burned ~5 turns wrongly insisting on loaded-mile framing + comparing his NET rate ($5.25/loaded) to a GROSS booked number ($6.47) — apples-to-oranges; he was right, I was overcomplicating. Freight bill example (BCO Load Detail): linehaul $8,955 + FSC $1,110.42 ($0.62/mi×1,791) + Tarp $150 = $10,215.42 customer gross; his cut = 73%×linehaul + 100%×FSC + tarp% → net ~$7,797. 176 tests. **NEXT: accessorial per-type %s (Jason: NOT all 100% — ICOA Appendix A-1: tarp/detention/toll/etc 100%, surveillance/security 85%, unlisted 65%; his `accessorial_pct=1.0` in settlement_schedules overstates non-100% ones), then Phase 3 (net-revenue coverage sweep: agent/lane/truck/driver pages still gross).**

**⏸ PAUSED FOR THE NIGHT 2026-07-12 — PARKED FOR PICKUP (in priority order):**
1. **ACCESSORIAL PER-TYPE %s — in progress, BLOCKED on Jason's input.** Recon done: his prod accessorials have FREE-TEXT spelling variants (Tarp×20 vs "Tarp Charges"×1; Hazmat/"Hazmat "/Hazmt/"Hazardous Cargo Charge"; "Stop Off"/Stop) + 3 types the ICOA doesn't pin down. **Plan:** per-type pay-% (Settings-managed, seeded from ICOA), applied in the getLoads net computation (replace flat `accessorial_pct`); make accessorial entry a DROPDOWN (clean list, no typos) + consolidate his existing variants. Seed CLEAR ones from ICOA: Tarp/Detention/Tolls/Loading = 100%. **SHIPPED v1.58.3 (PR #212, 2026-07-13).** **VERIFIED the rates EMPIRICALLY against Jason's actual Landstar settlements** (his instinct: verify, don't guess — and 2 of his 3 guesses were WRONG). **METHOD (reusable):** settlements live in Drive `Bookkeeping/[year]/[month]/[MM-DD-YYYY]_Settlement`; freight bills in `Operations/Loads/[freightbill#]` where freightbill# = loads.load_number. Find the settlement via `fullText contains '<load_number>'`. The settlement pays each line as TWO rows — **tractor 65% + trailer 8% = 73%** — for linehaul AND base-rate accessorials; FSC/tarp show one 100% row. **VERIFIED RATES:** Hazmat = **73%** (NOT his guess 100% — settlement 05-27 load 6723509: $300 → $195 tractor + $24 trailer = $219); Stop-Off = **73%** (NOT his guess 65% — same load: $50 → $32.50 + $4 = $36.50; he forgot the +8% trailer); **EVC = 0%** (CONFIRMED — settlement 04-29 load 7413468 has NO EVC line; the $582.80 freight-bill EVC is kept entirely by Landstar). Tarp/FSC/Detention/Tolls/Loading = **100%**. **KEY INSIGHT: the trailer 8% rides on base-rate accessorials just like linehaul, so the honest UNLISTED DEFAULT = 73% (= linehaulTake), NOT 65%.** IMPLEMENTATION: migration `034_accessorial_pay_rates.sql` (per-user per-type `accessorial_pay_rates` table, PK user_id+accessorial_type, pay_pct 0–2); `getLoads`/`getLoad` net_revenue LEFT JOINs it (`a.amount * COALESCE(apr.pay_pct, COALESCE(ss.accessorial_pct,1))`), fallback to `settlement_schedules.accessorial_pct` (set to **0.73** for his user). Seeded dev + PROD with his 12 free-text variant names (Tarp/"Tarp Charges"/Detention/Tolls/Loading=1.0; Hazmat/"Hazmat "/Hazmt/"Hazardous Cargo Charge"/"Stop Off"/Stop=0.73; "High Value-EVC"=0.0). Prod table+seed applied BEFORE merge (getLoads LEFT JOINs it). Verified prod: 6723509→$4,041.12, 7413468→$4,138.15. **ACCESSORIAL EDITOR + DROPDOWN SHIPPED v1.59.0 (PR #213, 2026-07-13):** `/accessorial-rates` CRUD (`accessorialRateServices.js` get/upsert/delete + `accessorialRateRoutes.js` GET/PUT/DELETE :type, mounted); frontend `services/accessorialRateService.ts` + `types/accessorialRate.ts` + `components/settings/AccessorialRatesCard.tsx` (list/add/edit-%/delete, added to SettingsPage below the settlement schedule) + LoadDetailPage accessorial type now a `<select>` off the rate list with "Other…" free-text escape (falls to schedule's `accessorial_pct` default 0.73). **CONSOLIDATED his 12 free-text spelling variants → canonical 7 on dev + PROD (net-neutral rename, verified test loads unchanged $4,041.12/$4,138.15):** Tarp/Detention/Tolls/Loading=100%, Hazmat/Stop-Off=73%, High Value-EVC=0%. `accessorials` data renamed to canonical (Tarp Charges→Tarp; Hazmat /Hazmt/Hazardous Cargo Charge→Hazmat; Stop Off/Stop→Stop-Off) BEFORE merge. STILL DEFERRED: Settings UI already edits the settlement schedule (SettingsPage) — that's done; per-diem/other settings later. **METRIC-TRANSPARENCY GUIDE SHIPPED v1.60.0 (PR #214, 2026-07-13):** GuidePage (`/guide`) rewritten — leads with "The money": 8 metric explainers (net revenue, true cost/mile, break-even/rate-to-book, rate ladder, weekly/daily targets, RPM, deadhead, MPG), each = plain-English + Formula box + worked example + Why, with inline SVG/CSS visuals on the key ones (net-revenue keep-% bars, cost→÷keep→book-rate ChainBox, MiniLadder). Agents section kept below. Static illustrative numbers (his ballpark), not live. The trust layer Jason wanted. **CARRIER-GENERIC for productizing (Jason: don't hardcode "Landstar" — will be any leased carrier OR own-authority):** dropped "Landstar" from GuidePage, SettingsPage linehaul help, PlayerCard "net of carrier cut" sub, ServiceForm PM placeholder; added "own authority = keep it all, set 100%" framing. **STILL HARDCODE "Landstar" (deferred — need a CARRIER-NAME SETTING): `AgentCard.tsx` "· Landstar" + `AgentDetailPage.tsx` "Landstar Agent" (data labels, want a configurable carrier name).** **CARRIER-NAME SETTING SHIPPED v1.61.0 (PR #215, 2026-07-13):** `carrier_name` column on `settlement_schedules` (migration `035`); `settlementScheduleServices` get/upsert include it (COLUMNS = pcts + carrier_name; upsert handles carrier_name string, empty→null). Frontend: `SettlementSchedule` type +`carrier_name`, service coerces it, `hooks/useCarrierName.ts` (fetches schedule → name or ""), SettingsPage "Carrier name" field (blank = own authority), AgentCard (+`carrierName` prop) + AgentsPage (passes it) + AgentDetailPage show `{broker} · {carrier}` / `· {carrier} Agent` or nothing if blank. Removed the last hardcoded "Landstar". Prod migrated + his carrier set to "Landstar" before merge (display unchanged). 176 tests.

**EXPENSES PAGE CLEANUP SHIPPED v1.61.1 (PR #216, 2026-07-13):** relabeled the ambiguous per-mile KPIs — "Cost / mile"→"Cost / total mile" (trueCpm = cost÷total miles, $3.17), "Break-even RPM"→"Break-even / loaded mi" (trueBreakEvenRpm = cost÷loaded miles, net, $4.72); added a clarifier (cost over all miles vs break-even over paid miles, gap = deadhead) + a live cost→÷keep→book chain box above the booking ladder ($3.17/total mi → ÷73% keep → $4.34 to book, using rateBasis.costPerTotalMile/linehaulTake/rateLadder.walkAway). Defuses the multi-turn three-numbers ($3.17/$4.72/$4.34) confusion on the page itself. Frontend-only, 176 tests.

**PHASE 3 NET-REVENUE SWEEP SHIPPED v1.62.0 (PR #217, 2026-07-13):** the final decision was NUANCED, not "net everywhere" — Jason's rule: **money the company KEEPS = net; market economics + the pricing lens = gross.** Net now: Truck/Driver "Net revenue · all time" (recent-load amounts too), Trailer NEW "Net revenue · all time" + "Loads hauled" stat ("what's the trailer made me"), LoadDetail NEW "Your net" line under the rate breakdown (only shows when net≠gross), LoadsPage "This month · net" + "Outstanding AR". **Kept GROSS on purpose (Jason's explicit call): AGENT + LANE pages** ("the agent/lane's economics is the full market rate, independent of my cut"), the LoadsPage **"Gross" column** + LoadDetail headline (the deal size / booked rate), **every rate-per-mile** (rate card, load detail, loads-page avg — the pricing lens), and **Pipeline = a COUNT** (Jason corrected me — it's booked-load count, no $). loads.ts loadsKpis now imports rateTargets' net `loadRevenue` as `loadNet` for the dollar KPIs (deliveredGross→deliveredNet field rename) but keeps a gross sum for rpm; killed the duplicate gross helper concern. New test pins net-dollars/gross-rpm split (177 tests). getGrossRevenue/getAverageRPM on agent.ts + lanes.ts left gross (correct).

**PER-ASSET ATTRIBUTION FIX v1.62.1 (PR #218, 2026-07-13) — the v1.62.0 truck/driver/trailer "net revenue · all time" was DOUBLY WRONG; Jason caught it because the numbers didn't tie to his dashboard:** (1) it summed net over ALL loads regardless of status — including 7 CANCELLED ($18,476, never happened) + booked/in-transit ($7,230, unearned); (2) it credited the trailer with the FULL load net instead of its slice. **THE CONFIRMED MODEL (asset-dependent — each page sums only loads tied to its truck_id/driver_id/trailer_id, so multi-truck fleets attribute per asset): TRUCK & DRIVER = FULL net of their DELIVERED+PAID loads** (they're on every load → both tie to dashboard "Net revenue · YTD" = $141,355.99, which is delivered+2026 only via getRevenueYTD). **TRAILER = its OWN share only = trailer_pct(8%)×linehaul + trailer_pct×base-rate accessorials** (the ones paid at linehaul_pct+trailer_pct=0.73: hazmat/stop-off/unlisted); FSC + flat-100% accessorials (tarp/detention/tolls/loading) stay with the TRACTOR. **BASIS = EARNED = delivered AND paid** (Jason's call; all 47 delivered happen to be paid so = $141,356). Backend added per-load `trailer_net` computed column to getLoads+getLoad (no migration, no stored column); frontend `loadTrailerNet` helper + pages filter `load_status==='delivered' && payment_status==='paid'`. **DATA FIX applied to prod: trailer 780991 (in_service 2026-02-03) was assigned to 3 January loads from before Jason owned it → set trailer_id=NULL on them (delivery_date<'2026-02-03'), dropping trailer $12,522→$11,066 (44 loads).** Truck/driver keep loadRevenue (full net); trailer uses loadTrailerNet. Driver page = full net is CORRECT (driver generated the whole load; not part of equipment split). Also still deferred: per-diem tracker, truck-payoff tracker, cash-flow forecast, load scorer, deadhead radar, RLS on the newer tables.**

**RECAP PRESTIGE TIERS + CEREMONY POPS SHIPPED v1.63.0 (PR #219, 2026-07-13):** Jason's asks — recap must look BACKWARD (never the in-progress period), fire a ceremonial pop when a period closes, and both the pop AND the card scale in prestige AND data with the period (month < quarter < year; year keeps his truck avatar). **BUG FIXED:** `resolvePeriod` year used `y-ago` (current, unfinished year) while month/quarter used `-1`; now `y-1-ago`. `latestRecapWithData(loads,now)` (year→quarter→month, longest with data) lands the recap page + dashboard link on the newest FINISHED period (Q2 2026 for him; 2025 empty). **PRESTIGE = bronze(month)/silver(quarter)/gold(year)** via shared `lib/constants/recapTiers.ts` (RECAP_TIERS — one source of truth for card + pop). **DATA SCALES:** month=core 6 stats; quarter=+detail tiles+net+3-bar strip+top lane/agent; year=full poster+12-month strip+truck-avatar banner. New `monthlyGross` series in computeRecap (1/3/12 bars). **CEREMONY:** new `components/comic/RecapCeremony.tsx` screen-takeover, fires once per period-close via the existing per-device "seen" store; added as a new `"recap"` AwardTier in awards.ts (earnedAwards emits `recap:{scope}:{label}` for each completed period w/ data; newAwards plays them month→quarter→year crescendo BEFORE marquees; AwardPopHost renders recap takeover above marquee). `useAwardPops` now also returns truckAvatarUrl (year medallion + banner). Ceremony CTA navigates to /recap w/ router state `{scope}`; RecapPage honors nav scope else auto-scopes to latest-with-data. Preview via `?awarddemo` (DEMO_AWARDS has all 3 recap tiers). Design approved via 2 show_widget mockups first. Frontend-only, no migration, 179 tests. NOTE: blank-page scare during dev was a stale HMR bundle, not a real bug (no import cycle). **SUBSUMPTION FIX v1.63.1 (PR #220, 2026-07-13):** on prod the June pop fired and masked Q2. Rule now — a smaller period that CLOSES ON THE SAME DAY as a bigger one is subsumed: only the grandest ceremony pops (June ends when Q2 ends → Q2 pops, June suppressed; Dec hands off to Q4 + year). earnedAwards compares `resolvePeriod(scope,0,now).end` across month/quarter/year (sameClose → skip the smaller); newAwards now orders recaps GRANDEST-first (year→quarter→month, was crescendo month-first). Subsumed period still browsable on the recap page. Caveat: on a device that already fired June, Q2 got marked seen at the same instant so it won't re-pop there (fix corrects future boundaries only; `?awarddemo` previews the look). 182 tests. STILL QUEUED (separate): trophy-earn (Trophy Hall) → fire a ceremony/marquee pop; per-diem, truck-payoff, cash-flow forecast, RLS.**

**AWARD TIERS = THREE HOMES (DRIVER PAGE REWORK v1.64.0, PR #221, 2026-07-13) — Jason's model, now the durable structure:** the `earnedAwards` tiers map 1:1 to display homes so a POP always has a matching persistent home under the SAME name/icon. **`burst` → BADGES** (frequent wins; new badge area on the player card, record NUMBERS folded in as the badge detail — e.g. "Tight Lines · deadhead down to 7.2%", "Feather Foot · 6.9 mpg"; replaced the old "Personal Bests" records strip). **`marquee` → TROPHY SHELF** (rarer: Mile Club, Strong Season; rank kept OFF the shelf since it's in the card header). **career milestones → TROPHY ROOM** (the 10-trophy Hall at /trophy-room; link added under the card). Root problem this fixed: two parallel systems (awards.ts tiers vs playerCard earnedTrophies/personalBests) diverged — the "Light Footed"/Tight Lines burst pop had NO card home + the shelf MIXED big+small. Now DriverDetailPage's card memo computes `earnedAwards` (with computeGrind streak) once and splits `badges=tier==='burst'` / `shelf=tier==='marquee' && !rank`. PlayerCard props changed bests/trophies→badges/shelf. `earnedTrophies` in playerCard.ts is now DEAD (kept, harmless; personalBests still used BY awards.ts for the burst awards). **Also v1.64.0:** "THIS SEASON"→"LAST SEASON" (it's last 3 complete months); EntityAvatar (shared truck/driver/trailer) collapses its generate controls behind a corner-pencil "Change" once an avatar exists (Done re-collapses); driver avatar 118→150 to fill the card header.**

**TROPHY-EARN CEREMONY POPS SHIPPED v1.65.0 (PR #222, 2026-07-13):** completes the tier ladder — earning a Hall trophy now fires the GRANDEST pop. New top AwardTier `"trophy"` (above recap>marquee>burst); Award gained `image?` (the approved AI art). `earnedTrophyAwards(catalog, statuses, recordsByKey)` in awards.ts maps earned trophies → awards. `useAwardPops` now also fetches getTrophies+getDrivers and runs the SAME `computeAllStatuses(TROPHY_CATALOG, byKey, {lifetimeMiles, driverCount=active, truckCount, cumulativeGross=Σ delivered loadRevenue})` the Hall uses, merging earned trophy awards ahead of the base awards → fire-once via seen store. New `components/comic/TrophyCeremony.tsx`: full-gold screen takeover, confetti storm, shows the trophy `image` (fallback Trophy icon), "TROPHY UNLOCKED", button → /trophy-room; AwardPopHost renders it as the top-priority takeover (trophy ?? recap ?? marquee); newAwards orders trophy first. DEMO_AWARDS gained a trophy demo (no art → icon). **On Jason's prod device the seen-store predates trophies, so his already-earned Owner Operator trophy reads as NEW and pops once on next dashboard load (a built-in first-run reveal) — future earns pop thereafter.** 183 tests. The full award pipeline is now: burst→BADGES(driver card), marquee→TROPHY SHELF, recap-close→RecapCeremony, trophy-earn→TrophyCeremony. STILL QUEUED: per-diem, cash-flow forecast, RLS.**

**DESIGN-FIRST RULE (Jason's, added to CLAUDE.md §2 2026-07-13):** for any feature with a visual/UI surface, show Jason a rendered design mockup (show_widget/artifact) and get approval BEFORE writing code — a hard gate like the verify gate. Applied it on the payoff tracker (2 show_widget iterations before building).

**PAYOFF TRACKERS + AUTO-EARN TROPHIES SHIPPED v1.66.0 (PR #223, 2026-07-13):** "own it outright" tracker on the truck/trailer ASSET pages (Jason's call — trackers on asset pages, INPUT on the Expenses obligation editor). Migration 036 added to `obligations`: original_balance, current_balance, balance_as_of, payoff_date, asset_type ('truck'|'trailer'), asset_id (nullable, no FK — spans trucks+trailers). `lib/metrics/payoff.ts` computePayoff → %owned=(orig-owed)/orig, paymentsLeft/payoffDate EXACT from payoff_date else estimated at pace (balance÷payment) — chosen because the TRUCK is a Lone Mountain LEASE-purchase (no APR amortization) while the TRAILER is a First Bank of the Lake amortizing loan; simple current-balance model fits both + productizes (user updates balance off a statement, re-anchors). `components/fleet/PayoffTracker.tsx` = road-to-own card (vehicle rides to a FREE & CLEAR flag, gold PAID OFF at $0). ObligationsCard editor expanded with the loan fields (Fragment-wrapped rows). **AUTO-EARN:** status.ts free-and-clear/trailer-paid-off now `loanTrophy(record, loan)` = earned if manual OR loan.paidOff; shows "$Xk to go" progress bar in the Hall; feeds Highway Legend capstone. `assetLoanStatus()` in payoff.ts derives {paidOff,ownedPct,owed} from obligations; wired into BOTH useAwardPops + TrophyHallPage's computeAllStatuses (both added getObligations). When a balance hits $0 → auto-earns → fires TrophyCeremony pop (they already have art). **PROD DATA (from Jason's Drive contracts, seeded):** TRUCK (Lone Mountain lease) orig $69,000 → owed $44,100 (no payoff_date → ~28 payments/~Nov2028 at pace, 36% owned), asset truck 3686d2e6. TRAILER (FBOL loan #105660970, 7.75%, 120mo) orig $39,408.09 → owed $38,298.63 as-of 06/27/2026 (payoff_date 2036-01-27 exact, 3% owned), asset trailer 7c2988b1; **trailer payment CORRECTED $478→$475.19** (contract). Best Egg (personal, ~$11.4k) left as plain obligation (no tracker). 189 tests. STILL QUEUED: per-diem, cash-flow "when money lands", RLS.**

**AWARD SYSTEM REBUILT — 4 TIERS (Phase 1 SHIPPED v1.67.0, PR #229, 2026-07-13):** Jason restructured the old conflated "badges" into FOUR distinct tiers, each with its own MECHANIC + in-theme name (he dislikes "badge"): **PERSONAL BESTS / "Record Book"** = improving records (best week/tank/load/deadhead/most-per-week), compete-with-self, easy, slide-in pop then record updates; **PATCHES** = HARD, STACKABLE ×N feats (re-earn stacks a count), on the driver page patch-board; **MEDALS** = tiered I/II/III honors, worn on the CARD HEADER right of the name (ribboned bronze/silver/gold medallions), earned-only; **TROPHIES** = career monuments (Trophy Room, unchanged). Hierarchy metaphor: patches(collectible) < medals(awarded) < trophies(won). **ADAPTIVE RATCHETING BAR (Jason's key rule, `lib/awards/adaptiveBar.ts` computeStack):** "impressive feat" awards' thresholds are NOT hardcoded — set from the user's OWN history at ~Nth-best-ever (n=5) for the metric = hard-but-attainable; **ratchets HARDER only, never eases** (a good economy raises the bar, a downturn holds it — never scales backward); earns LOCK IN (walk chronologically, earn if value clears the bar-in-effect-at-that-time → ×N count is monotonic, never rolls back). lowerIsBetter flag for deadhead. Cold-start uses a conservative floor until minHistory. **FIXED vs ADAPTIVE split (confirmed):** cumulative MILESTONES stay FIXED universal marks (Mile Club 100k/250k/500k/1M, Freight Hauler 100/250/500, Big Earner $250k/500k/750k net, Iron Streak 4/8/12wk, Debt Crusher 25/50/75% off the payoff tracker, Strong Season) — 1M miles means the same for everyone; only the relative "how-good-was-that" PATCHES adapt. Patches: big-ticket($/load), long-hauler(mi), mountain-mover(weight), rainmaker($/month net), iron-week(loads/wk), clean-run(deadhead% lower-better) = adaptive; trailblazer(distinct states — the ×N IS the 48-state map), coast-to-coast(W↔E states), doubleheader(2+/day) = structural. All derived from load data + asset-scopeable (pass pre-filtered loads → reuse for truck/trailer later). Files: `lib/awards/{adaptiveBar,patches,medals}.ts` (+tests, 12 tests), `components/awards/{awardIcons,MedalBadge,RecordBook,PatchBoard}`; PlayerCard props badges/shelf→medals; DriverDetailPage computes all three. Cut tarp+hazmat patches (too frequent). **DESIGN approved via 2 show_widget mockups first (per design-first rule). Debt Crusher medal hangs off the v1.66 payoff tracker; every medal line ends in a Trophy = one striving ladder.** 201 tests. **PHASE 2 SHIPPED v1.68.0 (PR #234, 2026-07-13):** pop restyle + Guide catalog — the ONE engine now drives card + pops + Guide identically. awards.ts `earnedAwards` REWRITTEN to emit tiers **record/patch/medal** (dropped burst/marquee; imports computePatches/computeMedals + personalBests): record beat→corner slide-in (~3.5s via AwardPopHost timeout, "New record — Top Week"); patch earned/stacked→corner slide-in w/ ×count; medal tier-up→NEW `components/comic/MedalAward.tsx` screen-takeover w/ ribboned medallion in its metal (bronze/silver/gold via medalTier); rank-up = a medal award (medalTier 3). AwardInputs gained `loanPaidPct` (useAwardPops passes max truck/trailer ownedPct → Debt Crusher medal). AwardPopHost: takeover=trophy>recap>medal, slide-ins=patch+record, icons via shared awardIcon() (extended w/ truck/package/stack/users). DELETED dead MarqueeAward.tsx. newAwards order: trophy>recap(grandest)>medal>patch>record. DEMO_AWARDS updated. GuidePage "The Award System" section: 4 tiers explained + full medal ladder + full patch list w/ how-to, from static `PATCH_GUIDE`(patches.ts)/`MEDAL_GUIDE`(medals.ts). AWARD SYSTEM NOW COMPLETE (records/patches/medals/trophies). Also still queued: per-diem, cash-flow "when money lands", RLS.**

**TRUCK PAGE METRICS + TRUCK AWARDS + FLEET COMPARISON SHIPPED v1.69.0 (PR #236, 2026-07-13):** each ASSET gets its OWN achievements (Jason's rule — not the driver's revenue feats recycled). `lib/metrics/truckMetrics.ts` computeTruckMetrics → **utilization = active-weeks % (Jason's pick: distinct weeks-with-a-delivered-load ÷ weeks-since-in-service)**, avg MPG + best tank (fuelStats), fuel/mi, revenue/mi, **cost-to-run/mi = (fuel spend + maintenance spend) ÷ miles** — Jason: label must be CLEAR it's "what the truck cost the owner" → "Cost to run / mi · fuel + maintenance" (the truck NOTE is excluded, it lives in the payoff tracker, not double-counted), mi/month; `fleetSummary()` per-truck rows. Maintenance has no truck_id → scoped by service.unit ('tractor'/'both') + cost (fine for 1 truck). `lib/awards/truckAwards.ts` reuses adaptiveBar + medals.tiered (now exported): PATCHES = feather-foot(tank mpg), iron-horse(month miles), marathon(haul miles), thrifty(tank $/mi lower-better) — all adaptive ratcheting; MEDALS = mile-club(odometer), fuel-miser(avg mpg 6.5/7/7.5), workhorse(loads 100/250/500), debt-crusher(truck payoff); RECORDS = best tank/big month miles/best rev-per-mi/longest haul. TruckDetailPage: medals worn on header (MedalBadge by unit#), metrics strip (Kpi tiles), RecordBook + PatchBoard. TrucksPage: fleet-comparison table (util/mpg/rev-mi/mi-mo per truck, best-per-column green), shown only at 2+ trucks (dormant for Jason's 1 truck). `RecordBook` GENERALIZED to take `RecordChip[]` (+ exported `driverRecordChips`); driver/truck/trailer each feed own set. awardIcons += feather/flag/droplet. 205 tests. **TRAILER PAGE is the natural next step — mostly a new catalog (trailer-flavored awards + trailer metrics), engine + components already reusable.** Also still queued: per-diem, cash-flow "when money lands", RLS.**
2. **METRIC TRANSPARENCY / TRUST (Jason 2026-07-12, high priority):** he wants to SEE the math behind every metric so he can challenge it + confirm correctness; end-users need to TRUST the metrics. **Build a GUIDE section explaining how each metric is derived and WHY** — ideally with GRAPHICAL representations. Surface the formula + inputs, not just the result. (He's caught real errors by challenging the math — this is the whole-app trust layer.) See auto-memory [[metric-transparency-and-trust]].
3. **EXPENSES PAGE UPDATE:** reflect the new booking-ladder / gross-per-mile / net-revenue model. Make the cost KPIs UNAMBIGUOUS — label cost-per-mile as "cost / TOTAL mile" ($3.17, all miles driven) distinct from the booking walk-away ($4.34) and show the chain (cost/mile → ÷keep → book rate; deadhead folded into miles). This was the source of a multi-turn confusion; explicit labels + the derivation would prevent it.
4. **Phase 3 — net-revenue coverage sweep — SHIPPED v1.62.0 (PR #217, 2026-07-13).** Net = money kept (truck/driver/trailer/load-detail-net-line/loads-page $); gross = market + pricing (agent/lane/rate-per-mile/gross-column); pipeline = count. Still queued: trophy-earn → marquee award pop.** **Per Diem tracker** also still queued (own page on the money side; count days-away-from-tax-home → IRS deduction). PASSED FOR NOW (offered, not chosen): #3 settlement reconciliation (heavy but real recovered money — Landstar deduction leaks + days-to-pay/AR aging), #4 oversize permit/escort intel (niche differentiator). **Round 2 greenlit (parked 2026-07-10):** (6) **Own-the-truck payoff tracker** — turn the loan obligation into a finish line: progress bar to owning the truck outright, projected payoff date from revenue pace, "≈N more loads to free-and-clear"; comic truck fills amber → gold PAID OFF stamp. LIGHT, Jason's top round-2 pick. (7) **Cash-flow forecast / "when money lands"** — days-to-pay per broker/agent + AR aging → forward money-incoming calendar (owed $X, ~$Y lands this week). MEDIUM. (10) **Grind meter — SHIPPED v1.51.0 (PR #198), FIXED v1.51.1 (PR #199, 2026-07-11).** Dashboard card: each pay-week (Wed–Tue) graded on **WEEKLY GROSS vs the gross PACE targets** (getGrossTargets weeklyBreakEven/weeklyTarget + getWeekBookedGross) — beat target(green)/covered floor(amber)/below(red)/home(grey, no freight). **v1.51.1 fix: originally graded on rate-per-loaded-mile (rate ladder), which CONTRADICTED the "this week · booked" card above it** (a big-gross/high-mile week read "covering costs" while that card said "beat target"). Now both use gross pace so they agree. `classify(gross, grossTargets)`. Streak = consecutive TARGET-beating weeks (Jason's chosen bar); HOME weeks skipped (rest never breaks it, inferred as zero-delivery weeks — upgrades to real home data when per-diem lands). Card: flame + current streak, personal best, 14-week strip, this-week status, ON FIRE stamp ≥5wk. Milestones 4/8/12wk fire "On a Roll" burst via award pops. `lib/metrics/grind.ts` (computeGrind/classify/currentStreakOf/bestStreakOf, 5 tests) reuses getCostBasis/getRateLadder/getWeekRpm; `hooks/useGrind.ts`; `components/dashboard/GrindMeter.tsx`; awards.ts gained `streak` input + on-a-roll:<mark>; useAwardPops computes grind (all-active obligations for ladder). Real-data check: his weeks are a green-run-then-amber mix, current streak reads low/cold now (best ~5) — honest, target is a high bar. **NOW BUILDING: #8 player card** (see below).

**PLAYER CARD (#8) — DESIGN SPEC (in design as of 2026-07-10, not yet built):** An owner-operator "trading card" for Jason/DTS — the agent prestige system turned inward. **Two-part model:** (a) permanent **career prestige** tier that only climbs — Rookie → Roadrunner → Veteran → Road Captain → Highway Legend (parallel to agentPrestige rookie→legend), seeded from real lifetime miles (truck at 582k = already Veteran/Road Captain) + banked good seasons; (b) **current-season form** (per quarter, can move up/down) = **worst-of two sub-grades**: RPM-vs-your-own-rate-ladder (reuse getRateLadder/getWindowRpm, walkAway/minimum/target/strong) AND net-margin-vs-bands. Worst-of is deliberate (honest + diagnostic — shows which lever to pull); show BOTH sub-grades on the card. **Margin = P&L operating net margin, NET of Landstar** = (income_total − cogs_total − expense_total)/income_total; income_total is ALREADY net of Landstar's commission (verified: only a $2k/6mo "Commissions & fees" line, not the cut). Do NOT subtract truck/loan principal from margin — debt is already in RPM's true-cash break-even (walkAway), avoid double-count. **MARGIN BANDS (LOCKED):** Strong ≥27% · Target 17–27% · Minimum 8–17% · Below <8%. Grounded in his real 6mo (Jan–Jun 2026) net margins: 25.2/34.3/23.3/15.3/34.0/19.8, blended 25.1%; vs industry (owner-op basis, incl. own pay): avg 10–15%, strong 15–25%, excellent 25–35%+. **Card stat line:** miles, net revenue, net profit, loads, avg RPM, deadhead %, best lane, best MPG, biggest load + trophy shelf. **Avatar:** reuse his generated driver avatar_url (monogram fallback). **Lives:** own top-level sidebar page "My Card" under Dashboard. **AWARDS (tiered):** full catalog, two visual tiers — 🎬 Marquee = full animated card-takeover popup on dashboard login (rare/big: rank-ups, mile clubs, truck paid off, record month, Strong season, $1M revenue); minor = a LIGHT frequent treatment (🚫 wax-seal/envelope-seal REJECTED twice — do not revisit; deciding among comic POW-burst / ink-splat / mini-trading-card / neon-marquee per the ADULT-COMIC/Roger-Rabbit tone — see [[suggest-comic-theme-opportunities]]). **Jason's award principle: he most values RE-EARNABLE self-competition awards** (beat your own best / clear a recurring bar) over one-time milestones. Catalog incl. re-earnable (Personal Best Week, new best MPG "Feather Foot", Wide Load [widest yet], most loads/wk, best RPM load, lowest deadhead "Tight Lines", best margin, biggest load, streaks) + one-time (mile clubs, $1M, truck paid off, Century) + **Relationship Builder** (Jason's idea — Nth load with the same agent, re-earnable per agent). "Wide Load"/"Super Load" needs a NUMERIC width field on loads (dimensions is free-text string today) — small add, compounds with oversize-permit idea #4. **SEEN vs EARNED architecture (Jason caught the muddle — get this right):** EARNED (rank/badges) = computed live from data now, NO backend table needed for card+pops; a small backend achievements ledger (earned_at only, SHARED) is deferred to the grind-meter/recap phase. SEEN ("已celebrated?", decides if a pop fires) = per-DEVICE localStorage, NEVER backend — because **Jason + Brandie SHARE the one DTS login**, so a shared "seen" flag would let whoever-logs-in-first rob the other of the pop; per-device makes both see each pop once (first device-load silently baselines current earned set → no day-one flood). Adding the backend earned-ledger later does NOT reintroduce the problem (it tracks earned, never seen). **Build phasing:** Phase 1 = card page (rank + season grade + stat line + trophy shelf + avatar); Phase 2 = celebration layer (award engine + marquee popup + minor treatment + per-device seen). **MINOR-AWARD TREATMENT DECIDED:** three-tier system — 🎬 marquee big-card takeover (rare), comic **POW-burst** (frequent re-earnable pops, `ComicBurst` primitive), **mini trading cards** = the trophy shelf collection (`MiniTrophyCard`). Bangers font. **PLACEMENT DECIDED:** NOT a separate page — the player card IS the hauling driver's page header (driver page also keeps records section + trophy shelf; CDL stays read-only; non-hauling drivers keep the plain page). If a 2nd hauling driver is ever added, lift business-level bits (margin/RPM/prestige) to the dashboard. **PHASE 1 SHIPPED — v1.47.0 (PR #192 merged 2026-07-10, Vercel auto-deploy; frontend-only). Jason verifies visually on prod.** New: `lib/constants/playerCard.ts` (RANK_TIERS mile thresholds Rookie/100k/300k/500k/1M, MARGIN_BANDS 27/17/8), `lib/metrics/playerCard.ts` (careerRank, marginGrade, rpmGrade, worseGrade, getSeasonStats, personalBests, earnedTrophies — 10 tests), `components/comic/ComicBurst.tsx` + `MiniTrophyCard.tsx`, `components/playercard/PlayerCard.tsx`, Bangers font in index.html + `--font-comic` in index.css, DriverDetailPage rewritten (card header for hauling driver). **REAL-DATA FINDINGS (prod):** all 56 loads driver-assigned to Jason (card renders); windowRPM gross $6.30/loaded mi, biggest load $10,065, best week $14,086; **deadhead reads 0% — deadhead_miles not entered on his loads (real, not a bug)**; given true-cost break-even (~$4.72, incl obligations incl $1000 distribution), RPM sub-grade ≈ Minimum → season Form ≈ Minimum (honest, not the mock's Target). Season stat "Miles" falls back to loaded+deadhead when odometer absent. **v1.47.1 (PR #193): added "THIS SEASON · <range> · last 3 complete months" heading over the stat line (timeframe was unclear).** **v1.48.0 (PR #194): True Net + metric colors.** Card headline profit = **True Net = operating profit − active DEBT obligations** (owner DRAWS excluded — a draw is profit taken, not a cost). Stat line = waterfall: net revenue → operating profit → true net ("what you keep, before draw"). **Grades + awards stay on OPERATING P&L (immutable) → toggling any obligation can NEVER fire a false award / change a grade (closes the hide/unhide gaming hole Jason flagged).** Obligations gained `is_draw` flag (migration `031_obligation_draw.sql`, applied dev+prod; prod Distribution backfilled is_draw=true; Truck Note/Trailer/Best Egg = debt). Backend obligationServices returns/accepts is_draw; ObligationsCard has per-row owner-draw toggle (HandCoins). Draws stay in break-even/rate math (getCostBasis uses ALL active incl draw) but drop from True Net (getSeasonStats new `obligationsDebtMonthly` param = active non-draw). Q2: $79.3k rev → $18.8k op profit → **$11.5k true net** (−$2,411/mo loans×3; $1k draw excluded), op-margin grade 23.7% (relabeled "Op margin"). Metric COLORS: avg RPM off rate ladder, deadhead off DEADHEAD_TARGET(0.2), profit by sign, records green; volume stats neutral. 154 tests. **PHASE 2 SHIPPED — v1.49.0 (PR #195, 2026-07-10; frontend-only).** Celebration layer on the DASHBOARD. `lib/metrics/awards.ts` (3 tests): `earnedAwards(inputs)` → Award[] {id,tier,name,detail,icon}; **STABLE ids that change only on genuine re-earn** (best-week:<value>, mileclub:<marker>, rank:<key>, relationship:<agent>:<mark>, strong-season:<label>, century:<n>) so beating a record re-pops, unchanged stays quiet; `newAwards(earned, seenSet)` (marquee-first order); `DEMO_AWARDS` for preview. `hooks/useAwardPops.ts`: fetches periods/fuel/trucks/obligations, computes earned, diffs vs **per-DEVICE localStorage** `dash.awards.v1` {baselined, seen[]} — SEEN is never backend (Jason+Brandie share login → per-device lets both catch each pop once); **first device-load silently baselines** (no flood). Components `components/comic/`: `MarqueeAward` (full-screen takeover: card fly-in + shine + ray-burst + confetti, "LET'S ROLL" dismiss — rare/big: rank-up/mileclub/strong-season), `BurstAward` (corner toast, ComicBurst press-in, auto-fade 5s — frequent re-earnable), `AwardPopHost` (marquee blocks bursts until dismissed; icon map). Keyframes (award-pop/shine/spin/fall/press/toast-in) in index.css. Wired into DashboardPage. **PREVIEW: dashboard `?awarddemo`** shows sample marquee→bursts (real device baselines silently so no pop without earning). 157 tests. **AGENTS RE-SKIN DONE — v1.50.0 (PR #196, 2026-07-10):** prestige bursts heavier ink, RatingStamp + prestige/rank labels → Bangers, TrophyCase → noir card + halftone (matches player-card shelf); rating medallion list chips left clean. Remaining clean-comic surfaces if ever wanted: mile clubs, maintenance stamps. Also deferred: backend achievements ledger (earned_at) for grind-meter #10 / season-recap #5.

### dash — prior version 1.45.0 (fuel odometer folded into derived tractor odometer 2026-07-10)

**FUEL ODOMETER FOLD — SHIPPED v1.45.0 (PR #189, 2026-07-10; frontend-only).** Fill-ups carry an odometer + are usually the freshest source, but the derived "latest odometer" only used loads + service readings (both maintenance files had `// when fuel carries odometer` TODOs). New pure `maxFuelOdometer(entries)` in fuelEconomy.ts (2 tests → 133 total) folded into ALL THREE tractor-odometer derivations for coherence: `TruckDetailPage` (filters fuel by truck_id), `MaintenancePage` + `useMaintenanceAlerts` (fleet max, single-tractor assumption like currentTractorMiles). **Trailer hub untouched** (fuel is truck-scoped). Also feeds maintenance-due math → more accurate. Dev proof: tractor odo sources stored 568737 / loads 314697 / services 568737 / **fuel 582450** → derived latest jumps 568737 → 582450.

### dash — prior version 1.44.0 (fuel page chunk 2: EIA national diesel + form auto-calc 2026-07-10)

**FUEL PAGE CHUNK 2 — SHIPPED v1.44.0 (PR #188, 2026-07-10; backend + frontend, NO migration).** (a) **EIA national diesel price**: backend proxy `GET /fuel/national-diesel` (`backend/src/services/fuelPriceService.js` + route in `fuelEntryRoutes.js` — MUST sit BEFORE `/:fuel_entry_id` or it's read as an id) calls EIA v2 petroleum server-side so the key never hits the browser; **narrowed to ONE series** `EMD_EPD2D_PTE_NUS_DPG` (weekly U.S. No 2 Diesel retail $/gal) via facets `duoarea=NUS`/`product=EPD2D`/`process=PTE` + `length=1` (NOT the 5000-row firehose Jason's raw URL pulled); `value` arrives as a STRING → coerce; in-memory cache 12h (feed updates weekly Mon); fails SOFT → returns `{diesel:null}` (HTTP 200) so page never breaks. Pure `parseNationalDiesel(json)` (`json.response.data[0]`) unit-verified + live-verified (2026-07-06 = $4.578/gal). Frontend `components/fuel/DieselCompareCard.tsx` (national avg + week vs your blended cost/gal + delta; **comic "Under the national" `<Stamp color=#4ade80>` when you beat it** — Jason runs ~$4.16 vs $4.58 national). `getNationalDiesel()` in fuelService, `NationalDiesel` type, fetched best-effort in its own effect. (b) **FORM AUTO-CALC**: add-fill-up form now takes **Total $** and computes **price/gal = total ÷ gallons** (3-decimal, live) — the two numbers on the receipt — instead of asking price/gal directly (Jason's request). **KEY HANDLING**: EIA key written to `backend/.env` (gitignored, verified `git check-ignore`); NEVER echoed/committed. **⚠️ DEPLOY TODO: set `EIA_API_KEY` in RAILWAY Variables for prod** — until then the endpoint returns null + the compare card just hides (no breakage). Vercel auto-deploys frontend, Railway auto-deploys backend from main. Backend has NO test runner → verified via node inline import + live probe (throwaway, deleted). 131 frontend tests green.

### dash — prior version 1.43.0 (fuel page chunk 1 2026-07-10)

**FUEL PAGE CHUNK 1 — SHIPPED v1.43.0 (PR #187, 2026-07-10; frontend-only, NO backend/migration — `/fuel` CRUD + `fuel_entries` table already live).** App IS Jason's fuel tracker now → NO CSV importer; manual add-fill-up form. Jason's rule (NOT Fuelly's): **≥120 gal = FULL, <120 = PARTIAL**; partials roll into the next full; MPG per full = odometer delta ÷ all gallons since last full (incl. the closing full). Engine `frontend/src/lib/metrics/fuelEconomy.ts` (pure, `now` param; 8 tests): `isFull`, `entryCost`, `mpgWindows` (odo-sorted, opens at first full, closes each on next full → {date,toOdometer,miles,gallons,cost,mpg}), `fuelStats` → {entryCount,totalGallons,totalSpend,avgCostPerGallon,totalMiles,avgMpg=totalMiles/windowGallons,costPerMile,bestMpg,worstMpg,avgWeeklyCost90,windows}, `avgWeeklyCost` (last-90d spend ÷ span-days clamp 7–90 ÷7), `weeklyCostSeries` (ISO-Monday buckets). Page `FuelEntriesPage.tsx` (route `/fuel-entries`): 6 KPIs (Avg MPG, Cost/mile, Cost/gal, Avg weekly·90d, Total gallons, Total spend), toggle add-fill-up form (date/odo/gallons/price/vendor/city/state; single-truck auto-resolves; state uppercased; "120+ = full" hint), `MpgChart` (recharts line, amber, one pt/full) + `WeeklyCostChart` (recharts bar + dashed 90d-avg ReferenceLine), fill-up log (odo-desc, Full green/Partial amber pills, per-tank MPG, delete). `types/fuelEntry.ts` + `services/fuelService.ts` (coerces numeric strings). **17 real Fuelly fills SEEDED TO DEV** (user a0…01, odo 570368–582450): 9 full/8 partial, 1975.33 gal, $8213.42 spend, $4.16/gal, avg MPG 6.55 — metrics cross-checked vs dev SQL, all reconcile. **PROD LOADED 2026-07-10** — same 17 fills inserted into PROD (user 2fe59f51-155c-4a18-b9e1-1d8a30cfc59d, truck 3686d2e6-98f6-415c-b41f-d89bc2bc2437 unit 580991) via idempotent NOT-EXISTS-on-(user,odometer) guard; prod totals match dev exactly (17/9/8, 1975.33 gal, $8213.42, $4.158/gal). Couldn't preview authed pages here → Jason should eyeball on dev (esp. the two charts render right). **NEXT: Chunk 2 = EIA national diesel price** ($/gal vs his cost/gal) — needs Jason's free EIA API key (eia.gov/opendata) → backend proxy keeps key server-side + caches (weekly US retail diesel, series EMD_EPD2D_PTE_NUS_DPG, EIA v2 petroleum/pri/gnd); env `EIA_API_KEY` on dev .env + Railway. Deferred: fuel vendors by city/state (later issue); optional comic best-tank flame (kept sober for now). [fuel odometer fold DONE v1.45.0]

### dash — prior version 1.42.1 (app-wide mobile responsiveness 2026-07-10)

**MOBILE RESPONSIVENESS — SHIPPED v1.42.1 (PR #186, 2026-07-10; frontend-only).** App-wide responsive pass, desktop unchanged (sm/md prefixes preserve original cols; max-w caps old fixed widths). `AppLayout`: removed double padding (was main p-4 wrapper + page p-6 → now Outlet direct, pages own p-6), added `min-w-0` on `<main>` (KEY: lets wide `min-w-[…]` tables scroll inside their card instead of forcing horizontal page scroll) + `overflow-y-auto`, trigger bar p-3. Modals (LoadsPage/LoadDetailPage load form `w-[750px]`, AgentDetailPage rating `w-[450px]`) → `w-full max-w-[…] mx-4` + `p-4 sm:p-6`. Form grids stacked on phones (`grid-cols-1 sm:grid-cols-2/3...`): LoadForm, EntityForm, MaintenanceItemForm, ServiceForm, TripForm, LanesKpis. Sidebar already had shadcn mobile Sheet drawer (SidebarProvider+SidebarTrigger in AppLayout). Tables kept horizontal-scroll (Jason's call, not card-ified). Vercel auto-deploys from main (confirmed prod live through the whole run; team_V7GBzqWZRcIuuWxzf88LvJDf, project prj_BBIwH57QEkPqHk97YLwOaxrmMdKA). **NEXT: fuel page — see [[fuel-page-spec]] (≥120 gal=full/<120=partial-rollover, MPG across fulls; Fuelly CSV in scratchpad). Couldn't preview authed pages here (no dev login) — Jason verifies mobile on his phone.**

**MAINTENANCE COMIC — SHIPPED v1.42.0 (PR #185, 2026-07-10; frontend-only). COMPLETES THE 4-ITEM COMIC SWEEP (dashboard v1.39, fleet v1.40, lanes v1.41, maintenance v1.42).** `fleetHealth(counts)` in maintenance.ts → {score 0-100 or null, label Healthy(≥85 green)/Needs attention(≥60 amber)/Rough shape(<60 red)}: `(ok + 0.5*soon)/(overdue+soon+ok)`, unknown excluded. `components/maintenance/HealthGauge.tsx` — semicircular dial (3-zone red/amber/green arc + needle at score angle `180-1.8*score` deg + % + verdict + count chips), tops the Schedule tab (replaced the loose chip row). `components/Stamp.tsx` — SHARED ink-stamp primitive (label+color, rotated -6°, double border, size sm/md) — mirrors RatingStamp; used for red OVERDUE (on overdue board ItemRows) + green DONE! (transient 2.6s banner in ServicesTab on service log). **Comic-theme system now spans: agent medallions/prestige bursts/trophies/starbursts (agents+dashboard), fleet mile-club bursts, lanes flame+crown, maintenance health-gauge+stamps. Expenses deliberately left sober. Reusable primitives: `<Stamp>` (ink-stamp), the starburst polygon (still duplicated in ~4 files — extract `<Burst>` if it spreads again).** Per [[suggest-comic-theme-opportunities]] keep offering fun comic touches on new work.

**LANES COMIC — SHIPPED v1.41.0 (PR #184, 2026-07-10; frontend-only).** Comic-queue item 3 of 4. `LanesKpis`: flame (Top RPM lane, #e8621e), truck (Highest volume), crown (Best origin market, amber) icons on the 3 KPI card labels. `LanesMap`: flame pins (filled lucide flame path #e8621e, pointer-events:none so hover still works) on the top-3 states by windowed avgRpm (min 3 footprint loads) at their `pathGen.centroid`; legend updated ("flame = best-paying states"). Reuses existing lanes metrics, no new logic. **Remaining comic queue: (4) Maintenance — OVERDUE/DONE ink-stamps + truck health gauge. Expenses stays sober.**

**FLEET MILE CLUBS — SHIPPED v1.40.0 (PR #183, 2026-07-10; frontend-only).** Comic-queue item 2 of 4. `lib/metrics/mileClub.ts` — `mileMilestone(miles)` → {crossed, tier bronze/silver/gold/platinum, title, label, next, toNext, pct}; clubs 100K Seasoned / 250K Road Warrior / 500K Workhorse / 1M Million-Miler, +500K platinum beyond, none under 100K. `components/fleet/MilestoneBurst.tsx` (starburst stamped with mile marker + `MILE_TIER_COLOR`) + `MileClub.tsx` (banner: burst + title + progress-to-next bar). Truck/Trailer/Driver DETAIL pages: MileClub banner in the header (trucks derived odometer, trailers derived hub `unit="hub"`, drivers career loaded miles from delivered loads + new "Miles hauled" stat card). Truck/Trailer/Driver LIST cards: corner MilestoneBurst (DriversPage added `useLoads` + per-driver miles map). Ran prettier on the 6 touched pages. **Remaining comic queue (one at a time, design-first): (3) Lanes flame on top-RPM lane + crown on best market (+ optional hot states on map); (4) Maintenance OVERDUE/DONE ink-stamps + truck health gauge. Expenses stays sober.** Burst polygon string now in 3 files (PrestigeBadge, RateTargetsCard, MilestoneBurst) — extract a shared `<Burst>` primitive if it spreads again.

**DASHBOARD COMIC — SHIPPED v1.39.0 (PR #182, 2026-07-10; frontend-only).** Comic-queue item 1 of 4 done. `TopAgents` (dashboard top-5) now takes `honors`+`standings` maps (DashboardPage computes `computeHonors`/`currentQuarterStandings` from loads) → each earner shows a small `PrestigeBurst` (career rank) + pulsing amber LIVE dot when on the board this quarter; #1 amber; names still link to /agents/:id; dropped the old revenue bar. `RateTargetsCard`: green "TARGET!" starburst (+"Beat target by $X" line) fires in the This-week-booked section when `weekBooked >= gross.weeklyTarget`. **Remaining comic queue (one at a time, design-first): (2) Fleet odometer/hub milestone bursts (250K/500K/1M MILE CLUB) + character-card feel; (3) Lanes flame on top-RPM lane + crown on best market; (4) Maintenance OVERDUE/DONE ink-stamps + truck health gauge. Expenses stays sober.** Reusable burst polygon string now duplicated in PrestigeBadge + RateTargetsCard (could extract a shared `<Burst>` primitive if it spreads further).

**LIVE PROVISIONAL STANDING — SHIPPED v1.38.0 (PR #181, 2026-07-10; frontend-only).** Since the in-progress quarter doesn't count officially (v1.37.1), show it as a live, provisional figure. `currentQuarterStandings(loads, now)` (batch) + `currentQuarterStanding(loads, agentId, now)` in agentLeaderboard.ts → `LiveStanding {quarter, result gold|silver|board|ran, boardRank, loads, revenue}`. Detail trophy case: dashed pulsing `LIVE` callout (`TrophyCase` gained `live` prop + `<Live>`). Agent card: compact `● LIVE` pill (animate-pulse dot) shown ONLY when on the board this quarter (gold/silver/board) — uses the space freed by removing the trophy tally. **COMIC-THEME QUEUE — Jason wants ALL of these next (picked 2026-07-10, do one at a time, each design-first; see [[suggest-comic-theme-opportunities]]):** (1) **Dashboard** [Claude's top pick]: put agent medallions/prestige bursts on the top-5 widget + a "TARGET!" burst when weekly/daily gross beats target; (2) **Fleet**: odometer/hub milestone bursts (250K/500K/"1M MILE CLUB") on trucks/drivers, lean into character-card feel (avatars already comic); (3) **Lanes**: flame badge on top-RPM lane + crown on best origin market (+ optional hot states on map); (4) **Maintenance**: reuse ink-stamp motif — "OVERDUE" red stamp + "DONE" stamp on logging a service + comic truck health gauge. **Expenses stays sober (no comic — money page).**

**AGENTS REWORK — SHIPPED v1.37.0 (PR #179, 2026-07-10; frontend-only, NO backend, NO migration).** Same design-drift retackle as loads, now with a COMIC-BOOK layer Jason loves (see [[suggest-comic-theme-opportunities]] — proactively suggest comic flourishes going forward). Rating SYSTEM unchanged (manual 1–5: 5 Call first, 4 Good, 3 Default, 2 Avoid, 1 Blacklist); only the DISPLAY reinvented. **Agents LIST** (`AgentsPage`): roster of comic cards (`components/agents/AgentCard`) — monogram emblem (amber ring), tier **medallion** (`RatingMedallion`: color+lucide icon per tier, replaces 5 stars), per-agent stats (loads·revenue·last-worked), prestige starburst corner badge. 4 KPIs via `rosterKpis` (roster+rated, go-to bench [call-first count + avoid count], top earner [90d, links], active agents [90d]); search + sort pills (Rating/Revenue/Recently worked; **rating ties break by career achievement** gold*10000+silver*100+board so Champion leads). "How this works →" link to /guide. **LEADERBOARD + AWARDS — all derived from loads history by CALENDAR QUARTER (`lib/metrics/agentLeaderboard.ts`, 110 tests):** `computeHonors(loads)`→per-agent {board,gold,silver}: **board** = quarterly top-5 by delivered revenue (2+ loads); **gold/silver** = #1/#2 among agents with 3+ loads that quarter. `agentPrestige(honors)`→career rank **Contender**(boarded)→**All-Star**(1+ medal)→**Champion**(3+ golds)→**Legend**(8+ golds); `PrestigeBadge`/`PrestigeBurst` = tier-colored starburst (bronze/silver/gold/platinum + icon), REPLACES raw tallies on card (Jason's declutter call — card stays fixed size over years, full record on detail). `agentSeasonLog(loads,agentId)`→per-quarter finish for the trophy case. `perAgentStats`. **AGENT DETAIL** (`AgentDetailPage`): coherent hub — header (emblem+name+broker+prestige word) + **ink-stamp rating** (`RatingStamp`: tier-colored double-border rubber stamp, rotated -6°, the "hero" rating display) + Edit rating; 5 KPIs (Loads/Gross/Avg RPM/Cancelled/Last worked, reuses existing `lib/metrics/agent.ts`); **`TrophyCase`** (prestige rank + gold/silver/board counts + quarter-by-quarter icon strip); loads table (load#→/loads/:id, **columns aligned via matching pr-4 + right-aligned Gross** — Jason flagged misalignment); restyled activity timeline (notes+ratingHistory via `buildTimeline`, note composer, RatingForm modal); contact card. Fetches `useLoads(0)` (all loads, for quarterly ranking) + `useAgent`. **GUIDE PAGE** (`/guide`, `GuidePage` + sidebar "Guide" link): graphical explainer built from the REAL components (medallions, trophies, prestige bursts) so it can't drift — ratings, quarterly leaderboard, trophies, prestige. **DEV SEED (dev only, not code):** added 3 agents (Dana Cruz/Rick Boyd/Cody Ray) + 130 delivered SEED-prefixed loads across 9 quarters (2024-Q3→2026-Q3) for user a0000000-…0001 to exercise the leaderboard → Mike=Champion(7g), Dana=All-Star, Rick/Ausra=All-Star, Jennifer=Contender, rest clean. ⚠ **GOTCHA: dev id space `e0000000-…-00X` overlaps ANOTHER user (a0000000-…0002) — `ON CONFLICT (agent_id) DO NOTHING` silently cross-linked "Dana" to their agent; had to create Dana with `gen_random_uuid()` + re-point loads. When seeding dev, verify inserts landed under the right user_id.** NEXT: Jason's remaining drifted pages if any; offered to (a) add "How this works" contextual links elsewhere, (b) grow the guide to cover loads/maintenance/expenses, (c) sweep older pages (dashboard TopAgents/RecentLoads) for missing drill-down links. **HOTFIX v1.37.1 (PR #180, 2026-07-10):** awards were counting the IN-PROGRESS quarter (a mid-quarter lead credited a badge before quarter-close). Jason: "an agent doesn't officially make the leaderboard until the end of the quarter." Fix: `computeHonors(loads, now)` + `agentSeasonLog(loads, agentId, now)` now skip any quarter `>= quarterKey(now)` — completed quarters only. (Seed's 2026-Q3 no longer counts → Mike 8 board/6 gold, still Champion.) **Possible enhancement Jason may want: show the in-progress quarter as a PROVISIONAL/live standing in the trophy case (not counted, but visible) — offered, not built.**

---


**LOADS REWORK — SHIPPED v1.36.0 (PR #178, 2026-07-09; hotfix'd nothing; no migration — loads fleet cols already from 028).** Jason flagged DESIGN DRIFT: the pages he built himself (loads list, load detail, agents) don't match the pages we designed together (dashboard/lanes/expenses/maintenance/fleet-detail) — he prefers OUR shared look. Retackling his pages one at a time, DESIGN FIRST each. Shared visual language = `p-6 bg-iron` shell, `h1 font-condensed`, `bg-plate rounded-lg` cards, `text-muted-text` labels, **amber** single accent, StatusBadge chips (NOT generic PageHeader/MetricStrip/`bg-card`/`text-foreground`/`primary`). Tokens: steel #0d1117, iron #1c2333, plate #2a3347, amber #e8940a, amber-light #f5b03a, light #ebedf5, muted #9daabb; status-positive #1a3a2a/#4ade80, aware #3a2a0a/amber, info #1a2a3a/#60a5fa. **Loads LIST:** 4 KPIs via `loadsKpis(loads,now)` (this-month gross+count, avg rate/mile [rpm, null-guarded], outstanding AR $+count, pipeline booked+in-transit); search + status pills + payment select (client filter, KPIs ignore filter); table load#→link, broker·agent, combined Lane, in-transit amber left accent. **Loads DETAIL:** hub of `bg-plate` cards (route timeline, FLEET [amber border, links to truck/driver/trailer], revenue, mileage, cargo, dates, fuel, broker·agent, update-status) — dropped tabs + right sidebar + generic PageHeader/MetricStrip. `getLoad` now LEFT JOINs trucks/drivers/trailers → `truck_unit`/`driver_name`/`trailer_unit` (aliases trk/drv/trl; MUST qualify `l.truck_id` — bare is ambiguous vs trucks.truck_id; verified on dev). KPIs: total revenue, rate/mile (guarded), loaded miles, deadhead %. **FUEL section (Jason's spec):** `estimateLoadFuel(load)` in `lib/metrics/fuel.ts` — miles = (odometer_end - odometer_start) if BOTH readings present & end>start → basis "actual"; else loaded+deadhead (or loaded if no deadhead) → "estimated"; cost = miles/MPG*price; `ASSUMED_MPG=6.5`, `ASSUMED_FUEL_PRICE=5.5` (labeled assumptions until fuel page feeds real MPG/price); returns null if no miles. Badge est./actual. **Load FORM:** new Fleet section — fetches trucks/drivers/trailers, auto-assigns the SOLE one (read-only "auto-assigned" line), picker only if >1 (resolve-single-entity, client-side); sends truck_id/driver_id/trailer_id. `createLoad`/`patchLoad` allowedFields + `loadValidation` gained truck_id/driver_id/trailer_id (lenient: null ok, else UUID). Removed stray console.log, fixed text-red-500→destructive. Shared `components/Kpi.tsx` extracted (used by both loads pages). New metrics: `loadRevenue`,`getInTransitCount`,`outstandingLoads`,`loadsKpis`,`loadRpm`,`deadheadShare` + fuel; 100 tests. **⚠ DESIGN MOCKUPS via `visualize` show_widget: render the dark app frame INSIDE the widget (iron bg, Barlow Condensed via fonts.googleapis import) — the tool's default is claude.ai-native light theme; Jason approved both loads mockups this way.** **DRILL-DOWN RULE (Jason corrected me — [[drill-down-links-for-entities-with-pages]]): any UI reference to an entity that HAS its own detail page MUST be a click-through link, on EVERY page (existing + new). Page-backed entities: loads /loads/:id, agents /agents/:id, trucks /trucks/:id, drivers /drivers/:id, trailers /trailers/:id. Brokers + markets have NO page → plain text. Note getLoads returns agent_id but NOT broker_id.** NEXT (Jason's plan): retackle his other drifted pages — agents list + agent detail — same design-first + coherence + drill-down treatment. Offered to sweep existing pages (dashboard TopAgents/RecentLoads, etc.) for missing drill-down links separately.

---


Shipped recently:

- Full **agent detail page** (header, rating, metrics, loads table, contact, activity timeline merging notes + rating history, with inline note composer).
- **Agents directory** (list page, sorted by rating desc, unrated sunk to bottom).
- **Notes backend** (agent_notes table, services, routes) + notes in getAgent.
- **Dashboard v1** (7-issue arc): data layer (revenue MTD/last/YTD, deadhead %, monthly revenue & RPM series, outstanding loads), KPI strip, revenue-over-time bar chart, RPM-vs-break-even line chart, outstanding loads list. Uses recharts.

**Known caveats to revisit:**

- Dashboard `BREAK_EVEN_RPM` constant set to **$2.96** — NOT confirmed as true cost-per-mile. If wrong, the profitability lens (RPM color, break-even line) is wrong. Confirm against real CPM work.
- `patchTrip` still has old `allowedFields` (missing `is_estimated`) — inconsistent with `createTrip`. Fix if trip-editing is built.

### dts-tools — COLD (weeks stale)

Side Course B (Python). Backend recon phase. Next queued task: `notes/comdata-recon.md` (ComData statement structure) — never done. Open thread: PDF-to-CSV library question (tabula-py/camelot/pdfplumber `extract_tables()`) unresolved. Report scope simplified to 4 metrics (prev-week gross tractor revenue, gross trailer revenue, compliance events, YTD 1099). Flag consciously — past its watch window.

---

## TRIPS ARC — DETAILED STATE

**Model decided (Option B):** A non-revenue trip = `trip_type: 'deadhead'`, `trip_source: 'user'`, `status: 'completed'`. Loads untouched. `tripStops` and `trip_type='revenue'` stay dormant for a future full-model arc.

**trips table (EXISTS in prod):** trip_id (uuid PK), user_id (FK), truck_id (FK, nullable), driver_id (FK, nullable), trip_type (enum revenue|deadhead), trip_source (enum user|system), trip_date (date), status (enum planned|active|completed|cancelled, default planned), odometer_start/end (int4, nullable, CHECK >=0), is_estimated (bool, default true), trip_number (int4, UNIQUE, auto via `nextval('trips_trip_number_seq')`), created_at/updated_at.

**Issue #1 — backend fixes: DONE + tested.**

- `createTrip` now sets trip_type='deadhead', trip_source='user', status='completed'; `is_estimated` added to allowedFields.
- getTrips/getTrip: inner JOINs → LEFT JOINs (nullable truck/driver — else null-FK trips vanish).
- `.http` verified: deadhead trip inserts, auto-numbers, lists (even with null truck+driver).

**Issue #2 — frontend data layer: essentially DONE (confirm merged).**

- `Trip` type (types/trip.ts) — correct nullability, enum literal unions.
- `TripInput` type (types/tripInput.ts) — FIX APPLIED: odometers are `number` (not string), fields optional (`?:`) except trip_date.
- `getTrips` + `createTrip` services (tripsService.ts) — extract `response.data.trips` / `.trip`.
- `useTrips(refreshKey)` hook — FIX APPLIED: `setIsLoading(true)` + `setError(null)` at start of each fetch (was stuck false / stale error on refetch).

**Trip categories contract — DONE on dev (2026-07-07), NOT yet on prod.** Migration `023_add_trip_purpose.sql` (CREATE TYPE trip_purpose enum + DELETE test rows + ADD COLUMN NOT NULL) ran clean on dev. Types (`Trip`, `TripInput`) carry `trip_purpose` required/non-null. Backend: `createTrip` allowedFields + two-part validation (presence check in `validateTripCreate` + value-check rule in shared `rules`) done & `.http`-tested. **PROD MIGRATION STILL PENDING — apply at ship time alongside the code deploy; re-run `SELECT count(*) FROM loads WHERE trip_id IS NOT NULL` on PROD (must be 0) before the DELETE runs there.** NOTE: `getDeadheadPercent` NOT yet wired to trips (still backlog).

**Issue #3 — Trips page: BUILT on dev, awaiting Jason's manual test (2026-07-08).** ✅ Slice 1+2 (route/nav/read-path list). ✅ Slice 3 built by instructor at Jason's explicit "build it for me" request:
- Backend: `getLatestOdometer(user_id)` service (GREATEST of MAX(odometer_end) over loads+trips) + `GET /trips/latest-odometer` route (declared BEFORE `/:id`). Added cross-field validation: `odometer_end >= odometer_start` when both present.
- Frontend: `createTrip` + `getLatestOdometer` added to `tripsService.ts` (createTrip surfaces backend error message). New `TripForm.tsx` (date, purpose select, odo start [prefilled], odo end, is_estimated Estimated/Actual select). Wired into `TripsPage.tsx` with a "Log Trip" toggle + list refresh on success.
- Instructor judgment calls (Jason can veto): omitted truck/driver from the form (single-truck, nullable); added is_estimated select; added the cross-field odometer rule; prefill endpoint is GLOBAL across trucks (commented for future per-truck).
- **Dev-tested by Jason (passed). SHIPPED 2026-07-08:** commit `2cf60ef`, `feat: add trips page with create form and odometer prefill - closes #150`, PR merged, tagged **v1.23.0**, prod migration `023` completed successfully, deployed. Trips feature DONE. Next: wire trips into the deadhead metric (now the active arc). **Trips was a CONFIRMED must-have for v1. Jason opted out of coding THIS feature (instructor builds it — teaching-mode exception, like the dashboard rush). Plan: build → test on dev → ship. No keep/cut question.**

**ODOMETER MODEL (clarified 2026-07-07 — corrects a prior misread by the instructor):** `getDeadheadPercent` (`frontend/src/lib/metrics/dashboard.ts:70`) is computed PER LOAD: `Σ(odometer_end − odometer_start) − Σ(loaded_miles)` over delivered loads that have both odometers. It is NOT gap-inferred between consecutive loads. Consequence: empty miles NOT tied to fetching a load (home/shop/personal/repositioning) currently FALL THROUGH — a real, uncounted gap. Trips fill that gap by carrying their OWN `odometer_start/end`. Loads + trips must TILE the odometer line (each segment's start = prior segment's end; no gaps, no overlaps) or deadhead double-counts. Wiring trips INTO the deadhead metric stays deferred (backlog), but capture odometers now so that wiring is later a pure calc change (no migration/backfill).

**DEADHEAD vs REPOSITIONING (Jason's vocabulary — governs, do NOT conflate):** *deadhead* = per-load empty miles to reach the next load's pickup (the odo-delta-minus-loaded above); automatic, lives on the load, NEVER a trip — cost forced by the load taken. *repositioning* = empty miles driven to a BETTER MARKET with NO booked load being fetched; speculative; REQUIRES a logged trip to capture the odo gap; is one of the four categories.

**ODOMETER PREFILL (must-have this version — data protection; Brandie also inputs):** create-trip form auto-prefills `odometer_start` from the truck's LATEST recorded odometer = `MAX(odometer_end)` across BOTH loads AND trips (NOT just the last load's drop — else back-to-back repositioning trips overlap). RESOLVED (2026-07-08): sourced via a small backend endpoint `GET /trips/latest-odometer` (authoritative in SQL; the trips page has no loads client-side to derive from).

---

## OPEN DECISIONS (resolve at next session start)

1. **TRIP CATEGORIES — RESOLVED 2026-07-07.** Final list: `repositioning | home | shop | personal`, **required**, as a new `trip_purpose` ENUM + column on trips. No "Other" bucket (add ENUM values later if a real one appears; near-empty table = trivial migration). "bobtail" rejected — it's a truck *configuration* (no trailer), not a purpose; would be a separate boolean if ever needed. Vocabulary: see DEADHEAD vs REPOSITIONING in the Trips Arc section.
   - **Build touches:** (a) migration ✅ (`023` on dev); (b) backend ✅ (allowedFields + validation, tested); (c) types ✅; (d) Issue #3 page WITH the category selector from the start — **THIS IS THE ONLY REMAINING PIECE.**

2. **Version cleanup — RESOLVED 2026-07-08.** Verified clean: `package.json` = `1.22.0` == tag `v1.22.0` (at PR #147 outstanding-loads); no stray bump. HEAD is 7 commits past v1.22.0 (trips backend/data/purpose, untagged). Trips page ships as a `feat` → **1.23.0**.

---

## REFERENCE SPECIFICS (volatile — update as the stack/paths change)

**dash — stack & locations:**

- Frontend: React 19 + TypeScript + Vite + Tailwind + shadcn/ui, on Vercel. Path alias `@/`. Local: `~/projects/dash/frontend/`.
- Backend: Express 5 + Postgres (Supabase); two projects (prod + dts-dash-dev). Local: `~/projects/dash/backend/`. `db` from `db/pool.js` (`db.query`; `db.pool.connect()` for transactions).
- Charts: **recharts** (needs explicit hex/CSS-var colors, not Tailwind classes; needs explicit-height `ResponsiveContainer`).
- Testing/REST: **Vitest**; `.http`/`requests.rest` (REST Client) — re-run Login for a fresh JWT (~1hr expiry).
- Version lives in `frontend/package.json`, treated as the app version. Bump via `npm version <type> --no-git-tag-version` from `frontend/`, commit, tag manually on main after squash-merge.
- Commit format: `type: description - closes #N` (repeat keyword for multiple: `closes #X, closes #Y`).
- GitHub: user `delgado-jason`, repo `dash`, project board "DTS - TMS Roadmap".

**dash — brand kit (8 tokens):** steel `#0d1117`, iron `#1c2333`, plate `#2a3347`, amber `#e8940a` (primary), light/chrome `#ebedf5`, muted-text/silver `#9daabb`, amber-dark `#b5700a`, amber-light `#f5b03a`. Semantic status tokens: positive(green)/negative(red)/aware(amber)/neutral/info(blue).

**dts-tools — location & setup:**

- Location: `~/terminal-course/dts-tools/`. Venv at `venv/` — **every session starts with `source venv/bin/activate`** (look for `(venv)`; without it `python3` may fall back to system Python 2.7).
- PDF parsing: **pdfplumber**. Parser architecture: one file per document type; no function over 30 lines.
- GitHub-first, conventional commits, iTerm2 + VS Code.
- (Side Course B = Python. Current progress/queued tasks are under "dts-tools — COLD" above.)

---

## BACKLOG (logged, not scheduled)

- **Lane Profitability page** (standalone, cross-agent aggregate, RPM-ranked/color-coded; regions via a state→region frontend constant; future map with geocoded markets). Was next-up before trips redirect.
- **Agent customer intelligence** (direct-shipper vs spot-market distinction; shipper concentration; search by customer geography — lives on agents list).
- **Richer agents list** (search/filter; per-agent metrics → the SQL-aggregate-vs-JS-function decision, deferred).
- **Cost data into dash** → upgrades dashboard from revenue-lens to true net-profit; makes break-even computed not a constant.
- **Wire trips into dashboard deadhead metric** (currently trips are standalone v1; deadhead calc uses load odometers only).
- **Flag loads/trips missing odometer readings** (data-quality feature).
- Chores: remove dead delete-agent route; drop obsolete `agents.notes` column; build out placeholder pages (Fuel Entries etc.); backend should return DATE cols as plain YYYY-MM-DD strings (band-aid: `{timeZone:'UTC'}`).
- **IFTA: explicitly OUT of scope** — Jason decided to leave IFTA to third-party software.

---

## BEHAVIOR LOG

- **2026-09-04 · Review tab + tier caps 5/10 (fix, v1.195.1, PR #453).** Jason: Monthly Review gets its own clickable TAB on /relationships (house tab-strip grammar from MaintenancePage: THE BOOK | MONTHLY REVIEW views); tier caps → T1 = 5, T2 = 10 (TIER_CAPS map replaces T1_CAP; per-tier cap enforcement in the retier popup, headers show "of N max"; over-cap seats grandfathered — cap blocks moving IN, never ejects). Guide updated. Tests 782.

- **2026-09-04 · The Monthly Review (feat, v1.195.0, PR #452).** Jason: pull each agent's monthly stats to weigh tier moves — then HIS pushback: ~8 loads/mo means a monthly window is noise. DOCTRINE LOCKED: review MONTHLY, judge on TRAILING 90 DAYS (the app-wide ~90-day rule; v2 mockup approved after remock). /relationships board: window picker (90d ending on chosen month's last day, current month clamps to today, stepper capped at current month), rows grouped by tier then revenue: loads·90d, net revenue, net $/mi (null under scorecard 2-load min) graded vs the NET ladder (getRateLadder(breakEvenRpm)), inbound % (attributed only), LAST-LOAD recency (amber>30d red>60d — at his volume recency beats monthly counts), touches out/in (the fairness column). MOVE chips = ADVICE with printed evidence rules ON the board: ▲ = 3+ loads + data says call-first; CONVERSION EXCEPTION promotes on the first DELIVERED load in-window (delivery basis — same as every column; already-T1 converts get no phantom ▲); ▼ = T1 + ≤1 load + >60d cold, touches cited ("you held up your end" vs "run the cadence first"); <3 loads → THIN — NO VERDICT (never a fake grade). COPY REPORT = plain-text export. Tap name → retier popup (Brandie read-only). Review agent caught: literal \n JSX node, future-stepping month guard, pickup/delivery conversion-window mismatch (a straddling first haul made a 0-load phantom ▲ or vanished entirely), T1→T1 phantom ▲, footer-vs-code rule drift, untested window boundaries — all fixed+pinned. Tests 782.

- **2026-09-04 · Agent detail page crash (fix, v1.194.1, PR #451).** The v1.194.0 touch-history hooks were inserted BELOW the page's isLoading/error early returns → rendered-more-hooks crash the moment loading finished ("The agent detail page is broken" — Jason). Tests/tsc/build all green through it; only runtime (or eslint react-hooks) catches this class. Hooks moved above the returns with a scar comment. LESSON → verify loop now includes `npx eslint` on touched files (lint ran clean on the other three recently-edited pages). Tests 766.

- **2026-09-04 · Touch history + mislog delete (feat, v1.194.0, PR #450).** Follow-up #1 from the relationships roadmap. Touches now WEAVE into AgentDetailPage's existing activity timeline (buildTimeline grew a third arm: notes + ratings + touches, one stream, newest first) — direction-colored rail (inbound green / outbound blue), method icon, type label, note, and a per-row ✕ (deleteAgentContact finally has a caller; optimistic remove, metrics recompute since everything derives). AgentActionPopup on /relationships links "Touch history & profile →" to the detail page. Remaining roadmap (deliberately waiting on data): location-matched cold prospects (needs pool), conversion-by-source (needs conversions), October gauge review together. Tests 766.

- **2026-09-03 · The relationship system (feat, v1.193.0, PR #448).** Jason's planned system (planning→design→build, both mockups approved; broker/agency-code amendment honored). MIGRATION 064 (PROD only — dev DB auth still broken): agents += relationship_tier (1/2/3 default 3, tier_set_at stamped SERVER-side on retier), agent_city/state, source; agent_contacts (direction/method/type/note/load_id, RLS — last-touched ALWAYS derived MAX, never stored); loads += booked_via ENUM. DOCTRINE: tiers are the OWNER'S call (scorecard GoToTier only paints blue DATA-disagrees chips — gut-vs-data grammar); prospect lifecycle 100% DERIVED (0 loads = prospect; cold outbound = touched, 14d follow-up; inbound = replied, 7d; first load = auto-graduated — NO status column); attribution REQUIRED on new loads (LoadForm 2-tap toggle; legacy nulls outside every denominator); dash DRAFTS+LOGS but NEVER SENDS (authenticity; no mail infra). /relationships page: THIS WEEK'S RITUAL (Mon capacity blast w/ Foreman-anchored empty line + one-tap log-all; Tue = longest-untouched T2; NOW close-out rows per undelivered… per un-closed-out delivery w/ draft; Fri auto list of T1s run), THE TIERS (T1 cap 8 enforced in retier popup), IS IT WORKING (inbound trend w/ SYSTEM_START 2026-09-03 baseline ReferenceLine, per-tier bars, cold funnel). lib relationships.ts (17 tests) + relationshipTemplates.ts (capacity + close-out drafts, emptyNextAnchor-based). T1/T2/T3 cadence 7/14/90d. REVIEW (verify stage partially killed by session limits — findings adjudicated solo): BLOCKER fixed — single-load SELECT lacked booked_via so ANY detail-page edit wiped attribution (3-layer chain: getLoad SELECT + LoadDetailPage initialData + patch-writes-null); /relationships moved OUT of AdminRoute (Brandie's link was dead — page-level canRetier gate is the only tier guard); tier_set_at client-strip (server truth only); booked_via + contacted_at backend validation (400s not 500s); capacity blast idempotent-per-day w/ named failures; Friday = one button PER agent w/ thanked-✓ state (was fri[0] only under a plural label); frozen-mount `now` clamps (touches logged in-session read 0d not −1d); prospect broker code pre-matches existing (no dup 500). Tests 765.

- **2026-09-03 · FSC coverage graded vs PAR, not 100% (feat, v1.192.0, PR #447).** Jason: the fuel-surcharge report "is always going to show red" — correct: the card demanded FSC ≥ 100% of fuel, structurally impossible (a surcharge pays only the price ABOVE the contract peg, on LOADED miles only). THE STANDARD (taught + implemented): par = (price − peg)/price × loadedShare × (mpg/6.0); FSC_PEG = 1.25 (industry-typical DOE base — JASON MUST VERIFY vs his Landstar ICOA FSC schedule and correct the constant), FSC_BASE_MPG = 6.0. His real months vs par: Jun 74% vs ~48% par, Jul 58% vs ~54%, Aug 53% vs ~51% (diesel spiked $4.94) — he's been ON PAR all along; the red stamp was a threshold bug. fuelVsRevenue(entries, loads, avgMpg) gains per-month gallons/loaded/driven → expectedCoverage; card grades par(green) / within-5pts(amber) / short(red, "FSC leaking"), stamp SURCHARGE ON PAR / NEAR PAR / SHORT, gap = dollars under PAR (not under 100%); trend bars colored vs each month's own par with a par tick per bar. Guide rewritten (the peg/loaded-miles/moves-with-diesel lesson). Tests 748.

- **2026-09-02 · The Rate Dial + market dial advice (feat, v1.191.0, PR #446).** Jason's own design (mockup approved): tiers are SET in MARGIN space — one slider per freight class (Standard/Specialized) on Settings; rungs derive target−5/target/target+5 in margin space → converted (markup = margin/(1−margin), lib rateDial.ts) → written to the SAME rate_tier_* columns (Scorer/ladders untouched, no migration). Track shows fixed BREAK-EVEN 0% + GOAL markers with band shading — padding is GEOGRAPHY, never a hidden fudge (his "extra padding" ask was deliberately made visible instead of automatic). Danger: <0% red "every mile pays the broker" (rungs FLOOR at 0% — dial never writes a losing tier); 0→goal amber down-market warning. Live readout: margin ⇒ markup ⇒ $/mi gross off live cash break-even + window label. BandedLadder app-wide prints BOTH currencies per rung ("+25% ⇒ ~20% margin"). MARKET PAGE "THE DIAL ADVICE" board: FRED trend → recommended dial positions anchored to the GOAL (softening → goal+2, flat → goal+5 house posture, firming → goal+8; never below goal; spec = std + his risk premium floored 5pts; within 1pt → HOLD) — documented heuristic, dialAdvice() in rateDial.ts, tested. TierCard deleted. Tests 744.

- **2026-09-02 · One margin truth — goal-derived bands + lens labels (feat, v1.190.0, PR #445).** Jason (after the rationalization talk): 15% is the researched flatbed/oversize target; two unlabeled margins + hardcoded bands confused him; "values need to stay consistent throughout the app — one page can't contradict another." BUILT: (1) MARGIN_BANDS (8/17/27, hardcoded, pre-depreciation-era) DELETED — `marginBandsFrom(goal)` = goal−5/goal/goal+5 pts from the ONE margin_goal setting (his 0.15 ⇒ 10/15/20); marginGrade(pct, goal) threaded through ALL callers (DriverDetail lever + seasonStrong, awards.ts medal criterion, TrophyHall strong-season) — NOTE: strong band 27→20 means the strong-season medal/trophy threshold DROPPED (flagged to Jason; may fire once legitimately). (2) TWO-LENS labeling: CASH lens (break-even/ladder/2-week board — includes principal, no depreciation) vs BOOKS lens (QBO pretax — depreciation in); every margin/break-even surface names its lens+window: CostBasis.windowLabel ("Jun–Aug", gap→list, year-wrap→both years) on Pulse break-even sub, Expenses tiles ("cash · Apr–Jun"), LeversBoard Rate sub ("cash · <window>"), Expenses "Net margin · app est.", cashflow "YTD pretax margin (QBO)". (3) Break-even confirmed CASH basis, trailing 3 COMPLETE months stepping monthly (his own July design); principal≈depreciation wash (~1,960 vs ~1,805/mo) means +25% target tier ≈ 20% pretax ≥ his 15% — tiers untouched; REVISIT when the truck note retires (wash breaks). Tests 736.

- **2026-09-01 · Archive freshness + the August forecast drop (feat, v1.189.0, PR #444).** Jason imported AUGUST (a LOSS month: NI −3,235.59, income 22,695.06, opex 19,224.34 — highest of year; financing −5,948.03 = ~3,630 EXTRA principal beyond the −2,318 floor, likely plan-cascade paydown) and the forecast dropped Feb ’27 45,062 → 28,733. AUDITED penny-exact: math is CORRECT — decomposition: −10,638.05 starting-cash miss (Aug actual ending 17,206.46 vs projected 27,844.51) + −5,690.72 baseline roll (5,003.23 → 3,817.67: Feb rolled out, loss-Aug rolled in; ×0.8 tax ×6 mo) = −16,328.77 ✓. Aug revenue vs app: 1.3% drift ✓. NOTE: P&L columns are NOT a closed identity (income−cogs−opex−interest ≠ NI by ~1.1–1.5k/mo every month — his QBO has lines outside the four columns, e.g. Other Expenses; cash-side reconciliation is the enforced check, by design). BUILT: monthly_financials.updated_at on the wire; SIX-MONTH ROAD header shows "updated <date,time>" + "baseline avg of <Mar–Aug> net" — an import now announces itself instead of silently moving the forecast. FLAGGED to Jason: forecast financing floor (−2,318) is optimistic vs his actual plan-driven paydowns; app-wide freshness stamps = possible follow-up.

- **2026-08-31 · Settlement holdbacks on the 2-week cash view (feat, v1.188.0, PR #442).** Jason: settlements should be "projected minus the fuel advance" (+ avg settlement deductions). Migration 063 (PROD only — DEV project auth is BROKEN, postgres password rejected, flagged to Jason): cash_assumptions += weekly_fuel_advance (seeded 2000) + weekly_settlement_deductions (seeded 0 — NOT derivable from dash data; Jason averages real settlement statements and sets it in ASSUMPTIONS; told him plainly rather than inventing a number). twoWeekLiquidity: holdback = advance + deductions, its own VISIBLE table row (Settlements row stays projected revenue — no silent netting); holdback applies UNIFORMLY — overrides included (an override replaces the settlements figure only; the skip-on-override design was reversed after review: editing the projection silently erased the holdback, so shaving \$47 off a week RAISED its ending \$2,203); negative holdback inputs clamp to 0 (lib) + popup blocks them; fallback weeks hold back too (weekly_revenue 4647 = PRE-holdback net — label fixed). FLAGGED: PulseTab 'landing on next settlement' still shows pre-holdback net (~\$2,250 overstated) — follow-up, Jason's call. Tests 735.

- **2026-08-25 · Margin lever reads QBO pretax margin (feat, v1.187.0, PR #441).** Jason: "wire the driver card's margin lever to the QBO pretax margin." `qboPretaxMargin(actuals, 3)` in cashflow.ts: POOLED Σnet_income ÷ Σtotal_income over the last ≤3 CLOSED months of monthly_financials (not an average of ratios), label carries the actual months ("May–Jul ’26"). DriverDetailPage: lever value/grade prefer QBO (tile relabels "Pretax margin", sub "QBO · <months>"); app season estimate only as labeled fallback ("Op margin · app est."). CONTRACT: award criteria stay on APP math — seasonStrong (medals) still grades season.netMargin, so a QBO import can never re-fire medals. LeversBoard grew marginValue/marginLabel/marginBasis props (+ LeverTile sub line). Guide: bottleneck + pretax-margin cards updated. Tests 728 (his real numbers: May–Jul pooled = 22.69%).

- **2026-08-24 · Cash-flow planning layer (feat, v1.186.0, PR #440).** Jason's written spec + his DTS-FINANCIALS.xlsx (QBO actuals). NEW PAGE /cashflow (admin sidebar), full-bleed. MIGRATION 062 (dev+prod, seeded): obligations grew the draft calendar — category ENUM loan_lease|insurance|other, day_of_month, draft_amount (FULL bank draft), on_pl flag; his 18 bills seeded; cash_assumptions (typed single row: weekly_revenue 4647, payroll 1908, depreciation 1804.61, fed/state .15/.05, financing_floor −2318, tax_catchup 10000); monthly_financials (PERMANENT QBO archive — 7 months Jan–Jul 2026 backfilled from his sheet, upsert-only, reconciliation-checked); forecast_adjustments (Nov 2026 = 1 wk, baby). THE NO-DOUBLE-COUNT CONTRACT: liquidity drafts COALESCE(draft_amount, amount); break-even (monthlyObligationCost + ExpensesPage sum + ObligationsCard list) reads amount WHERE NOT on_pl (loan rows keep principal there — interest is on the P&L); forecast reads neither (interest in NI, principal in financing_floor). LIB cashflow.ts: nextDraftDate (clamps day-31→month end), expectedPayDate = first settlement STRICTLY after delivery (his 99% rule; delivered ON Wed waits a week), twoWeekLiquidity (real loads → net revenue on their Wednesdays, fallback weekly_revenue when none booked, per-week manual override; beginning = latest snapshot OPS balance, overridable; red line = plan float_line NOT a second constant), buildForecast (baseline = avg NI last 6 actuals — his 5003.23; weeks_off shave weekly revenue; dep added back; endings chain to his 41,572.88 Jan'27 — his sheet is the test fixture, penny-exact), pretaxMargin = NI÷income (dep now in P&L — his ask; YTD chip + per-month column). parseFinancials.ts: paste-import parser (TSV/CSV, header-skip, $()-negatives, per-row ending=beg+change check ±0.05 — archive never takes half a paste). Popups: PASTE MONTHS (backfill + monthly ritual, re-paste updates), ASSUMPTIONS, THE BILLS (calendar editor; new non-loan bills auto on_pl=true; remove=deactivate) — BILLS popup was NOT in the approved mockup (flagged to Jason as an addition; P&L bills had no editor otherwise). DRIFT AUDIT (his ask, ~1% ok): app net revenue vs QBO income YTD −2.5%, −1.4% ex-January (partial tracking month); Apr/May pair = settlement-timing offset; residual negative drift ≈ income dash doesn't track — likely TRAILER RENTAL revenue (flagged as possible future feature). Tests 721.

- **2026-08-18 · Per-diem default flip — unmarked = FULL day out (feat, v1.185.0, PR #439).** Jason: "it should automatically assume it's a full day unless I mark it home or half — redundant to mark full days all the time." THE CONTRACT: `FULL_DEFAULT_SINCE = "2026-08-18"` (exported from frontend/src/lib/perDiem.ts) — from that day an UNMARKED day is a FULL per-diem day; he only marks home time + departure/return halves; manual marks always win. Days BEFORE the boundary keep the old rules (home unless a delivered load's span covers it — hollow/confirmable — or marked) so YTD history/deduction never rewrites itself (tax safety; Jason approved boundary-at-ship-date over retroactive). SAME boundary flips unmarked-day categorization in truckMetrics (idle post-flip, home pre) and fleet dayStatus/lastHomeDay/heatmap — one model: "out unless I said home." computePerDiem walks the whole year Jan1→cap (unmarked post-flip days ARE the claim; marks past cap don't count; confirm-nudge pre-boundary only). Tap cycle post-flip: half → home → clear (full is a no-op). Adversarial review found 8 real pre-merge: era-gated header/legend (2025 board said the wrong rule), driver-page hometime chip moved CLIENT-side onto the same lastHomeDay as FleetTab (backend /per-diem/last-home was explicit-marks-only — getLastHomeDay service fn deleted; endpoint now unconsumed), lastHomeDay exhausted-scan returns the 400-day floor instead of null (alarm must not dissolve into "no marks yet" exactly when most overdue), boundary pinned day-by-day in all three consumers (>= mutations fail), past-year + cross-boundary-load-span walks tested. Tests 702.

- **2026-08-18 · ONE canonical fuel $/mi — 90-day tank windows (fix, v1.184.1, PR #438).** Jason: dashboard rig card said fuel $0.31/mi, fuel page $0.68/mi — "we gotta make sure these numbers align... I need to be able to trust these numbers." ROOT CAUSE: FleetTab divided ALL fuel spend (log starts 2026-05-24) by ALL-YEAR paid-load miles (since Jan 6) — 3 months of diesel over 7 months of driving ($15,662÷51,138=$0.306, reproduced penny-exact on prod); costToRunPerMile had the same mixed-window fuel baked in. The fuel page's tank-window math was honest ($15,050÷22,253=$0.676). JASON'S RULE: fuel $/mi = 90-DAY ROLLING window of full-tank-to-full-tank windows (fuel prices too volatile for all-time). THE CONTRACT: `fuelStats.costPerMile90` (windows CLOSED within 90d of now; mpg90/avgCostPerGallon90/windows90 factor the same set) is THE fuel rate app-wide — NEVER divide fuel spend by load miles (the logs start on different dates). costToRunPerMile = fuel90 + maint÷miles + note, NULL when fuel or maint basis unknown (never a fuel-less total); FleetTab mirrors the same gate; load-detail estimate prices off mpg90·$/gal90 (≡costPerMile90) with all-time→assumed fallbacks, labeled; latest-tank cpmVsAvg vs 90d avg, null when the tank is the only recent window. Adversarial review workflow found 10 confirmed issues pre-merge (weak FleetTab gate, load-detail all-time pricing, UNPINNED 90-day boundary — a 100-day mutation passed all 685 tests) — all fixed; boundary now pinned day-90-in/day-91-out. Tests 690. NOTE: `git add -A` swept untracked MONEY_MATH_AUDIT.md + AGENT_HANDOFF.md onto main in this PR — flagged; Jason's call: KEEP them in the repo (they're canon now, don't "clean up").

- **2026-08-17 · Plan accounts become data (feat, v1.184.0, PR #437).** Jason: "add and remove accounts on the plan page — I'm going to repurpose the trailer account and make it the vault account." Migration 061 (dev+prod): plan_accounts (name, ROLE ops|vault|reserve, position, active — RLS) + snapshot_balances (per-account rows, RLS); seeded the 5 accounts; BACKFILLED his first real snapshot (Aug 17: Ops 19,090.93 / Vault 0 / Maint 4,773.01 / Tax 545.04 / Trailer holding 1,222.72 — verified penny-exact) then DROPPED the five fixed columns. ROLES are the contract: the math reads exactly one ops (float/sweep) + one vault (cascade); reserves are watched only — lib SnapshotInput slimmed to {ops, vault} (the holding-invariance is now typed away, test asserts the shape). Backend: accounts CRUD + createSnapshot takes balances[] in a pool-client TRANSACTION (db.pool.connect BEGIN/COMMIT — house pattern). PAGE: snapshot popup renders one input per ACTIVE account (role chip on ops/vault; save requires both); Reserves board = dynamic reserve accounts; Ops/Vault cards + trend resolve via roles; EDIT PLAN gains THE ACCOUNTS section (rename inline / role select / ✕ REMOVE = deactivate, history preserved / + ADD ACCOUNT). His repurposing path: rename 'Trailer holding'→whatever + role vault, retire the old Vault row — no code. Marge NOW READS snapshot_balances JOIN plan_accounts (breaking change for her queries — flagged). Tests 677.

- **2026-08-16 · DieStamp contrast (fix, v1.183.2).** Jason: the DELIVERED stamp on load detail needs more contrast. Root cause: the S8 deboss design made the TEXT the dark carve (delivered fg #0c2417 near-black-green on the #2c3549 plate, legibility hanging on a 1px light edge). Inverted the trick: verdict now BRIGHT status color (delivered #6fd08c / paid #7ab0e8 / cancelled+tonu #f08a8a), carved look via dark top text-shadow + faint light bottom edge, glow bumped. EDGE map deleted. DieStamp is shared (LoadDetailPage only consumer today) so any future stamp site inherits. DESIGN LESSON: deboss-by-dark-text on dark metal never reads — carve with shadow, not with the glyph color.

- **2026-08-16 · Trailer semantics corrected (fix, v1.183.1).** Jason's catch on v1.183.0: the snapshot's `trailer` column is his trailer HOLDING account (trailer revenue in → note $230 + loan guarantor out, ZEROES MONTHLY; 55% of trailer revenue committed to vault monthly) — NOT the trade-up fund. The trade-up fund is "any money over and above the vault cushion when I get to that stage" = VAULT OVERFLOW. FIX: 'trailer'-kind stages are now OVERFLOW FUNDS — done when vault − ratchet ≥ target_lo, progress = above/target, currentValue = the above-ratchet amount; the snapshot's trailer column drives NO stage (test pins holding-account invariance: trailer 0 vs 99,999 → identical waterfall). stageDone/stageProgress take the running ratchet (grows only via earlier completed vault stages). UI: snapshot field 'Trailer holding', Reserves board → 3 cells (+ Trailer holding · zeroes monthly), editor kind label 'trade-up fund · vault overflow above the ratchet', waterfall caption 'above the ratchet · of $15–18k', Guide reworded. DB unchanged (kind value 'trailer' kept, column name kept — semantics live in the lib, commented). Tests 677.

- **2026-08-16 · Account Status — the plan tracker (feat, v1.183.0, PR #434).** Jason's full spec + two mockup rounds. THE FRAMEWORK: `plans` (year/label/float lines/weekly reserves/active — one active at a time, POST/PATCH auto-retire others) + `plan_stages` (the waterfall as ORDERED DATA: kind vault|obligation|trailer, obligation stages BIND to obligations — Best Egg bound live at seed, IRS ships unbound until he adds the obligation) + `account_snapshots` (append-only Friday history). Migration 060 dev+prod, RLS all three, seeded THE 2027 PLAN (float 10k/home 13-14k, maint 500/wk, tax 350/wk; stages IRS→floor 15k→Best Egg→trade fund 15-18k→cushion 30-35k). THE CASCADE (his refinement): vault stages RATCHET the protected level; overflow = vault − highest completed vault threshold funds the active non-vault stage ("anything above the floor starts contributing to Best Egg"). RITUAL DECISION (his question, my call he accepted): snapshot FIRST (raw balances) → the page issues orders (SWEEP $X / send overflow) → next snapshot confirms. lib/metrics/planStatus.ts pure + plan-as-input (2028 = data): getSweep/getCushionProgress/getWaterfallStage/getPlanStatus — 15 tests incl. his exact 16,200→$1,200-overflow case, unbound-obligation ghosting, full-done finish line. PAGE /status: plan sentence (ON PLAN/BELOW FLOAT/NO SNAPSHOT stamps + orders), Ops card (SWEEP action), Vault card (fixed non-overlapping legend), waterfall board (done/active/pending + overflow line + bind nudge), reserves, Recharts trend WITH $-Y/date-X axes (his ask), snapshot popup (live verdict as you type), plan editor popup (thresholds + stage rows ▲▼/add/delete/bind + WRITE THE 2028 PLAN = copy-forward + auto-active). ADMIN-ONLY (ADMIN_ONLY_PREFIXES + sidebar adminOnly 'Plan'). Marge reads plans/stages/snapshots/obligations raw. Guide section added. Tests 675.

- **2026-08-16 · The identity chimera (fix, v1.182.5).** Jason's repro: on Brandie's dispatch board → clicked through → landed on HIS admin dashboard → browser back → dispatch board with a NO-NAME user → award-pop storm. DIAGNOSIS (four compounding defects, all frontend identity plumbing — #277's delegated-access model is localStorage-only): (1) getRole() DEFAULTS TO "admin" when the role key is missing — fail-open; any partial/clobbered storage promotes the viewer to the owner UI. (2) Identity is NON-REACTIVE and cross-tab shared: every component reads localStorage ad hoc; two tabs + one storage = mid-session identity swaps with no reload (his exact repro shape: a login/logout in one tab reskins the other tab's next navigation). (3) Award-pop seen-stores were DEVICE-GLOBAL (dash.awards.v1 / dash.dispatch.awards.v1, no user key): identity swap → another user's earned set diffs against the old baseline → STORM ✓. (4) getUser(selfId) fails silently → member null → no-name hero. FIXES: main.tsx storage-event listener — any token/user_id/role/display_name change from another tab hard-reloads (converges every tab on the current session; logout elsewhere → guard bounces to login); auth.hasCompleteSession() (valid token AND user_id AND role — tested incl. the chimera case) + ProtectedRoute now fail-CLOSED (clears + /login on any partial session — kills the admin-default path); both pop stores keyed per identity (BASE.userId) with one-time legacy migration to the first identity that reads it (no upgrade re-storm) + skip when no userId. NOTE FOR JASON: role remains a VIEW boundary — her token can reach owner data by design (#277 phase 2); real enforcement = backend role claims, a deliberate future decision. Tests 660.

- **2026-08-15 · Her projected double-count (fix, v1.182.4).** Jason: "is her committed amount being double counted? projected 4500 with one delivered at 2250 and one committed at 2250." CONFIRMED — it was ONE load wearing both hats: getWeekGrossCommitted excludes only cancelled (delivered loads stay in), and the dispatch card sums earned + committed → every delivered load counts twice in projected AND the PaceMeter (ghost stacks ON TOP of filled). The S9 Pulse double-count reborn: the owner card knew the trick (committedAhead = weekBooked − weekEarned; its PaceBar renders committed as an absolute UNDERLAY — correct), the dispatch card hand-rolled the sum without it. FIX: new rateTargets.getWeekGrossPipeline (in-window, status ≠ delivered — the slice that legitimately stacks on earned; comment names the trap) + tests pinning her one-delivered-load case (pipeline 0, projected stays 2250); dispatch card committed → pipeline. getWeekGrossCommitted UNCHANGED (owner underlay semantics intact). LESSON (now twice): any surface computing projected = earned + something must use a something that EXCLUDES earned — grep for new '+ committed' sums in future reviews. Tests 657.

- **2026-08-15 · Her pace card: BOOKED AHEAD (fix, v1.182.3).** Jason: "she has 5220 in booked status, but it says otherwise." Diagnosis: NOT the net-named-gross fns (her card already uses the getWeekGross* twins) and NOT attribution (load 4024146, $4,573 lh + $647 FSC = exactly $5,220, booked_by Brandie ✓). It's the WEEK WINDOW: the load delivers Aug 21 — next pay week (current: Wed Aug 12 → Tue Aug 18) — and committed counts by landing week, so a Saturday booking for next Friday showed nowhere. FIX (additive; week-pace semantics untouched): new rateTargets.getBookedAheadGross(loads, weekEnd) = gross of live (booked/in_transit, undelivered) loads with delivery_date ≥ weekEnd; test pins her exact $5,220 case incl. this-week + already-delivered exclusions. Card shows amber "BOOKED AHEAD · lands beyond this week" cell when > 0. FORK STATED TO JASON: if he'd rather her whole pace grade by BOOKING week (work done) instead of landing week, that's a deliberate metric change to make together — not slipped in. TEST-FILE LESSON ×2: blind `import {` replace hit the vitest import; and the second card-anchor mismatch died pre-write while the shell chain kept going — a bumped version + green build proved NOTHING had applied. Verify the write happened, not just the pipeline exit.

- **2026-08-15 · Dispatch board: HEAT meter un-vanished + THE RACE unified (fix, v1.182.2).** Jason: "the other session took away the grind meter… the agent leaderboard is not designed and using the same logic as the admins." (1) The meter wasn't removed — #428's personal mode returns hasLadder:false until 3 active booking weeks exist (Brandie's bookings only started crediting to her at #428), and GrindMeterView returned null → vanished. Ghost-rule fix: personal mode now renders the HEAT board ghosted while the baseline forges — dashed cells, "forges after 3 active booking weeks — N of 3 logged," criterion stated; Grind gains optional activeWeeks/weeksNeeded (computePersonalGrind + empty case populate them; test added). (2) Her leaderboard was the old TopAgents (revenue-ranked, comic-era) while the admin's Agents tab runs the forged quarter RACE plate (quarterStandings + contenders + REIGNING CHAMP). Extracted the plate verbatim into shared components/dashboard/agents/RaceBoard.tsx (self-contained: useAgents + quarter logic inside) — consumed by BOTH AgentsTab and DispatchDashboard (anti-drift, BandedLadder pattern). DELETED: TopAgents.tsx, getTopAgentsByRevenue + collectRecentAgents from lib/metrics/dashboard (AgentRevenue + agentGross KEPT — getAgentGrossTable uses them; first cut sliced too deep, restored from HEAD and re-cut surgically). Tests 653.

- **2026-08-15 · Dispatch Forge achievements to the hardware grammar (fix, v1.182.1).** Jason: "The dispatcher forge needs to have the achievements look like the design we created." The v1.182.0 Forge page had rolled its own award visuals — emoji icon map (🪙👑🔒 + fallback), hand-rolled TIER_RING circles, gradient Bar, comic-cream #f5e6c8. Replaced with the approved hardware grammar shared with the driver's board: earned coins = forge/Coin struck metal by tier (bronze/bronze/silver/gold/platinum map), unearned = dashed ghost circle + hint (ghost rule); tags = punched amber chip (name · badge) / dashed ghost + hint; season crowns = die-stamp plate "SEASON CROWN" / dashed "UNCLAIMED"; ALL progress = MeterCells (now EXPORTED from HardwareBoard so the two surfaces cannot drift — BandedLadder lesson applied). Kept everything #428 ordered: page structure, hero, NEXT UP within-reach sorting, how-to-earn line on every award, personal-pace basis, pops. The ★ rank glyph stays (house grammar). Emoji gate on the page: only ★ remains.

_Append a dated entry whenever Jason requests new behavior or functionality from me. Newest at top._

- **2026-08-08** — **VISUAL FACELIFT ARC STARTED (reskin + motion pass, NOT a rebuild).** Jason's brief: make dash look/feel like a premium game without changing what anything computes. **FROZEN:** backend/**, src/lib/**, src/types/**, src/services/**, all formulas, trophy catalog semantics, routes/URLs. **FAIR GAME:** src/components/**, layouts, page visual composition, Tailwind tokens/theme, plus a NEW lazy-loaded animation/3D layer (@react-three/fiber + drei [needs fiber v9 for React 19], gsap + @gsap/react useGSAP; prefers-reduced-motion + non-WebGL fallbacks REQUIRED; never block data on WebGL; must stay smooth on Brandie's tablet/phone). **Hard gates in order: audit → approved mockups (animated HTML artifacts for motion surfaces) → approved design-system tokens/primitives → implement in slices (branch-per-issue, 488-test suite + strict build green every step).** AUDIT DONE 2026-08-08: baseline green (488 tests/53 files, strict build clean); main bundle 1.64MB/493KB gz — only Lanes code-split, so the new layer must be lazy chunks (+ opportunity to split more routes); motion today = single-shot CSS keyframes only (award-pop/stampSlam/shimmer), no orchestration lib, no 3D; reduced-motion support ~absent (1 ref, RollingNumber); already in place: 8-token kit in Tailwind v4 @theme, 4 fonts (Bebas/Barlow Condensed/Barlow/Bangers), ds-* layer (Panel default/steel/hero+noir+interactive, ds-input/ds-ctrl, ds-skeleton, ds-sfx), Web Audio sfx.ts cues (motion should sync to them); game surfaces = AwardPopHost stack + Recap/Trophy ceremonies, player/dispatcher cards, TrophyHall (approved AI art), Garage/RigCard, prestige medallions/stamps/mile clubs/grind/health gauge. Standing rules carried in: Expenses stays sober; drill-down links everywhere. **SCOPE REDIRECT (Jason, same day): game layer SECONDARY — this is an adult TMS; operational value first, game stuff revisited after. Deliverable = app-wide UI/UX facelift (GSAP + three/R3F), charts included, UNIFY the two coexisting design systems (old: agent/load detail pages; new: dashboard tab shell). He asked for 3 mockups of MY directions.** Delivered 3 + a blend, all as artifacts (same content each — dashboard + load-detail fragment, real-shaped numbers, animated, reduced-motion verified, hover tooltips; chart marks CVD/contrast-validated for dark: #c87f0a amber / #4f8cd6 blue — brand amber #e8940a too bright for data marks, stays UI-accent): **A · Night Cab** (instrument cluster, hairline-etched board, mechanical motion) https://claude.ai/code/artifact/321eef10-3d91-4805-ae7a-e8ad32d20c60 · **B · Manifest** (de-carded editorial freight-ledger) https://claude.ai/code/artifact/3d632696-0ee3-4f46-aba7-a8bf60e8e818 · **C · Forged** (machined plates/depth, evolution of ds-panel) https://claude.ai/code/artifact/096da506-3ea2-4861-9e26-8c0a037eae2a · **A+C blend** https://claude.ai/code/artifact/714eded6-a9f7-4b47-8e5a-746850814dda. **✅ LOCKED (Jason 2026-08-08): A+C — "Night Cab, forged reserve." Governing rule: FLAT = things you read (telemetry/charts/tables, etched hairline panels); DEPTH (machined/forged plates, tilt, glow) = ONLY things you chase or win (pace target, rank, net, stamps). GSAP/R3F budget spent only on reserve surfaces — cheap on Brandie's tablet.** **COMIC-NOIR RETIRED for this design (Jason: doesn't fit)** — Bangers/halftone/ink-stamps/confetti out; NEW game-layer theme in design: **"The Forge"** — awards as machined objects (records=engraved plates, patches=stamped steel tags w/ punched ×N pips, medals=struck coins, trophies=milled monuments, streak=heat, win-stamps=die-pressed deboss), ceremony = strike/sparks/cool instead of confetti; no new fonts (Bebas carries it; Bangers dropped); sfx.ts synth cues KEPT (already mechanical). Trophy-hall AI art will need a re-brief/regen in the Studio if forge theme locks (Jason-driven, ~cents/image). **✅ FORGE THEME + STENCIL APPROVED (Jason: "I love it all. Let's keep going.")** — theme mockup https://claude.ai/code/artifact/b748c6be-7f65-48fc-8116-d488d8e9d5c6. **GATE 3 IN REVIEW: Design System v2 spec delivered as a living artifact** (tokens + primitives all rendered FROM the tokens): 4 principles (flat=read / forged=chase / struck=won / quiet game — money pages zero game), full color tokens (canvas #070a10 / panel #0c111a / well #090d15 / hairlines #1c2637·#141c2a / ink·dim·faint / amber trio + NEW hot #ffcf7a / forged plate stops #2b3448·#1f2737·#171e2c + edge-hi/lo / chart marks c-amber #c87f0a · c-blue #4f8cd6), type roles (Bebas identity · BarlowCond all numerals tabular · Barlow body/labels · Big Shoulders Stencil 600/700 = Forge voice only), elevation 0 canvas/1 board/2 forged/3 takeover + inset well, motion tokens (t-fast 120/quick 240/base 500/slow 900 · ease-mech (.22,1,.36,1) · ease-settle (.22,1.4,.36,1) · ease-strike-in · ease-slam · stagger 60–90) + SIX VERBS (engrave/punch/settle/sweep/cool/slam), ceremony beat map (dim→press→impact→cool→verdict→roll-on; records/patches = corner toasts; trophy = +headlight sweep, the ONE R3F scene), chart rules, and the IMPLEMENTATION MAP: tokens→index.css @theme v2 block (v1 tokens coexist until migration done), fonts→index.html (+BSS; Bangers removed at S13), NEW src/theme/motion.ts, primitives→ui/ (Panel→Board/ForgedPlate + BoardCell/Well/StatusPill/AlertLamp/Btn/Ctrl), NEW src/components/forge/ (Coin/SteelTag/RecordPlate/Monument/HeatStrip/DieStamp/StrikeCeremony), charts theme.ts, deps gsap+@gsap/react at S1 / R3F+drei lazy at S12 only. **13 SLICES (branch-per-issue, 488 tests+strict build green each):** S1 foundation(no visual change) → S2 primitives+shell → S3 Pulse → S4 other dash tabs+dispatcher → S5 loads list/detail → S6 driver card+forge hardware → S7 agents/vendors → S8 fleet/maint/compliance → S9 money pages(sober) → S10 ceremonies GSAP → S11 recap/garage → S12 Trophy Hall R3F+art re-brief → S13 cleanup (drop Bangers+v1 tokens+dead comic comps). Guardrails per slice: Guide page updates, drill-down links, reduced-motion built-in, semver. **✅ GATE 3 SIGNED OFF (Jason 2026-08-08, spec https://claude.ai/code/artifact/03b7db3f-a166-4b0e-904f-5767ff75837b) with ONE AMENDMENT — prefers-reduced-motion DROPPED: Jason: "I invite that. Motion is good." Motion always plays, app-wide; do NOT add reduced-motion gates. (Instructor counsel on record: PRM is an OS-level opt-in, would never affect Jason/Brandie unless set at device level; matters mainly if dash productizes; re-add = one gate in src/theme/motion.ts + media blocks. Decision is Jason's — do not re-add or relitigate without him.)** → **GATE 4 STARTED: S1 foundation** (v2 tokens in @theme + :root motion vars, Big Shoulders Stencil in index.html [Bangers stays until S13], NEW src/theme/motion.ts [DUR/EASE/GSAP_EASE/STAGGER, no gsap import = zero bundle cost], gsap + @gsap/react installed) — **S1 SHIPPED: #349 → PR #350 squash-merged (`d6b304b`), tagged v1.140.0, zero visual change, Vercel auto-deployed.** **S2 BUILT (primitives + Night Cab shell): #351 → PR #352, branch ds-v2-s2-shell, v1.141.0** — new ui primitives Board/BoardCell + ForgedPlate/Well + StatusPill/AlertLamp (built from v2 tokens, unconsumed until S3) + `.ds2-*` styles + `--color-rail #05080d`; shell restyled: sidebar vars→v2 (rail bg, hairline borders, amber ring), active nav = `.ds2-nav-active` amber glow rail, Bebas "DASH / DTS" wordmark, app `--background`→canvas (unmigrated pages keep their own bg-iron wrappers — mixed frame is EXPECTED until slices land), trigger bar hairline. Zero behavior change (nav structure/roles/accordion/sfx untouched). 488 tests + build green. **S2 SHIPPED: PR #352 squash-merged (`fd4fc3f`), tagged v1.141.0.** **S3 BUILT (Dashboard Pulse): #353 → branch ds-v2-s3-pulse, v1.142.0** — PulseTab rebuilt on the system (ALL metrics/hooks byte-identical): pay-week hero = the view's ONE ForgedPlate (chamfer+tilt) w/ LED PaceSegments (earned solid/hot tip, committed dimmed, floor+target markers — floor read from targets.gross.weeklyBreakEven, presentation-only); vital signs → Board+BoardCells w/ tone underlines; weekly-earnings SVG → validated chart marks (now=amber-hi, hit=chart-amber, miss=plate-b); rate-vs-floor → Night Cab scale instrument (red zone, ladder ticks+names, amber needle w/ glow); FIRST production GSAP (ds2-boot stagger + CountUp component + needle sweep, all from theme/motion tokens); GrindMeter → Forge HEAT (font-forge stencil, heat cells hot/warm/below/home, "RUNNING HOT" — first comic surface retired, Bangers+Stamp+halftone OFF it); DashboardShell tabs/AlertBanners/WhatsNext re-tokened. 488 green, build green. **S3 FEEDBACK (Jason): shipped Pulse missed mockup details — the OPERATIONS statusbar (Bebas title + day/date/week#/pay-week + Road Captain & streak chips) and full-bleed (no chrome bar above the page; canvas to the top). LESSON: the approved mockup is the CONTRACT — implement to it exactly, don't approximate.** Fixed on the S3 branch (`798ffae`): new OpsStatusbar (rank via careerRank(max odometer across loads+trips, the Garage basis), streak via useGrind, ISO week + pay-week span from targets.weekStart, SidebarTrigger inline) + AppLayout FULL_BLEED_PREFIXES mechanism (owner /dashboard only — dispatcher keeps chrome bar until her slice; list deleted at S13). **NEW STANDING RULE (Jason 2026-08-08): EVERY page slice gets its own mockup in the locked system, approved BEFORE implementation — the design-first gate now applies per-page, not just per-arc. He specifically wants the Lanes page (/lanes, d3-geo US map) elevated with GSAP + R3F — map mockup in progress.** **S3 SHIPPED: PR #354 merged (`a3b53c5`), tagged v1.142.0. LANES MOCKUP APPROVED (Jason: full R3F treatment) — https://claude.ai/code/artifact/b8852b1c-f3b3-4d38-af47-e0f8a540e006. S4 BUILT (Lanes page): #355 → PR #356, branch ds-v2-s4-lanes, v1.143.0 — R3F situation board (lazy 712KB chunk: three/fiber/drei/postprocessing; states extrude by volume via the real albersUsa projection; bloom arcs from origin_state→destination_state flows, traveling pulses; GSAP west→east boot; parallax; hover/click-drill/level-grouping preserved; shared mapColor.ts so SVG+3D can't drift; SVG board kept as non-WebGL fallback [capability check, NOT a motion pref]; flame→machined hot ring); full-bleed statusbar w/ window+mode segs; Kpis/Table/DetailPanel re-tokened; FULL_BLEED_PREFIXES now role-aware ({prefix, ownerOnly}). 488 green, build green, main bundle unchanged. S4 SHIPPED: PR #356 merged (`b09f812`), tagged v1.143.0 — the R3F board is live.** **NEW STANDING PROTOCOL (Jason 2026-08-09): before each page's slice, deliver a PAGE EVALUATION — structure, KPIs, design/placement, what's lacking, improvement recommendations, candidate game elements/data points — THEN the mockup, then build. IA RULE: dashboard tabs TELL THE STORY (Pulse=now/this-week/momentum · Money=months/structure/P&L/AR · Lanes=where · Agents=who · Fleet=iron); individual pages COMPLEMENT tabs, never duplicate. "We're going to restructure the app" — composition/IA changes are in scope (still presentation-only; lib stays frozen).** **PULSE REVIEW under the protocol (2026-08-09): KEEP hero forged pace plate/HEAT/rate instrument/on-time+deadhead. FIX (fold into Money slice, one PR): (1) rate story told 3× on one tab — keep ONLY the scale-instrument card; recut hero right column to MTD + quarter pace + next-settlement beat; (2) DROP the Pipeline vital cell — MoneyTab already owns a Settlement-pipeline section (verified in code); (3) vitals cells get drill-down links (house rule, carried gap); (4) GAME-AS-DATA add: "Best week" PB cell + PB marker line on the weekly chart + pace-plate PB-in-reach glow — all computable from the existing 8-week series already in the component, zero lib. VERDICT: clear to proceed to Money.** MoneyTab current structure (read 2026-08-09): 4 Tiles (Income·YTD net-of-cut / Operating profit / Margin·after-notes vs marginGoal / Best month), monthly P&L layered bars (income+profit overlay, margin % row, negative months dark red), margin trend (operating vs after-notes vs goal), cost-structure category strip, settlement-pipeline section. Money evaluation + mockup = next deliverable. **ABSOLUTE RULE (Jason 2026-08-08, reinforced): NOTHING ships — no merge, no deploy, no publish of user-facing product — without his explicit per-item authorization. No blanket pre-approvals.** **BUG + LESSON (Jason spotted, 2026-08-08): shipped Pulse rate-vs-floor instrument has OVERLAPPING ladder labels — root cause: the build kept the legacy 0→gaugeMax pos() domain while the approved mockup used a floor-anchored padded domain, cramming real ladder values into the right third. LESSON: when building from an approved mockup, port the mockup's GEOMETRY/scales, not just its skin. Fix: floor-anchored domain (pad below walkAway/above strong) in PulseTab — PR'd, awaiting authorization. Also audit every scale/label surface for collisions before presenting.** #357 AUTHORIZED + merged (`efa39c6`), tagged v1.143.1. **TWO MORE STANDING RULES (Jason 2026-08-09): (1) PLAIN LANGUAGE — no invented jargon or abbreviations ("PB", "the nut" both rejected); use Jason's own vocabulary (the app already says "notes"); the Brandie test: if she'd have to ask what a word means, the surface failed. (2) TRACKER HEADROOM — never pin a target at 100% of a meter: he regularly BEATS target, so targets sit at ~78% with an overdrive zone past them (over-target cells burn hotter). Applies to every gauge/pace surface.** Both tab mockups revised accordingly + APPROVED by Jason ("both pages look good" modulo these fixes): Pulse v2 https://claude.ai/code/artifact/6bf1ec12-3ee8-4672-b312-a62444d2cbef · Money https://claude.ai/code/artifact/29ba3ac3-9667-42a2-a63b-0f0747540236. **S5 BUILT (Jason: "Build") #358 → PR #359, branch ds-v2-s5-money-pulse, v1.144.0.** Pulse recut: hero right col = MTD + quarter pace + next settlement (real date); vitals swap Pipeline→Best week, all 5 drill down (BoardCell to/go props); weekly chart best-week line (label collision-guarded vs target); shared ui/PaceMeter (target@78% + overdrive cells, plain-word markers via paceMarker()); record tag (NEW BEST WEEK IN REACH / IN REACH ≥90%); Next rail replaces WhatsNext (delivery/pickup/settlement/maintenance/compliance rows, each a door; WhatsNext.tsx now unused, prune at S13). Money rebuild: pipeline section DELETED; KPIs → BoardCells + NEW realized profit/mi (odometer-true miles from loads+trips, needs trips prop); THE NOTES ForgedPlate (month profit vs notes/goal markers, straight-line cover-pace date, coverage %, BEST MONTH plaque, NOTES COVERED stamp state, not-on-books fallback → Expenses); charts re-themed to validated marks; goal-streak chip (N MO ≥ GOAL); cat palette = cat1..6 tokens (amber/teal/violet/rose/blue/olive — CVD-validated IN ORDER 2026-08-09); "Other"=neutral plate-a; 2px strip gaps. **SETTLEMENT DAY (Jason-requested frozen-layer exception): migration 055 (settlement_day smallint default 3=Wed, in backend/db/migrations — NOT backend/migrations, the script reads db/migrations!), backend DEFAULTS/COLUMNS/validation, type+coerce, Settings selector (Sun–Sat), NEW lib/metrics/settlement.ts nextSettlementDate() + 5 clock-fixed tests. Migration RUN ON DEV; prod needs `npm run migrate` on deploy.** Guide updated (Pulse + Money TabLines). 493/493 tests (488+5), strict build green. **S5 SHIPPED (Jason: "merge s5"): PR #359 merged (`935f6ee`), tagged v1.144.0. Prod migrations run immediately post-merge — 055 AND a stale 054_vendors that had never run on prod (prod was one migration behind; lesson: check `npm run migrate` on prod at every backend-touching ship).** **NEXT-TABS EVALUATION delivered (2026-08-09): LanesTab duplicates /lanes (same LanesMap component + lanes list) — recommended slim-to-summary w/ mini map glyph (C) or no-map (A), Jason to pick; AgentsTab content strong (scatter/standings/concentration/going-cold/gut-vs-data) → v2 skin + standings as the forge surface (Q-race podium, ties to existing agent prestige) + drill-downs; FleetTab → hero = THE RIG plate (odometer + next mile-club marker on the PaceMeter, health/service), day-heatmap → heat cells, cost-to-run stays truck-scoped (Money owns whole-business per-mile — boundary). Dispatch board re-cut queued after tabs. Jason picked OPTION C (mini map thumbnail) + green-lit Agents/Fleet. THREE-TAB MOCKUP delivered as ONE artifact w/ live tab switcher (deep-linkable ?tab=): https://claude.ai/code/artifact/6e15486d-29c1-4152-aaf7-5ec180d39ee1 — Lanes: Option C (real-geometry thumbnail, zero interactions, one door; KPIs/top-lanes/mix stay, mix on cat tokens); Agents: Q3-race ForgedPlate (podium bars, Q2 CHAMP honor chip, days-left), scatter glance, cooling-cells going-cold rows, gut-vs-data kept; Fleet: THE RIG ForgedPlate (odometer CountUp + 600K-club marker on the PaceMeter @78%, service/DOT/days-out right rail), kboard (utilization/fuel/cost-to-run truck-scoped/shop spend), 5wk day heatmap in heat cells, shop-spend bars. Lanes pane render-verified; agents/fleet shots pending (all reused verified patterns) — patch-republish same URL if off. **Jason APPROVED all three ('Everything looks good') + 2 Fleet adjustments: (1) SPEEDOMETER gauges — the radial gauge from mockup A — for fuel economy (and pair it: utilization gauge too, 'speedometers' plural; new ui/GaugeDial primitive); (2) day heatmap = LAST 8 WEEKS (even two months), not 5. S6 BUILD IN PROGRESS (branch ds-v2-s6-tabs): LanesTab Option C (keep all metrics, new LanesMapThumb from usStates paths, no listeners), AgentsTab reskin (keep all data; Q-race ForgedPlate w/ computeHonors champ chip, scatter re-inked, cooling cells, gut-vs-data kept), FleetTab (RIG ForgedPlate w/ mileMilestone() from frozen lib driving the PaceMeter club chase + odometer CountUp; GaugeDial pair; 8wk heat-cell heatmap; kboard w/ truck-scoped cost-to-run). One PR, awaiting merge authorization after build. **S6 BUILD NOTES (mid-flight, branch ds-v2-s6-tabs): READS DONE — FleetTab (367L: keep ALL — util breakdown bar, cost-to-run parts fuel/maint/note per-mile, rev/mi, mi/mo, mpg sparkline last-8-tanks, road-ready dues+health badge, compliance chips, shop spend 12mo + recent visits, year-in-days heatmap via fleetHeatmap(...,26) — Jason wants that arg 26→56 IF it's days (VERIFY signature: it may be WEEKS since render does `heat.weeks*13` — check lib/metrics/fleet.ts fleetHeatmap param meaning; his ask = last 8 weeks shown as the day grid)); AgentsTab (356L: keep ALL — scorecards/roster/conc, quarterStandings live+last via MEDAL colors, BoardRow links, scatter via ./AgentScatter (SEPARATE component — reskin it too), recentRows 'running with lately', cold tier rows, flags gut-vs-data via ratingFlag, FRESH colors). REMAINING WRITES: (1) ui/GaugeDial.tsx — semicircle gauge from mockup A (arc dasharray + needle, gsap sweep), used for Fleet fuel economy + utilization pair; (2) lanes/LanesMapThumb.tsx — usStates paths + getAreaMapData state-level volume fills, NO listeners, whole card links /lanes; (3) LanesTab restyle Option C (keep metrics; kill embedded LanesMap+navigate); (4) AgentsTab reskin + Q-race ForgedPlate (liveBoard top-3 podium bars + honor chip from computeHonors? — honors import is on DISPATCH TopAgents; here use lastBoard[0] as reigning champ chip 'Q2 CHAMP'), cooling cells on cold rows (5 cells, drain by daysSince/week), KpiTile→BoardCell; (5) FleetTab reskin + RIG ForgedPlate (odometer CountUp from truck.current_odometer + mileMilestone(odo) → PaceMeter filled=odo-prev target=next-prev marker fmtMiles(next)+' club', service beat = nextSoon, DOT = chips[0], days-out = home.daysOut) + GaugeDial pair replacing Fuel/Utilization tiles + heatmap 8wk + token sweep (mpg line 5fd0e0→chart-blue, DAY_FILL→heat cells: underload=amber-gradient, home=well, idle=#5a4218). THEN: tests+build, bump 1.145.0, commit 'feat: lanes/agents/fleet tabs on design system v2 - closes #360'?? NO — issue not yet created: CREATE ISSUE for S6 first (title 'Design System v2 — S6: Lanes/Agents/Fleet tabs'), PR, NO MERGE without Jason.** **S6 BUILT & PR'D: #360 → PR #361, v1.145.0, 493 tests + build green. All writes above completed (GaugeDial min/max 4–9 mpg & 0–100%, LanesMapThumb, LanesTab Option C, AgentsTab race-plate/cooling-cells/Board-doors, FleetTab rig-plate/gauges/8wk-heatmap/token-sweep; fleetHeatmap arg was WEEKS — 26→8 ✓; Tile/KpiTile/C consts pruned). **S6 SHIPPED (Jason: 'merge s6'): PR #361 merged (`1177503`), tagged v1.145.0 — THE ENTIRE DASHBOARD IS ON v2 (all 5 tabs).** **DISPATCH BOARD evaluation + refreshed mockup delivered (2026-08-09): https://claude.ai/code/artifact/a935d79d-e247-4112-9e39-f68c3e3f6fe6 — draft refreshed thru all post-draft rules (meter 58/78% + overdrive cells; plain words already clean). Evaluation: KEEP her operational KPI set (Booked/InTransit/LoadsMTD+delta/Detention-hours/Deadhead-vs-90d), role rule (no net/RPM/P&L), searchable workhorse tables, her own pops. CHANGES: Bangers title→Bebas; RateTargetsCard→her ForgedPlate on the shared meter (she BOOKS the week — her chase); DispatcherChip + real rank machined chip (Load Wrangler · N booked · next tier); detention chips per-load in her table; KPIs→doors; TopAgents race stays on BOTH boards (role-duplication precedent = HEAT: same surface, different audience — hers answers 'who to call', owner's manages the bench). Jason: 'build s7' → **S7 BUILT & PR'D: #362 → PR #363, branch ds-v2-s7-dispatch, v1.146.0, 493 tests + build green.** DispatchDashboard rewritten (statusbar w/ trigger + Bebas title [LAST comic headline retired from daily surfaces] + DispatcherChip + REAL rank chip via getDispatcherCard().rank + SCORE A LOAD CTA; KPI Board doors incl. detention-hours amb tone + deadhead pos/neg vs 90d; ForgedPlate = ladder instrument [floor-anchored pos()] + PaceMeter + earned CountUp/committed/projected + dailyTarget well — RateTargetsCard UNIMPORTED, prune S13; HEAT beside; tables+TopAgents+DispatcherChip token-swept [11/14/11/2 swaps]; AppLayout /dashboard ownerOnly→false; Guide dispatch section updated w/ rank+meter copy). **S7 SHIPPED (Jason: 'merge s7'): PR #363 merged (`e8b2ee7`), tagged v1.146.0 — EVERY DAILY SURFACE ON v2.** **LOADS PAGES evaluation delivered (next per protocol): LIST (397L: h1+create, 4 KPIs [MTD-net/avg-rpm/Outstanding-AR/pipeline-counts], search+status filter, Panel table) — findings: MTD-net duplicates Pulse, AR KPI dead per BCO fact, pipeline counts duplicate boards → REC: KPIs become a FILTERED-VIEW summary line (count · $net · $/mi of current search+filter = the list as an answering tool, unique value), v2 skin, doors, amber Create CTA. DETAIL (1099L, RubberStamp comic at L455): the approved A+C mockup already covers its core (statusbar w/ load#+status, route rail, settlement well w/ your-net, fleet chips) → build extends to full real content; RubberStamp → DIE-STAMP deboss (FIRST Forge hardware in production, contained); GAME-AS-DATA add: the load's rate-tier grade (existing Score lib) as a header chip. Jason: 'go ahead' → LOADS PAIR MOCKUP delivered + render-verified (both views): https://claude.ai/code/artifact/37304ccd-4a1b-48a5-98b7-15d7f887888e — LIST (statusbar+CREATE CTA, search well + status segs, THE ANSWERING LINE re-sums the filtered view live [count · net · $/mi], board table w/ status pills + detention chips, rows drill to detail in-mockup) + DETAIL (die-stamp DELIVERED deboss, TAKE-IT grade chip [Score engine], booked→picked→delivered→paid timeline w/ next-Wed settlement beat, route rail w/ per-stop on-time/detention grading, settlement well, PAID stamp ghost). **FEATURE-PARITY RULE (Jason 2026-08-09, STANDING): mockups show the glance layer, but the OLD PAGE IS THE FUNCTIONAL CONTRACT — before building any page slice, inventory EVERY interactive element (forms, actions, mutations, auto-recommendations) and the PR must check each off. Zero feature loss; simplification only with his explicit ok. Trigger: he caught the Loads mockup omitting create-load form, edit-load form, update-status section, accessorials section, and the detention auto-recommendation. S8 PARITY CHECKLIST: create form (list page, CREATE CTA opens it, same fields/validation, v2 field skins) · edit form (detail, Edit btn) · status updater (controls host on the timeline stations — behavior 1:1, or keep section as-is if risky) · accessorials editor (stays beside settlement well, editable rows + add-line) · detention auto-rec (frozen lib logic UNTOUCHED — the over-free-time chip IS the recommendation surfacing; its add-detention action stays wired) · mark-paid · delete. All wiring = existing services/hooks, frozen, untouched by reskin. **RULE UPDATE (Jason 2026-08-09): 'build' NOW INCLUDES THE MERGE — 'build sN' = build + merge + tag in one flow; the build command IS the per-slice merge authorization. (No-unauthorized-ship rule otherwise intact.) **S8 SHIPPED (built+merged per new rule): #364 → PR #365 merged (`2af1b2a`), tagged v1.147.0.** LoadsPage: answering line (live filtered sums count·net·$/mi, replaces story KPIs), full-bleed statusbar + amber CREATE CTA, v2 filter wells/segs, tables→boards, ALL parity kept (create/edit forms w/ reskinned modal shells, payment filter, pinned On-the-road group, TONU, DET/det?/det-paid chips+bars, detention auto-rec). LoadDetailPage: DieStamp SHIPS (ui/DieStamp.tsx — first Forge hardware in prod; RubberStamp stays for unmigrated comic surfaces til S13), statusbar-ish header + v2 buttons + token sweeps; status/accessorial/mark-paid/delete sections untouched-but-swept. BONUS: .ds-panel--default rethemed to etched-board — every unmigrated page coheres transitionally (steel/hero variants intact). DEFERRED, NOT LOST (S8.1 queue): status-timeline control hosting + rate-grade chip (needs lightweight basis source on detail). **COMPLIANCE PAGE SHIPPED (PR #409, v1.171.0 — recommendations→mockup→'build'): DATA: 8 papers (7 items + CDL from driver record); Landstar 120 ×2 (truck+trailer) expire Sep 8 = 29d, warn 21 → AMBER FROM AUG 18; Maint Reports Sep 22/Oct 5; 2290 Aug 2027; Medical Jan 2028; Hazmat Jun 2028; CDL Jan 2027 (169d). PAGE (maintenance's sibling — papers as clocks): answering line 'N PAPERS' + forge STAMP (GROUNDED red when summary.status grounded / CLEARED TO ROLL green when expiring>0 / ALL CLEAR when nothing warns) + EXPIRED/EXPIRING chips + computed nextUp (min daysRemaining over items+CDLs); scope boards Business/Driver(CDL leadRow)/Truck/Trailer w/ OPEN→ entity links + '+ADD PAPER'; rows = clock grammar (label + category text chip, ClockCells 14 — fill via clockFill(): elapsed share of issued→expires window, else renewal_months*30.44, else 365 — PRESENTATION ONLY comment, level always computeComplianceDue; valid=dim-amber cells, expiring=bright, expired=red), right verdict 'N days · date', sub 'doc# · renews every N mo · warns inside N days'; CDL row notes 'from the driver record — same clock as the dossier's papers plate'. FORMS → ONE forged MODAL (add via adding{scope,entityId} / edit via editingId→editingItem / CDL via cdlDriver — all three states render the same popup, inline renders removed; CdlForm rebuilt in-modal w/ forged buttons). Lucide FULLY out (13 icons incl the LucideIcon catIcon map → category text chips). AppLayout += /compliance. Tests 535. Unmigrated remaining: recap, garage, score, settings.****PER-DIEM PAGE SHIPPED (PR #408, v1.170.0 — recommendations→mockup→'build'): DATA: 148 full + 15 half + 6 home marked 2026 → deductible YTD ~$8,791 ((148×$69 + 15×$51.75)×80%). PAGE: full-bleed shell; answering line = '$X DEDUCTIBLE · YTD · N full · N half · $69/day · 80%' green display-face + inferred-days amber chip; THE DEDUCTION forged card (big green money, day splits, FORMULA WELL KEPT VERBATIM — metric transparency, ‹ › text year controls replacing lucide chevrons, Settings link); calendar board THE YEAR: months-map + tap-cycle home→full→half + optimistic save/revert + load-inference ALL 1:1; cellStyle → tokens with INFERRED = 1.5px dashed amber-hi (ghost rule applied to data — 'visible, not yet confirmed, tap to claim'), future = dashed hairline, today ring kept #f5b03a; legend re-skinned; footer 'the wiring: home marks here drive the hometime chip on your driver card'. PYTHON SEAM LESSON: capturing an existing JSX block into a template dragged its own closing tags along — one duplicated </div> broke the parse; check the seam lines after any block transplant. Tests 535. Unmigrated remaining: compliance, recap, garage, score, settings.****MARKET PAGE SHIPPED (PR #407, v1.169.0 — recommendations→mockup→'change the color of the dot for hazmat… too close to standard' + 'month names underneath' the barometer + 'go ahead and build'): DATA STORY: monthly median gross/driven-mi Jan 7.85(2)→Feb 3.06/Mar 3.24/Apr 3.40 (UNDER the 4.34 floor all spring!)→May 5.09/Jun 5.50/Jul 5.05→Aug 7.45(2); 27 of 54 measurable loads below floor — 20/28 STANDARD FLATBED vs 4/16 oversize ('the specialized freight carries the rate; standard is mostly repositioning money' — sharpest business fact the page ever surfaced). NEW lib/constants/loadTypeColors.ts — SEMANTIC app-wide palette (his catch): standard #f5b03a / oversize #4f8cd6 / hazmat #8f7ad0 VIOLET (cat3, CVD-safe — was tan #c9a86a everywhere + amber #e0a020 on market) / heavy #8494ab + teal/rose/olive fallback; PlayerCard mix bars switched from POSITIONAL MIX_COLORS to loadTypeColor(label) (sweep rule applied); MarketPage COLOR map was standard-BLUE/hazmat-AMBER/specialized-CORAL → palette. Barometer: XAxis month ticks ALREADY existed via monthTick (his ask came from the bare mock SVG) — kept visible w/ MUTED→#5a6880; 'you' line #4ade80→#f5b03a; MACRO #7fb2e6→#4f8cd6; BE→#e05252; GRID→#141c2a. PAGE: full-bleed + VERDICT answering line (▲ BEATING/▼ LAGGING/▪ MOVING WITH stamp rotate-1deg in yvmColor + gapPts + last monthly median + sample n — ds-sfx stamp retired from barometer, gate includes ds-sfx); scatter/barometer+yvm/tier-gauge Panels→ds2 boards + wells; NEW 'ladder as a distribution' board (zones computed inline vs bookingLadder walkAway/minimum/target, banded-ladder zone colors, + straight-read footer w/ computed stdBelow/stdTotal + specBelow/specTotal). Tests 535. Unmigrated remaining: compliance, per-diem, recap, garage, score, settings.****BANDED LADDER PROMOTED APP-WIDE (PR #406, v1.168.2 — Jason's rule: 'If that kind of design… was supposed to be on any other page… and it's not, implement it now'): BandedLadder extracted from ExpensesPage → components/ui/BandedLadder.tsx (props ladder/rpm/label, presentation over getRateLadder, null-guarded). Consumers: ExpensesPage (imports shared) + RateTargetsCard (Money tab — old <RateLadder ladder rpm spec/> → two BandedLadder rows Standard/Oversize, matching the expenses card). DispatcherSeasonCard only imported the lib TYPE (not the component) — untouched. dashboard/RateLadder.tsx DELETED (gate on JSX/import forms — the lib type name RateLadder stays legit). NEW STANDING RULE FROM JASON: when a design pattern is approved for one surface, sweep the app for its siblings and unify — don't leave the old pattern standing elsewhere. Tests 535.****RATE-TO-BOOK LADDER TO SPEC (PR #405, v1.168.1 — Jason: 'The rate to book card does not look like the mock up' — SECOND mockups-are-contracts catch, same class as the player card): I had dropped the existing dashboard RateLadder component into the card instead of building the mockup's BANDED ladder. FIX: page-local BandedLadder (presentation-only over getRateLadder): 14 cells scaled walkAway*0.92 → max(strong*1.12, rpm), each cell tinted by the ZONE its value lands in (below walk-away red-tint / to minimum amber-tint / to target green-tint / strong+ bright amber gradient), YOU BOOK HERE · $X.XX marker line at the live grossPerTotalMile, legend row with all four real tier values; TWO rows — Standard flatbed + Oversize/specialized (specRateLadder) — honoring the mockup's 'oversize tiers ride the same card'. Name collision: lib type imported as RateLadder as RateLadderT. Dashboard RateLadder component untouched elsewhere (RateTargetsCard/DispatcherSeasonCard). Tests 535.****EXPENSES PAGE SHIPPED + THE BURN (PR #404, v1.168.0 — recommendations→mockup→'I love it. Also… what it would cost me to sit and what it cost me to run. Daily and weekly'→burn board added to mockup→'build it'): JULY BOOKS: $33,741 in · $19,257 out · $14,484 kept · 42.9% margin (his best posted month); fixed $11,347 / variable $7,911 from real lines; notes $1,960/mo. NEW lib/metrics/sitRun.ts (HIS metric): sitRunCosts(fixedMonthly, variableMonthly, notesMonthly) — SIT = fixed+notes (bleeds parked), RUN = sit+variable, daily ÷30.44 DAYS_PER_MONTH, weekly ÷4.345 WEEKS_PER_MONTH (run/week ≡ page weekly-cost by construction), roadDaily = run−sit ('a cheap load isn't always cheaper than sitting'); 4 tests, 535 total. Burn BASIS: trailing-window full periods fetched (Promise.all getExpensePeriod over the 3-period window ids — list rows lack lines) → getExpenseMetrics fixed/variable per period → mean → sitRunCosts. PAGE: full-bleed shell (month segs last-8 reversed in statusbar + UPLOAD P&L CTA), month sentence answering line (in/out/kept/margin + '— your best month on the books' computed max-profit flag), 5-KPI month board + deadhead-gap explainer, THE BURN board (sit blue #4f8cd6 / run amber, day-over-week stacks, road-delta explainer), RATE TO BOOK crown-jewel card (derivation stamps $cost/total-mi → ÷73% keep → WALK-AWAY in display-face amber; RateLadder component unchanged inside), ExpenseYtdChart tokened (income→#f5b03a cost→#4f8cd6, contrast lesson), fixed-vs-variable board (#378add→#4f8cd6), ObligationsCard forged (Panel→ds2, ALL lucide→text controls HIDE/SHOW·DRAW·EDIT·SAVE·✕·+ADD NOTE, tracked notes wear 10-cell computePayoff meters inline — same fn as dossiers), ExpenseLedger (lucide→text EDIT/SAVE/ADD/✕/⇄), ExpenseUpload in FORGED MODAL (Panel shell stripped, page provides overlay; REGEX LESSON: single-line icon regex ate a multiline Trash2's onClick → removeLine orphaned, restored by hand — multiline icons need exact-anchor reps, never lazy regex). NO formula touched (per-mile/ladder/blends intact). AppLayout += /expenses. Unmigrated remaining: compliance, market, per-diem, recap, garage, score, settings.****FUEL PAGE SHIPPED (PR #403, v1.167.0 — recommendations→mockup→'Good to build. However, fix the tank by tank chart… barely any contrast' + earlier 'I don't like the inline form… make it a pop up… maintain the Forge theme'): DATA AT SHIP: 28 fills · 3,326 gal · $14,468 · $4.35/gal · fueled TODAY Toms Brook VA (Pilot, 133.068 gal FULL) closing a tank 834mi/133.1gal = 6.3 MPG above avg; 17 Fuelly-import fills state='--' → 1,975 gal/$8,213 IFTA-unassigned. CONTRAST FIX (his call): MpgChart + DieselPriceChart lone amber lines #e8940a→#f5b03a w2→2.5, axes #9daabb→#5a6880, grid→#141c2a, national line→#4f8cd6 (chart-amber token #c87f0a is for MULTI-series; a lone line on dark panel needs amber-hi — lesson). PAGE: full-bleed shell + answering line (fills·gal·$·avg/gal·avgMPG·$/mi·states — 6-Kpi strip DELETED, stats relocated into the line); LatestTankCard → forged THE LAST TANK card (sfx record cue + stampSlam NEW BEST kept, ABOVE YOUR AVERAGE chip added, lucide Trending/Trophy/Flame → ▲▼ text, 52px display MPG) — GLYPH LESSON: bare ▲ can't replace a JSX element inside ( ) ternaries — collapse to string ternary; DieselCompareCard/FuelVsRevenueCard → ds2 + forge stamps (UNDER THE NATIONAL / SURCHARGE COVERS FUEL / SURCHARGE SHORT). NEW by-state IFTA board (inline groupby, '--'→unassigned ghost burn-down line). LOG: ledger rows w/ FULL amber / PARTIAL dashed chips (120-gal rule visible), CLOSED THE TANK · X MPG stamp on closing fulls, row onClick openEdit, ✕ stopPropagation. POPUP (his order): one forged modal for CREATE+EDIT (forge head LOG/EDIT FILL-UP, fields block moved VERBATIM w/ all validation/receipt-math/$10 guard/vendor-state memory; derived line adds FULL/PARTIAL forecast); EDIT = frontend-only — backend PATCH /fuel-entries/:id ALREADY EXISTED (patchFuelEntry line 122; checked before building — no backend work); new updateFuelEntry service. Route is /fuel-entries (not /fuel) — AppLayout += /fuel-entries. Tests 531. Unmigrated remaining: compliance, expenses, market, per-diem, recap, garage, score, settings.****MAINTENANCE PAGE SHIPPED (PR #401, v1.166.0 — recommendations→mockup→'Perfect! Build it'): full-bleed shell w/ Schedule|Services segs IN statusbar + '+ LOG SERVICE' CTA (setTab(services) + serviceSignal counter → ServicesTab openSignal prop useEffect opens form); ANSWERING LINE at page level (counts via computeDue over items + $ytdSpend + 'tractor at N · hub at N') — chips OVERDUE red/CLOSE amber/RUNNING green/NO BASELINE dashed. LIVE STATE AT SHIP: chassis lube 119 mi (told him: a fuel stop away), brakes/seals 2,951, oil+fuel 4,605-or-Sep-2, wash Aug 28, 9 no-baseline. SCHEDULE TAB: HealthGauge.tsx DELETED → ui/GaugeDial (one gauge language w/ Fleet speedometers) + fleetHealth score + chips + Board|Table seg + ADD CLOCK; sections KEPT Truck/Transmission/Trailer (mockup showed 2 — data-true 3, stated); known rows = ClockCells meter grammar (14 cells, AT_THE_DOOR=0.97 presentation cue: fill≥97% renders hot even when level=soon — the 119-mi case; comment says level still from computeDue); unknown rows collapse per-section into ghost row w/ names as dotted-underline buttons → edit form ('set a baseline by hand'); table view kept + tokened; seed-starter empty state kept. SERVICES TAB: lucide out; VendorCell = rolodex link via useVendors LOWER(TRIM(name)) match (same rule as spend bridge) — filed → Link /vendors/:id amber, unfiled → dim + 'file it →' to /vendors; ledger = srow grid (date/desc+✓item-chips/vendor+location/reading+unit/cost/✕); By-vendor board (visits+total, AVG COLUMN DROPPED per mockup — stated); range presets → segs + custom month selects kept; justLogged Stamp → 'SERVICE LOGGED ✓' forge chip ('the clocks it covered just reset'). Forms: lucide Check/X out, tokens in, structure untouched. AppLayout += /maintenance. Tests 531. SegmentedTabs component still used elsewhere (kept).****TRAILERS PAIR SHIPPED + COMIC FLEET COMPONENTS RETIRED (PR #400, v1.165.0 — recommendations→mockup→'build'): DATA: Unit 780991 2021 Fontaine TL 53' flatbed (NOT 48' — data beat memory), plate PH21027 IN, in service 2026-02-03; 51 hauls carried, 47,937 mi carried, ~$13.9k its-share (build uses exact loadTrailerNet), Trailer Payment $230/mo $38,071/$39,408 = 3.4% owned (the long road), trailer clocks: lube 2-mo interval due ~Sep 2, brakes/ABS 568,737+25K hub scale = 3,006 mi out. ROSTER (TrailersPage rewrite): fleet-roster grammar, type chip '2021 FONTAINE TL · 53′ flatbed', service chips, line 'mi carried · hauls · its share $k · note N% paid', mile plate OR dashed ghost '100K AT 52% OUT' (m.crossed null); create form kept (hub field relabeled 'seed — services keep it fresh'); trailerFleetSummary comparison dropped like trucks'. DETAIL (flatbed dossier): hero = metrics.totalMiles 'miles carried · its own miles, not the tractor's' (hub stays the CLOCK scale only); card head = m.title ?? THE FLATBED + ghost chip 'EARNING ITS KEEP' until 100K; strip = revenue (earned loadTrailerNet) / deliveredLoads count / totalMiles; meters = PAYOFF ('the long road', paidPct 1 decimal, sub 'the TRAILER PAID OFF trophy waits at the end of this road' — ties trophy engine) / NEXT SERVICE (trailer-unit items vs HUB via clocks memo, date-based fallback 'due {date}' for the lube) / MILE CLUB (totalMiles toward first plate, 'every mile it carries counts — the 100K bronze punches first'). HardwareBoard(allTrailerMedals full, patches, recordChips: BEST PAYDAY/LONGEST HAUL/HEAVIEST LOAD/BIG MONTH). NUMBERS board = TrailerMetrics 4-up (utilization/earnings-mi/cost-to-run-mi 'no fuel in a trailer's basis'/mi-month). Recent hauls: money(loadTrailerNet(l)) + 'share' per row. RETIREMENTS: MilestoneBurst.tsx, MileClub.tsx, PayoffTracker.tsx, PatchBoard.tsx DELETED; RecordBook.tsx trimmed to lib (RecordChip type + driverRecordChips survive; component gone); STRICT gate = grep -E '<(Component)|from path' (plain-token gate tripped on prose comments — lesson: gate on JSX/import forms). AppLayout += /trailers. Tests 531. FLEET DOSSIER SET NOW COMPLETE: driver/dispatcher/truck/trailer all card grammar. Unmigrated remaining: maintenance, fuel, compliance, expenses, market, per-diem, recap, garage, score, settings.****TRUCKS PAIR SHIPPED (PR #399, v1.164.0 — recommendations→mockup (SendUserFile'd)→'build the trucks pair'): DATA: Unit 580991 2021 International LT625, 590,731mi (derived), 53 hauls, ~6.1 MPG, Truck Note $1,575/mo $42,525/$69,000 = 38% owned, CHASSIS LUBE DUE IN 174 MI + brakes 3,006 (2 clocks close — told Jason to grab the lube within the next tank). ROSTER (TrucksPage rewrite): rows = avatar, UNIT + year/make/model chip + STATUS_CHIP map (active/maintenance/out_of_service/inactive) + MAINTENANCE CHIP ON THE ROW (N OVERDUE red / N SERVICES CLOSE amber — computeDue per tractor item vs derived odo + recentMilesPerMonth) + subline 'odo mi · hauls · MPG avg (fuelStats) · note N% paid (computePayoff)' + gold mile plate; create form kept (odometer field relabeled 'seed — the app derives the live one'); DELIBERATE CUT: fleetSummary >1-truck comparison table dropped (never rendered at n=1; rebuild if fleet grows — flagged in report). DETAIL (rig dossier): dhead = avatar 84 + UNIT + chips + medals worn + ODOMETER HERO (RollingNumber in well, 'one constant'); RIG CARD: forge head = mileMilestone.title ('HALF-MILLION RIG') + gold {label} CLUB plate chip; strip = earned full-net loadRevenue / hauls delivered / avg MPG; meters = THE PAYOFF (computePayoff: paidPct fill, owed-to-go, original + $/mo + payoffDate exact-vs-pace, ghost when untracked) / NEXT SERVICE (clocks sorted by d.progress desc, top = meter (red cells when level!=ok) + right 'name · N mi out/over', sub = 2nd clock + closeCount + Maintenance→ link, ghost when no baselines; ITEMS unit type is tractor|trailer ONLY — no 'both' (that's SERVICES) — TS2367 caught it) / MILE CLUB (m.pct, next plate, platinum-strike line). HardwareBoard(allTruckMedals full + patches + recordChips incl BEST REV/MI $16.77, LONGEST HAUL 1,791). THE NUMBERS board = old 7-KPI strip + day-split relocated deliberately (utilization/avgMpg/bestTank/fuel-mi/rev-mi/cost-to-run/mi-month). Papers + recent hauls linked. AppLayout += /trucks. Old due-counts memo deleted, Kpi helper deleted, MileClub/PayoffTracker/RecordBook/PatchBoard usage OFF this page (components live for trailers slice — LAST users after trailers: none for MilestoneBurst+MileClub → retire then). Tests 531.****DISPATCHER CARD SHIPPED (PR #398, v1.163.0 — recommendations→mockup ('i dont see it' — PostToolUse browser-pane hook is NOT proof he sees a file; ALWAYS SendUserFile mockups)→'build it'): Brandie's dossier in the card grammar. DATA: she's booked exactly 1 load (1467215 Bellefonte PA→Littleton WV, $1,950 gross/210mi = $9.29/mi vs $4.34 floor = +$4.95 OVER — overdrive on day one), rank Rookie Dispatcher 1/25. LIB: getDispatcherCard += onTimeCount/gradedStops (ungated, same fix as facility scorecard). DispatcherCard.tsx REBUILT to card grammar (props → {card} only): stencil rank head + stars + SEASON·grade chip; strip = loads booked / gross booked / detention collected (fmtHours); meters = BOOKING OVER THE FLOOR (avgBookedRate vs breakEven notch@0.78 'FLOOR', overdrive white, sub 'margin she found', ghost when no cost basis) / CAREER LADDER (count→next.min cells + full 5-rung strip in-card: Rookie 0→Wrangler 25→Freight Closer 75→Rate Hawk 200→Dispatch Legend 500) / ON-TIME (count-of-graded, ghost until her loads run). PAGE: full-bleed dossier (statusbar DISPATCHER + ←back(admin?settings:dashboard), dhead avatar-64 + rank chip + DTS·DISPATCH chip + earned MedalBadges worn), card, HardwareBoard, DispatcherSeasonCard (tokened; lucide Trophy/Lock → font-forge WON/— + ★ strip — emoji rejected as off-vocabulary). HardwareBoard GENERALIZED: patches?/records? now optional (sections hide), NEW grindTags?: GrindPatch[] (dispatcher grind: earned amber 'NAME · badge — next: X' / ghost dashed, EACH with its own 8-cell MeterCells progress) — driver page unaffected. DELETED: DispatcherPatchBoard.tsx + in-page LockedMedal (ghost slots cover). Her medals all ghost (Steal/Double-Up/Whale/Superload/Perfect Week/Grand Slam/Big Week), Deal Closer tag earned ×1. FULL_BLEED += /dispatcher (route /dispatcher/:id). Tests 531.****PLAYER CARD TO SPEC + MONTH-COVERAGE METRIC (PR #397, v1.162.1 — Jason caught the card not matching the mockup ('don't cut corners' — feedback memory mockups-are-contracts written) + mid-build metric correction: 'one load will cover my notes… what I really need is covering my monthly expense threshold and whether I have margin over it… affects both the money tab and the player card'): (1) PlayerCard REBUILT to mockup structure (props fully changed — sole consumer is DriverDetailPage): forge-stencil head (rank name + stars + SEASON chip), 3-stat career strip (hauls/miles/linehaul from delivered), meter rows = COVERING THE MONTH / QUARTER PACE (notch@0.78=prevFinalNet, overdrive white cells past, verdict right-label) / MILE CLUB (rank.pct, 'miles / next.min') / LOAD MIX bars (full type mix + specialist TypeStrips); Cells primitive w/ notch+overdrive. Identity lives in dhead (avatar/name/chips + MedalBadge coins worn ml-auto). NEW LeversBoard.tsx (under HardwareBoard): 3 profit levers + bottleneck coach + QuarterPaceCard (tokened, lucide out — Jason hand-touched this file during build) + LAST SEASON 8-stat grid — old card content preserved, relocated deliberately. (2) NEW lib/metrics/monthCoverage.ts — THE month metric, one computation both surfaces: income (posted P&L income_total for current UTC month, else MTD sum of loadRevenue over delivered — computed IN-LIB with injected clock because dashboard's getRevenueMTD reads Date.now() internally = untestable) races threshold = getCostBasis.trueMonthlyCost (MEAN of last 3 complete P&L months: cogs+expenses+notes — his requested mean basis); opEx marker first, notes = the small final slice, past threshold = marginOver; short + straight-line coverDay; monthsInMean=0 → ghost state. 5 tests (clock-injected, no fake timers), 531 total. (3) MoneyTab: THE NOTES plate → COVERING THE MONTH: headline = income (was profit), meter markers [expenses@opEx, month@threshold] target=threshold, sub-line covered/short + notes-inside language, MONTH COVERED stamp (was NOTES COVERED), estimated footnote now MTD-income-based (medianMargin machinery deleted), else-branch = threshold-forges-from-P&L copy; goalStreak untouched. loadRevenue = linehaul+FSC+accessorials (NaN if total_accessorials missing from fixtures!).****DRIVERS PAIR SHIPPED (PR #396, v1.162.0 — recommendations→mockup→'I like it, especially the player card, but it's missing the achievements'→hardware section added to mockup→'build'): ROSTER (DriversPage rewrite): full-bleed, rows w/ avatar, ACTIVE chip, 'N hauls · N mi hauled · CDL thru date · endorsements', forged mile-club plate ('25K CLUB' die-stamp) REPLACING comic MilestoneBurst (component NOT deleted — TrucksPage/TrailersPage/MileClub still use it; retire at their slices), tel: CALL btn, row onClick nav. DOSSIER (DriverDetailPage): statusbar + dhead (EntityAvatar size 84, ACTIVE + HOMETIME chips — over=red 'OUT N DAYS · PAST YOUR X-DAY LINE' (his real state: out 28 vs 21!), ok=quiet, home=green; hometime NO LONGER passed to PlayerCard — dhead owns it, card strip hidden via optional prop). PLAYERCARD FORGED (restyle only, ALL logic/props kept + NEW streak?: prop): lucide OUT (Truck/Gauge/Home/Maximize2/Weight), halftone corner OUT, amber border-2 → hairline + head glow gradient, name → Bebas caps, rank block = font-forge caps + stars + 10-CELL meter (was thin bar) labeled 'Career rank — the mile club', NEW 'COVERING THE NOTES — WEEK STREAK' 10-cell meter row (grind.currentStreak via card.streak), Stat/LeverTile/TypeStrip/HometimeChip/bottleneck → wells+tokens (grade colors green/red/amber kept). NEW components/awards/HardwareBoard.tsx (driver page replaces RecordBook+PatchBoard render — components KEPT for truck/trailer/dispatcher pages): Coins = full computeMedals list via card.allMedals (earned → forge/Coin metal by tier + progress MeterCells toward next; tier 0 → dashed ghost circle + meter + hint), Tags = patches (earned amber/blue-operational '✓/×N', ghost dashed w/ hint inline), Record plates = driverRecordChips as rotated die-stamps 'LABEL · value', FORGE ROOM → link. Papers plate: specs grid + CDL CLOCK (local en-CA day math; ≤60 days red chip, else quiet '169 days'; EXPIRED state), EDIT → EntityForm inline, compliance link. Recent hauls: linked ledger rows (load_number → /loads/:id — drill-down rule fixed). Non-hauling fallback → ds2 3-cell strip. AppLayout FULL_BLEED += /drivers. Tests 526. PYTHON-EDIT LESSON: assert-all-reps script died mid-list on an indent-mismatch anchor → file untouched (writes at end) — re-ran with fixed anchor; check indentation against Read output exactly.****LANES PAGE COMPLETED + MAP CALMED (PR #395, v1.161.0 — Jason's correction: only the MAP was ever migrated; page needed standard protocol; also map 'too zoomed in… elevation way too elevated… too busy'): recommendations→mockup (SVG stand-in using the board's REAL geoAlbersUsa projection from us-atlas via node script)→nodded→built. MAP (LanesMap3D): camera [0,13.2,11.6]→[0,15.5,13.8] (+18% back), MAX_H 1.7→0.85 + sqrt height scale, STATE STAMPS (lit states, level==state only, flat drei Text at centroid via getStateAbbr — THE legibility fix), bloom 0.72/0.62/0.72→0.45/0.72/0.6, arc emissive 2.1→1.35, dot 3.4→2.2 r0.085→0.07, ring 2.6→1.7, pulse 0.004/1.12→0.0025/1.45 (~7.5s+rest), parallax 0.055/0.045→0.028/0.022, hover flare 0.45→0.3 sel 0.65→0.5 lerp 0.12, BOOT plays once (booted ref) — filter changes settle 0.3/0.35s. PAGE: LanesKpis.tsx DELETED (3 lucide KPI cards died — 'top lane' at n=1-2 was noise; dashboard LanesTab keeps using lib getLanesSummary, unaffected); statusbar answering line '23 LOADS · $101,954 linehaul · $7.08/mi blended · click a state to drill' via NEW getWindowTotals(); NEW OriginMarkets board via NEW getOriginStateRollup() (repeats ≥2 only, volume-then-rate, BEST ORIGIN die-stamp = best blended rate among repeats — his: SC $12.33/3; singles ride 'the map, not the ranking' dim line); lanes.ts edited via python (STRAY BYTE — always grep -a / surrogateescape); +4 lib tests = 526. LanesTable: lucide chevrons→text ▾/▸, hex→var(--color-faint). StateDetailPanel: X→✕. HIS 90d DATA: 23 loads $101,954 $7.08 blended; origins AL 4/$6.22, SC 3/$12.33, GA 2/$11.37, OH 2, PA 2, TN 2, +7 singles; lanes GA→TX ×2 $18.5k, SC→VA ×2 $14.92/mi, rest one-offs (spot oversize = origins recur, lanes don't — the page's honesty premise).****ODOMETER ONE-CONSTANT + DEADHEAD GATE + ON-TIME COUNT (PR #394, v1.160.1 — Jason's 3-symptom report, one issue by his order): (1) ODOMETER SPLIT-BRAIN: trucks.current_odometer stored column (was 582,596, ~8,100mi stale — only the truck form ever wrote it); TrucksPage + FleetTab rendered raw column (stale), TruckDetail computed max (fresh). FIX = backend truckServices getTrucks+getTruck now SELECT GREATEST(t.current_odometer, laterals: loads.odometer_end, trips.odometer_end, fuel_entries.odometer_reading, maintenance_services.odometer WHERE unit IN tractor/both) AS current_odometer (ODOMETER_SOURCES_SQL + CURRENT_ODOMETER_SQL consts; Postgres GREATEST ignores NULLs) — column is now seed/floor only; every API consumer fresh with zero frontend changes. VERIFIED prod: derived 590,731 (loads 590,731 > fuel 589,952 > trips 587,834 > services 580,391), dev shape clean. (2) hasOdometerWindow DROPPED load_status==='delivered' gate (both-readings only — 'miles are physics, not paperwork'); his in_transit 7059883 (589,624→590,731, loaded 475) now shows actual deadhead 632mi vs planned 515; RIPPLE: dashboard.ts deadheadLoad now explicitly excludes cancelled+tonu (preserves old product exclusion — flagged to Jason that TONU real empty miles stay out of the monthly trend, his call later). (3) FACILITY ON-TIME '0 of 2' BUG (MY facilities-pair bug): cell computed round(onTimePct*gradedStops) but onTimePct gates null under MIN_STOPS → 0-of-2 while he beat both appts. FIX: StopScore.onTimeCount raw ungated count (waited counts as on-time, unchanged), cell renders it. Tests 522 (deadhead gate test reworked w/ his real 632mi numbers; stopScore ACS regression test).****FACILITIES PAIR SHIPPED (PR #393, v1.160.0 — 'build the facilities pair'): stops-ledger rework per approved mockup, both pages full-bleed Night Cab. HIS DATA: 76 facilities, 8 repeats, 12 timed stops; A C S Manufacturing (Abbeville SC) median dwell 10h52m over 2 stops (in 6a out ~5p, beat 7a appt both times — detention material), Yes Milner 83m/360m, Red Oak 660m. NEW lib/metrics/facilityLedger.ts: facilityTimes() (median dwell + timed count + lastVisit, receiver falls back to pickup_date), timedStopCount() (facility-linked stops only), SLOW_DWELL_MIN=360 (presentation cue, tints #f0a35e) — 6 tests, 521 total. NEW components/facilities/KindChip (BUSINESS green #6fd08c / JOB SITE blue #9db2d8). LIST: answering line 'N FACILITIES · N repeat stops · N timed stops logged' + search well; ledger board rows sorted visits desc → lastVisit desc → name, 1-visit rows opacity-55; dwell chip w/ sample size ('median · N timed' or '1 timed stop') vs ghost 'no timed stops yet'; dupes board re-skinned amber-edged (mechanics 1:1: radio keeper, KEEPER chip, confirm modal canvas-skin). DETAIL (dock card): scorecard plate = 3 well-cells (median dwell / on-time 'N of N you-not-them' via round(onTimePct*gradedStops) / ran-past-free + unpaid red) shown from FIRST timed stop; ghost meter 'forges at MIN_STOPS(3)' w/ amber/dashed cells when 0<timed<3; full ghost when 0. Stop history rows: load# LINKED /loads/:id, sentence 'in 6:00a → out 5:00p · beat/made/missed the Xa appt' (waited/on-time green, late red via APPT_CLAUSE map). NOTES now editable on detail (schema had it, UI never showed): textarea well + SAVE on dirty via patchFacility {notes: trim||null} — backend verified accepts null (optionalString + allowedFields). Housekeeping plate: merge-into select + window.confirm ADDED (old detail merged with NO confirm — fixed). StopScorecard component NOT deleted (AgentDetailPage still uses it, old skin — future slice). AppLayout FULL_BLEED += /facilities. Unmigrated remaining: trucks/trailers/drivers detail, maintenance, fuel, compliance, expenses, market, per-diem, recap, garage, score, settings, dispatcher.****VENDORS PAIR SHIPPED (PR #392, v1.159.0 — 'build the vendors pair'): both pages full-bleed Night Cab per approved mockup. NEW components/vendors/VendorPips.tsx (5 punched pips: amber earned / red ≤2 / ghost dashed unrated, caption 'N of 5'/'not yet rated', align prop — also replaces the medallion demo in GuidePage rating section) + VendorTrustTag.tsx (vendorTrust(): 5=GO-TO amber, ≤2=STEER CLEAR red, null=UNPROVEN ghost, 3-4=no tag). vendorLeaderboard.ts: goToCount DELETED → trustCounts {goTo,steerClear,unproven}; + serviceAreaStates() (splits non-letters, validates against constants/states.ts STATES keys so 'to'/'and' can't chip, dedupes) — tests 515. LIST: trust-ledger answering line + search well (CATEGORY PILLS DROPPED — Guide wording updated, crown→GO-TO stamp language); unfiled board re-skinned; category ds2-boards with rotated GO-TO die-stamp header (champion), rows = div onClick nav (NOT Link — tel: anchor inside would nest <a>), hot left edge ≤2, notes first line italic, state chips, spend inline, CallButton tel: (strips non-digits, stopPropagation). DETAIL: statusbar + Bebas name dhead + pips; Contact plate w/ big CALL/EDIT; notes well (ghost when empty); spend plate (3 stats, shop hint when isShop&&!hasSpend); TEMPERING LOG plate (history rows old→new·date·initials·'reason'; ghost empty copy; RE-RATE—SAY WHY amber btn); DELETE VENDOR text btn bottom (confirm flow kept). DELETED: VendorRatingMedallion.tsx, VendorRatingStamp.tsx (hard-gate grep clean incl. goToCount). AppLayout FULL_BLEED += /vendors. Lucide fully out of both pages. Unmigrated pages remaining: facilities, trucks/trailers/drivers detail, maintenance, fuel, compliance, expenses, market, per-diem, recap, garage, score, settings, dispatcher.****VENDOR-MAINTENANCE BRIDGE (PR #391, v1.158.0 — Jason: 'I did add shops but they didnt count them from my maintenance page, find the problem and ship'): NOT a matching bug — prod vendors table had ZERO Shop rows; his six shops (Cummins $2,312.82, Thermo King $1,023.53, Ray's Tire Service $924.23, TA ×2 $865.37, Ray's Truck Express Lube $825.42, Clean Shot Truck Wash $160 — $6,111 total) live only in maintenance_services.vendor free text; nothing bridged the two stores. SHIPPED: (1) GET /vendors/unfiled (registered BEFORE /:vendor_id — Express order!) = maintenance vendor names with no vendor row, grouped LOWER(TRIM()) so spellings can't split, spend-desc; (2) spend gate broadened from category='Shop' to MAINTENANCE_CATEGORIES [Shop,Tires,Parts,Towing,Washout] (exported const, SQL IN-list) — frontend spendLabel + detail hasSpend de-gated to service_count>0 (isShop keeps only the no-match hint); (3) VendorsPage 'From your maintenance log' Panel (old skin — reskin still awaiting nod) with Add-to-rolodex → VendorForm new prefill prop {name,category:'Shop'} seeding create mode with the log's EXACT spelling (match requirement); New-vendor button clears prefill. SQL verified dev (shape) + prod (six rows exact). VENDORS RESKIN MOCKUP delivered & awaiting nod (scratchpad vendors-night-cab.html): rolodex-with-memory — trust-ledger answering line (go-to/steer-clear/unproven), tel: CALL buttons on rows, notes first-line on list, service-area state chips, GO-TO die-stamp, punched pips, STEER CLEAR hot edge, ghost unproven, tempering log using vendor_rating_history.reason (table empty, column unused today). His real vendors: West Coast Permits (5, Laura), TP Pilot Services (2, Karen, the warning note), Bradley's Pilot Car (unrated, Bradley Slade).****TRIPS BOARD YEAR-LEDGER REWORK (PR #390, v1.157.2 — Jason: 'the KPIs arent tracking now'): NOT a regression — deploys green (Vercel+Railway auto-deploy from main, verified via commit statuses), prod data tiles perfectly post-anchor; the S11 board was hard-keyed to CURRENT CALENDAR MONTH (unlabeled), so August-with-no-trips rendered four dead zeros. REWORK (mocked, nodded, built): board = YTD by purpose with header '2026 · TRIP MILES BY PURPOSE' + right-corner '{Mon} so far · X mi'; month line appears inside a cell only when that purpose has miles this month (amber value); zero-YTD cells render faint. New lib/metrics/tripTotals.ts (pure, clock-injected — no fake timers needed) + 5 tests incl. local-vs-UTC month-roll test (old code used toISOString = UTC roll bug). Tests 511. PROD FACTS LEARNED: frontend Vercel + backend Railway BOTH auto-deploy from main (merge = deploy!); Supabase prod=zeeglmaqitxjqzxfikwy dev=vbiboblkyavhmnegjbrz via MCP; backend/.env DATABASE_URL → PROD (never verify through local backend); trips schema has NO miles column (derived), purpose col = trip_purpose; his trips start Jul 6 2026 @ odo 581,919 (trip #1 deleted, numbering starts at 2). Repo also contains AGENT_HANDOFF.md — the older v1.84-era build-agent's handoff (context only).****GAP-DETECTOR ANCHOR FIX (PR #389, v1.157.1 — Jason's prod report: 9 gap callouts Jan–Jun, 'all accounted for, odometer correct — bug?'): NOT a bug — every gap PREDATED his first logged trip (trips feature shipped ~July); the detector was auditing pre-ledger history. FIX (zero-setting): audit anchor = min(trip.odometer_start) — gaps only surface where maxEnd >= anchor; no trips → no ledger → no audit; backfilling older trips honestly moves the anchor back. Test count 506 (8 gap tests: one reworked — a hole ENDING at the first trip is pre-history and stays quiet, that's the fix itself; +2 anchor tests). DOMAIN NOTE: Jason's Jan–Jun miles were 'accounted' under the pre-trips deadhead-only system; no backfill intended.****S11 SHIPPED (PR #388, v1.157.0, closes #387): Trips built per mockup. RECON WIN: backend PATCH /:id + DELETE /:id routes AND patchTrip/deleteTrip services ALREADY EXISTED (Jason's past self, again — recon before scoping); edit scope collapsed to frontend-only: tripsService updateTrip/deleteTrip (house axios-error pattern), TripForm edit mode (trip?/prefillOdometerStart? props, state seeded from trip, BOTH prefill effects guarded so they never clobber an edit or gap handoff, patch-vs-create submit, confirm-first Delete button w/ deleting state). NEW lib/metrics/odometerGaps.ts + 6 clock-free tests (windows from loads+trips both-odos-only, coerced, sorted, OVERLAP-COALESCED via maxEnd before measuring — inside-window trips can't fake gaps; GAP_THRESHOLD_MI=25 skips yard noise). Page: statusbar+CTA, month-by-purpose strip (plain words+tone dots), answering line + gaps chip, purpose segment filters, ledger w/ Miles column + condensed odometer + row→edit affordance, GAP CALLOUTS INTERLEAVED by odometer sort-key (desc by window start; gap sorts at its toOdo → sits beneath the row where coverage resumes) w/ LOG THIS GAP → prefilled create; gaps render in All view only. /trips full-bleed. 504/504 (498+6), build green.****TRIPS PAGE (next slice, mockup delivered 2026-08-09, AWAITING Jason's nod → 'build s11'): evaluation found the app's most primitive page (raw 6-col table, raw enum purposes, NO miles column, no filters, create-only form). Mockup https://claude.ai/code/artifact/d8bbc3d5-feaf-4b3e-b58e-2ce40669f0bd — statusbar+LOG TRIP CTA; month-by-purpose strip (plain words + tone chips: Repositioning amber/Home blue/Shop tan/Personal dim); answering line + gaps chip; purpose-filter segments; ledger w/ Miles column + condensed odometer col; THE GAP DETECTOR (novel: sort all load+trip odometer windows, surface end→start holes as inline dashed callouts '1,240 mi unaccounted · Jul 12→15' + LOG THIS GAP button prefilling the form's start odometer — the integrity gauge for the whole odometer-true system); EDIT SCOPED (Jason ordered): row-click → form edit mode + Delete (confirm-first) — build needs backend PUT route (delete exists server-side, GET/:id exists; NO update route), client updateTrip/deleteTrip service fns, TripForm initial-trip/mode props (odometer-prefill behavior preserved). Quiet game (ledger page). Mockup demo data note: answering-line count implies more rows than shown — real page sums real rows.****TROPHY CASE + STARBURST RETIRED FOR REAL (PR #386, v1.156.1 — Jason caught my false 'fully migrated' claim): TrophyCase rebuilt on the forge (ds2-board shell — the comic amber border + HALFTONE corner deleted; header stencil). ICON DECISION (Jason's open question, answered + shipped): one hardware vocabulary — quarter WINS = struck Coins, board APPEARANCES = punched Pips (the tag language: appearances stack), PROVISIONAL live standings = ghost slots ('struck when the quarter closes'), empty quarters = ghost slots; all lucide Trophy/Star/Minus pictograms gone from the case. PrestigeBurst had a THIRD live consumer my gate's file-scope missed (TopAgents on both boards — tsc caught a fourth in GuidePage): both now wear Coins; PrestigeBadge.tsx collapsed to a META-only module (burst + badge components deleted); Guide's career-rank section updated per the Guide rule (coin hardware + copy: 'the struck coin… Bronze → Silver → Gold → Platinum, the foil-finish coin'). Repo-wide gate: zero PrestigeBurst consumers. LESSON REINFORCED: gates must be REPO-WIDE greps, never file-scoped lists; and never claim 'fully migrated' without grepping the page's imported components too.****S10 CLOSED (PR #385, v1.156.0, closes #368): discovery — most of the pair had ALREADY landed during a summarized stretch (list page statusbar/answering-line/chips, detail header/full-bleed/delta-KPI/best-load plate, TrophyCase coins all on main). The true remainder shipped now: prestige-as-TEXT → the metal Coin on BOTH detail header ('[coin] Gold agent') and AgentCard corner (burst PrestigeBadge component now unused by both — candidate for deletion when nothing else imports it; grep before pruning); AgentCard shell ds-panel/border-[#3b4660] → ds2-board + ds2-cell-rule (Jason's card-view complaint); detail skeleton wrappers bg-iron dropped; loads-table rows → ds2-cell-rule. Hard-gated at zero old-skin classes on both files. AGENT PAGES FULLY MIGRATED.****S13 AFTERMATH + PROCESS LESSON (2026-08-09): the straggler sweep missed FOUR successive rounds of files (TopAgents → RecordBook/PatchBoard → 7 more incl. PayoffTracker/detail pages/DispatcherPage/TrophyStudioPage/GuidePage) because every verify grep was piped through `head -2` — I only ever SAW two files per round, and the ship chains ran anyway on green builds (unknown classes don't fail builds, they silently unstyle). FIXED in v1.155.1 + v1.155.2 (final batch, 25 uses total swept). RULE GOING FORWARD: repo-wide sweeps end with a HARD GATE — `[ $(grep -rl PATTERN src | wc -l) = 0 ] &&` chained BEFORE the ship commands, never a printed grep the eye has to catch; and never truncate a verify listing.****S13 SHIPPED (PR #382, v1.155.0) — THE ARC CLOSES: Bangers OUT of index.html (bundle = Bebas/BSS/BarlowCond/Barlow); --font-comic token deleted; every font-comic use swept incl. late-found stragglers (QuarterPaceCard, RubberStamp, DispatchLoadsTable, DispatchAgentsTable — my straggler list had missed the dispatch tables; grep-verify beats lists); .ds-sfx retargeted to var(--font-forge) (MarketPage's verdict word survives); comic/ dir → celebrations/ (imports swept); DELETED: WhatsNext.tsx, MiniTrophyCard.tsx, ComicBurst.tsx, 4 dead award-* keyframes; KEPT deliberately: RubberStamp.tsx (live loadStamp mapping), RollingNumber (KpiCard/TruckDetail), v1 tokens + FULL_BLEED list (unmigrated pages still consume: vendors/facilities/trucks/trailers/drivers/maintenance/fuel/compliance/expenses/market/per-diem/recap/garage/score/settings + S10-parked agent pages — each migrates under the per-page protocol when Jason points at it; final token prune happens when the last one lands).****G4 SHIPPED (PR #378, v1.154.0): (1) STUDIO RE-BRIEFED — lib/trophies/style.ts CORE/TROPHY_STYLE/HALL_PROMPT rewritten from comic/cel-shade to the forge world (machined steel, amber inlay, wordless, no-comic negatives); governs NEXT generations only — REGENERATING the existing hall art is JASON'S call in the Studio (~cents/image), flag it whenever he visits the topic. (2) THE 3D COIN — forge/Coin3D.tsx (WebGL gate + 2D Coin fallback) + CoinScene.tsx (R3F: cylinder+torus reeded edge, MeshPhysicalMaterial w/ REAL IRIDESCENCE for platinum, 3 plain lights NO HDRI fetches, drei Text numeral, slow turn); wired into MedalAward — the strike now lands a turning machined coin; CoinScene chunk = 1.47kB (three dedupes into the Lanes vendor chunk). (3) Forge Room atmosphere: fixed CSS spotlight cones. ARC REMAINING: S13 CLEANUP ONLY — Bangers out of index.html font link, v1 tokens pruned from @theme + .dark, ds-* dead styles, comic/ dir → forge/ rename + comic-font stragglers (RecapPoster/VendorRatingStamp/LatestTankCard/TrophyCase/MiniTrophyCard/RigCard), FULL_BLEED_PREFIXES list deletion (all pages migrated by then?? — NO: several pages still unmigrated [vendors/facilities/trucks/trailers/drivers/maintenance/fuel/compliance/expenses/market/per-diem/recap/garage/score/settings + detail pages from parked S10]; S13 prunes what's SAFE and keeps the list for unmigrated routes — cleanup ≠ finish-line for page migration, tell Jason honestly).****G3 SHIPPED (PR #376, v1.153.0): TrophyCeremony → MONUMENT UNVEIL (pedestal rises w/ settle ease, headlight beam sweeps the art, verdict engraves, 'SEE IT IN THE FORGE ROOM'); RecapCeremony → SEASON PLAQUES (bronze month/silver quarter/gold year slabs, slam-in, engraved kicker: MONTH IN THE BOOKS / QUARTER COMPLETE / THE YEAR, STRUCK; truck avatar rides the medallion; confetti deleted from both); MedalBadge → struck Coin (tier metal incl platinum) — BOTH cards re-hardwared through the one shared component; playercard/DispatcherCard font-comic → forge (6 sites). Remaining comic-font sites for S13: RecapPoster, VendorRatingStamp, LatestTankCard, TrophyCase, MiniTrophyCard, RigCard. NEXT: G4 (hall art re-brief in Studio [Jason-driven, ~cents/image] + R3F hall treatment) then S13 cleanup (Bangers out of index.html bundle, v1 tokens, comic dir rename, FULL_BLEED list deletion, unused WhatsNext etc).****G1b SHIPPED (PR #372 → `2fc5f02`, v1.151.0): Forge Room live — coins/tags/plates racks over the hall (engines' tier-0/count-0/progress/hint finally surfaced), platinum foil minted into Coin.tsx, hall art kept, font-comic off the hall, full-bleed. G2 SHIPPED (PR #374, v1.152.0): (a) TRUCK + TRAILER RACKS — racks extracted to components/forge/Racks.tsx (CoinRack/TagRack/PlateRack/RackHeader), truck section (per trucks[0]: computeTruckMedals w/ fuelStats avgMpg, utilization null-omitted; patches; records plates) + trailer section (computeTrailerMedals w/ loadTrailerNet — RE-EXPORTED from lib/metrics/rateTargets import path, one-word lib export change; patches; records); (b) CEREMONIES FORGED — BurstAward = steel toast (record→plate w/ groove-draw, patch→tag w/ pip-slam; GSAP settle), MedalAward = THE COIN STRIKE (timeline: die drops strikeIn → sfx 'stamp' AT impact [moved out of host] → flash+shake → coin cools brightness 2.2→1 → verdict clip-path engraves → ROLL ON; metal by medalTier 1-4 incl platinum; confetti deleted), host slide-in sfx pow→stamp, rankup kept for trophy/recap takeovers (those two ceremonies + RecapCeremony/TrophyCeremony rework = G3 with cards). Icon param kept on MedalAward for API parity (void).****G1a SHIPPED (PR #371 → `69264fc`, tagged v1.150.0): metal tier display names live (PRESTIGE_META: Bronze/Silver/Gold/Platinum; lib keys frozen) + all renames swept across engines/tests/guides (498/498 green). COLLISION ADJUSTMENTS flagged to Jason: records own Best Tank/Longest Haul/Cheapest Miles → truck patches became High-MPG Tank / Big Haul / Cheap Tank; Iron Week → Big Week (adaptive bar — hardcoded 'Five-Load' would lie). G1b NEXT (issue #370 still open, branch fresh from main): the Forge Room page — TrophyHallPage (400L: fetches trophies/drivers/trucks/fuel/obligations, computeAllStatuses w/ progress; the AI-art hall STAYS until G4 re-brief) becomes the MONUMENTS rack; add COINS (medals + prestige ladder + rank), TAGS (patches w/ pips + hint ghosts), PLATES (records + blank plates) racks above it, all ghost-ruled; forge components needed: SteelTag/RecordPlate/GhostSlot (Coin.tsx exists on parked S10 branch ds-v2-s10-agents — cherry-pick or recreate); route /trophy-room full-bleed; Guide awards section regenerate-from-catalog deferred to G2 if budget.****GAME-ARC DECISIONS APPROVED (Jason: "I like it all"): TIERS = METALS — Bronze/Silver/Gold/Platinum (platinum keeps the holo-foil finish; lib keys contender/all-star/champion/legend stay frozen, display names change in PRESTIGE_META); RENAMES approved: Rainmaker→Big Month, Iron Week→Five-Load Week, Trailblazer→48 States, Feather Foot→Best Tank, Iron Horse→Big-Mile Month, Marathon→Longest Haul, Thrifty→Cheapest Miles, Relentless→Days Under Load, Iron Streak→Target Streak (obvious names KEPT: Mile Club, Freight Hauler, Big Earner, Long Hauler, Wide Load, Super Load, Coast to Coast, Debt Crusher, trophies); GHOST RULE approved — every unearned award renders dashed w/ plain-words criterion + live meter (hint/adaptive-bar data already exists in lib, just never surfaced); TROPHY ROOM → THE FORGE ROOM (4 racks: coins/tags/plates/monuments, driver+truck+trailer, Brandie's mirror on her card; Guide awards section generates from the catalog). FORGE ROOM MOCKUP delivered + verified: https://claude.ai/code/artifact/0d1c22fb-1ea8-4fe6-8c9c-91a62087ecdf (build note: earned-tag chips need slot padding-top so they don't kiss the plate edge). AWAITING nod → build slices: G1 Forge Room page + display renames + ghost states; G2 ceremonies (strike/pops GSAP); G3 player/dispatcher cards; G4 Hall R3F + art re-brief; then S13 cleanup.** **GAME ARC OPENED (Jason 2026-08-09): the deferred second half. Directives: (1) prestige tiers (contender/all-star/champion/legend) must work with the Coin + theme — rework NAMES if needed (display names live in PRESTIGE_META, components layer; lib keys can stay); (2) FULL achievement-system rework — what they are, names (obvious-or-rename), how displayed, how the user DISCOVERS how to earn them (drivers AND dispatcher) — all on the current theme. This REOPENS the awards lib by owner's order (criteria changes = lib changes + tests). Brandie test governs every name. S10 agents-restyle PARKED mid-branch ds-v2-s10-agents (Coin.tsx + RatingStamp v2 built+green, uncommitted; AgentsPage untouched — failed assert aborted before write). Game arc flow: catalog evaluation → tier + system proposal → mockups (Forge Room = reworked Trophy Room as the one catalog w/ ghost/locked states + progress) → build slices.** **S9 POLISH SLICE (Jason's punch-list 2026-08-09, work order = build+merge): (1) PULSE BUG — projected $16,806 double-counts delivered loads (earned+weekBooked overlap); replace with SMART projection: new lib/metrics/weekProjection.ts + clock-fixed tests — each load counted ONCE by delivery_date within [weekStart, weekStart+7): delivered=earned, in-transit/booked delivering in-window=incoming; projection drives meter ghost + record tag + subline. (2) MONEY — notes plate dead because P&L uploads at month END: estimate the running month = MTD net income (loads, same basis) × median operating margin of trailing complete months; label 'estimated · from your typical margin'; real P&L takes over when it lands. (3) FLEET — mile-club chase is DEAD (506 wks to 1M, truck sold by then): rig plate headline → COST TO RUN $/mi + trend, meter → NEXT SERVICE INTERVAL consumed (hot near due), odometer demoted to stat line, mile-club to quiet badge; right-rail 'Days out' DROPPED (dups Home-time KPI) → 'Miles this year'; 8-week heatmap grid must STRETCH to its full-width card (fluid cells, not fixed 10px). (4) LOAD DETAIL — drop StatusBadge tags when stamp exists (stamp-only, badges only for unstampable states); MissionMap retheme to Night Cab (same logic); consolidate little cards → bigger grouped boards (reduce scroll); REVENUE AS A MANIFEST (B-direction dotted-leader document style in Night Cab tokens); PLUS the S8.1 deferred: status controls hosted on the timeline + rate-grade chip (basis via useLoads+useRateTargets on detail, like DispatchDashboard does).** **S9 PROGRESS (branch ds-v2-s9-polish, committed `4a091fc`, 498 tests green): DONE — (1) lib/metrics/weekProjection.ts + 5 tests, wired into PulseTab (meter ghost/record tag/subline use in-window incoming; double-count dead); (2) Money notes ESTIMATE (mtdIncome × median margin of trailing ≤6 complete months, 'estimated' labels, real P&L takes over); (3) Fleet rig plate reworked (cost-to-run headline w/ parts sub + odometer/club demoted to stat line; meter = service-interval consumed w/ 'due' marker [dues now carry item for interval_miles]; right rail Days-out→Miles/month) + heatmap stretched (fluid 1fr columns, 16px rows, % month labels); (4a) detail stamp-only statuses (badges only when stamp null); (4b) MissionMap rethemed (16 swaps: well canvas, hairline states, chart-amber loaded leg, faint dashed deadhead, amber-hi reticle/labels). REMAINING S9 (then merge as one PR): LoadDetailPage consolidation — section map: Route Panel span-2 @614 (map) / Fleet @647 / Revenue @708 / Mileage @738 / Cargo @766 / four unidentified Panels @784,824,864,914 (read them: likely dates/detention/payment/notes) / Update status span-2 @928 / Accessorials Panel @964. PLAN: ①ROUTE board = map + Mileage rows as stat strip under it; ②MANIFEST board = Revenue + Accessorials editor merged, dotted-leader document style (B-direction in NC tokens), customer total + YOUR NET well — keep create/patch/delete accessorial handlers verbatim; ③RIG&CARGO board = Fleet + Cargo merged; ④group remaining small panels sensibly (dates+detention together, Notes stays); ⑤Update-status → timeline-hosted controls (booked→picked→delivered→paid stations; advance button on next station; mark-paid = the paid station; keep all handlers) ; ⑥grade chip (needs useLoads+useRateTargets import on detail — heavier fetch, accepted). **S9 SHIPPED: PR #367 merged (`23d017a`), tagged v1.148.0, 498 tests. ALL punch-list items landed: smart projection (weekProjection.ts+tests) · Money estimate (median-margin, labeled) · rig plate = cost-to-run + service-interval meter (mile-club demoted; Days-out dedup; heatmap stretched) · detail: stamp-only statuses, MissionMap rethemed, cards consolidated (Route+shipper/receiver/mileage; rig+cargo/broker), SETTLEMENT MANIFEST revenue (dotted leaders + net well) w/ accessorials as second leaf (editors verbatim), journey timeline hosting status section, booked-grade chip (scoreLoad + VERDICT_META, basis via useRateTargets(allLoads)). **AGENTS PAGES EVALUATION delivered (2026-08-09): LIST (255L) — 5 KPIs (rate-leader/oversize/concentration/going-cold/roster) DUPLICATE the dashboard Agents tab verbatim (lifted there in S6) → REC: answering line over the filtered roster (N agents · 90d $ through them · median $/mi), KEEP tier chips (hot/warm/cold w/ counts → heat-cell accents) + table/cards view toggle (parity), v2 skin, doors. DETAIL (438L) = the agent PLAYER CARD: header + RatingStamp (comic→DieStamp family) + Edit-rating (RatingForm modal — parity) + PRESTIGE_META badge (medallions → STRUCK COINS = first production Coin forge component) + stats Panel + TrophyCase (honors/season/live → machined case) + activity timeline (notes+rating history) + NOTE COMPOSER (textarea+initials+Add via createAgentNoteService — parity) + loads-through-agent table (→ answering-line treatment). Agent CREATION lives in LoadForm flow (createAgentService), NOT on these pages — nothing to preserve there. GAME-AS-DATA adds: your-median-with-them vs overall delta; best-load-together record line. Jason: 'go ahead with the mockups' → AGENTS PAIR MOCKUP delivered + BOTH views render-verified: https://claude.ai/code/artifact/73832cf2-a7be-4b72-a168-5969e377f905 — LIST (answering line w/ live tier re-sum, heat-cell tier chips, Table/Cards toggle both live, star ratings + freshness dots + OVR badges, rows drill) + DETAIL/player-card (gold prestige COIN w/ reeded edge, rating DIE-STAMP in statusbar [build note: tune stamp contrast slightly], median-with-him delta cell, BEST LOAD TOGETHER record plate, machined trophy case w/ Q2-champ coin + live P1 line, note composer + activity timeline, loads-through-agent w/ mini answering line). **S10 SHIPPED (built+merged): #368 → PR #369 (`af6fcdb`), tagged v1.149.0, 498 tests. AgentsPage: answering line (filtered rows: count · Σ card.revenue 90d · median-of-medians $/mi), REAL filter taxonomy kept (All/Oversize/Specialty/Standard/Top-pick/Cold — richer than mockup trio), search well, table/cards toggle, full-bleed; roster/byId/rateLeader KPI vars pruned. AgentDetailPage: full-bleed, RatingStamp v2 (die-stamp, same API/labels/semantic colors — font-comic OFF this surface), avg-rate Kpi + vs-overall delta (useRateTargets(allLoads) — page already had allLoads!), BEST LOAD TOGETHER plate, forge/Coin.tsx MINTED (reeded-edge challenge coin) debuting in TrophyCase (gold/silver Q results = coins); PRESTIGE BURST KEPT deliberately (real tiers contender/all-star/champion/legend-w/-holo-foil — earned identity too good to flatten; told Jason). AgentTable/AgentCard/TrophyCase swept. AppLayout /agents full-bleed. REMAINING COMIC SURFACES: AwardPopHost/ceremonies, PlayerCard/driver pages, Garage/RigCard, Recap, TrophyRoom, RubberStamp residuals, Bangers font — the GAME-SURFACES ARC (ceremonies GSAP w/ Forge strike, recap plaques, Trophy Hall R3F + art re-brief, S13 cleanup). NEXT: remaining ops pages evaluations (trips/vendors/facilities/fleet-pages/compliance/expenses/market/score) OR the game arc — Jason's pick.** Remaining arc after S7: detail pages (loads/agents/etc per-page protocol), then game surfaces (ceremonies GSAP=The Forge build, forge hardware components, recap plaques, Trophy Hall R3F + art re-brief, S13 cleanup: drop Bangers/v1 tokens/dead comic components/FULL_BLEED list). (Dispatch-board mockup drafted pre-protocol in scratchpad — re-cut under the protocol when her slice comes.) ⚠ NOTE: this file's snapshot was STALE at audit time — said v1.90.0/#268-Phase-B-active, repo actually at **v1.139.0** (PR #348) with unlogged pages (DispatchDashboard [Brandie's own board, isDispatcher() branch], Garage, Market, Vendors, Facilities, ScoreLoad, PerDiem) and the owner dashboard now a 5-tab shell (Pulse/Money/Lanes/Agents/Fleet). Sections below describing "current" work are ~50 versions behind; trust git history over them.
- **2026-07-15** — **Big build session (RLS #240 + v1.86.0→v1.90.0).** SHIPPED: (1) **RLS hardening #240** (PR #261, migration 042) — enabled Row-Level Security on all 14 unprotected public tables; best-effort `rls_on_create_table` event trigger auto-enables RLS on new tables but is **superuser-gated → live on dev, skipped on prod**; real enforcement = the CLAUDE.md convention (every table-creating migration adds `ENABLE ROW LEVEL SECURITY`). Dev+prod both 0 unprotected. (2) **Driver-page cluster #230/#245/#233 → v1.86.0** (PR #262): "Form" grade → "Overall"; separate **Oversize + Heavy-haul** equipment strips (blue; show at ≥10 loads of that type, "specialist" at ≥40% & ≥5); **Hometime** chip (days since last per-diem `home` mark, 21-day default threshold on Settings). (3) **Structured load dimensions #244 → v1.87.0** (PR #263): free-text `dimensions` → `length_in/width_in/height_in` (inches; ft+in inputs); migration 044 add+backfill (7 loads hand-converted, verified reconstruct exactly), 045 drops old column (**expand/contract** — 045 applied only after deploy); over-8'6" width hint. (4) **Operation setting + oversize achievements #231 → v1.88.0** (PR #264): `operation` on settlement_schedules (migration 046, default flatbed); **blue operation-specific patch set** gated to open-deck ops — Wide Load (12' floor), Long Load (80'), Mountain Mover, **Super Load** (structural; real superload thresholds researched: 16'W/16'H/150'L/200k lb); universal patches stay amber. (5) **Days-based truck utilization + achievements → v1.89.0** (PR #267): reworked utilization from weeks-binary (pegged 100%) to **under-load-days ÷ window-days** (window = later of in-service & first logged load); day breakdown (under-load/home/idle) on truck page; **Road Warrior** medal (70/80/85% benchmark) + **Relentless** patch (longest consecutive under-load run); read-only `/per-diem/home-days`. Real util = 73%. (6) **Profit-lever bottleneck — epic #268 Phase A → v1.90.0** (PR #269): card "Overall" → three graded levers **Rate / Utilization / Margin**, weakest **named as the bottleneck** ("Firing on all cylinders" when all target+); shared **lever engine** (`utilizationGrade`/`bottleneckLevers`) is the foundation for Phase B (in-month dollar bridge) + Phase C (month-end variance attribution), both **pending**. **CLOSED won't-do:** #239 (cash-flow forecast — flat Landstar settlement cadence = no timing variance to act on; duplicates QBO), #265 (home-aware util — kept asset-honest; home counts, exclusion would be gameable). **FILED:** #266 (load-detail fuel calc hardcodes $5.50/6.5mpg vs real ~$4.17/actual-MPG — **still queued**), #268 (the epic). **DECISIONS to preserve:** utilization stays **days-based & asset-honest** — don't re-propose weeks-binary or home-exclusion; util benchmark 80% cited (FleetRabbit/ATBS) in the Guide.
- **2026-07-08** — **WORKFLOW CHANGE (dash).** Jason installed + authed `gh` (repo scope) and set a new durable working method for dash: issue-driven build mode — he creates a GitHub issue, instructor builds it, they verify intent + formula correctness together, test on dev, then ship + bump version if warranted. This replaces teaching-mode-default FOR dash (see CURRENT FOCUS). Track B (dts-tools) stays teaching mode. Amended CLAUDE.md §2 (done 2026-07-08) to scope build-mode to dash (Track A) while keeping dts-tools (Track B) in teaching mode.
- **2026-07-08** — Jason clarified: he's not interested in CODING the trips feature (not lukewarm on it — it's a MUST-HAVE). Instructor-built slice 3 per his "build it for me": `latest-odometer` endpoint + prefill, `TripForm.tsx`, page wiring, `createTrip`/`getLatestOdometer` services, cross-field odometer validation. Strict build green, not committed. Next: Jason tests on dev, then ship (prod migration `023` + deploy). No keep/cut question — it ships.
- **2026-07-07** — Trips arc decisions locked. (1) Categories = `repositioning | home | shop | personal`, **required**, new `trip_purpose` ENUM+column, no "Other", bobtail rejected. (2) Jason's vocabulary: *repositioning* (empty move to a better market, no booked load) needs a logged trip; *deadhead* (per-load empty miles to reach the next pickup = odo delta − loaded) is automatic, NOT a trip — distinct concepts, do not conflate. (3) Instructor corrected a misread of `getDeadheadPercent` — it's per-load, NOT gap-inferred, so home/repositioning miles fall through a real gap; trips fill it via their own odometer window; loads+trips must tile. (4) Odometer_start auto-prefill from the truck's LATEST recorded odometer (max odometer_end across loads AND trips) is a MUST-HAVE this version (data protection; Brandie also inputs). Issue #3 now unblocked — next step is Jason writing the migration.
- **2026-07-05** — Established the two-file agent system: read `MEMORY.md` first, then `CLAUDE.md`, before any action. Update `MEMORY.md`'s BEHAVIOR LOG whenever new behavior/functionality is requested. (Initial setup.)
- **(baseline)** — Teaching contract: instructor mode (concept → Jason writes → review); no finished code unless explicitly asked; mini-project new concepts before real files; direct/no-sycophancy/minimal-formatting; recon before building; reinforce "too heavy for v1" restraint. (See CLAUDE.md.)

- **2026-09-06 · The documents layer — dash plugs into the DTS server (feat, v1.196.0).** Phase 5a of the server plan: dash now RENDERS what the server FILES. New `documents` table (065, applied to prod via MCP before code, RLS on, UNIQUE(load_id, sha256) = idempotent registration). GET /documents/load/:id (JWT, account-scoped — Brandie sees chips); POST /documents/ingest gated by requireServiceToken: constant-time X-Service-Token vs DASH_SERVICE_TOKEN env, acting as DASH_SERVICE_ACCOUNT (uuid-validated) — both REQUIRED on Railway or 401 fail-closed. server_url must live under https://files.dts-ops.co/ (trailing slash enforced in code so an env override can't reopen the suffix-host bypass). LoadDetailPage grows MANIFEST · PAPERWORK (chips via formatDoctype; documents state RESET on load-change so load B never wears A's chips; deliberately NO upload in dash — the DTS Inbox is the only door). server.js PORT is env-first now (server-plan item D). Guide: "Paperwork on a load" in the dock group. Server side: ingest agent dash_link (best-effort, audited, never blocks filing) + backfill.py idempotent reconciler (canonical-name doctype recovery — raw doctype_of() would have permanently mislabeled freight bills as "other"). Pre-merge review: 5 findings, all fixed, no blockers. Known pre-existing: GuidePage react-hooks/static-components lint error in an untouched awards helper. Tests 663.
